---
status: pending
quick_id: 260803-t7q
slug: align-the-compact-cahmls-header-brand-cl
phase: quick-260803-t7q
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/components/WorkspaceNav.svelte
  - tests/e2e/workspace-lifecycle.spec.ts
must_haves:
  truths:
    - "In the compact header, the camel, coordinator-status dot, and CAHMLS wordmark have equal edge-to-edge spacing and share one vertical center line."
    - "The brand cluster remains geometrically stable when the wordmark underline expands on hover or keyboard focus."
    - "The coordinator-status dot retains its online, connecting, and offline appearance, and the CAHMLS home link, tooltip, focus behavior, and surrounding header controls remain usable."
    - "The corrected brand cluster fits without document or header overflow at compact and desktop viewport widths."
  artifacts:
    - path: "src/components/WorkspaceNav.svelte"
      provides: "One flex-participating, vertically centered CAHMLS brand cluster whose decorative underline is removed from layout geometry"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Bounding-box regression coverage for equal gaps, aligned centers, hover/focus stability, and viewport fit"
  key_links:
    - from: "src/components/WorkspaceNav.svelte:.brand"
      to: "src/components/WorkspaceNav.svelte:.brand-camel/.brand-status-dot/.brand-wordmark"
      via: "A single flex gap positions all three in-flow children"
      pattern: "brand-status-dot"
    - from: "src/components/WorkspaceNav.svelte:.brand-wordmark"
      to: "src/components/WorkspaceNav.svelte:.brand-wordmark::after"
      via: "The underline remains visually anchored to the wordmark without contributing to its flex-item height"
      pattern: "brand-wordmark::after"
    - from: "tests/e2e/workspace-lifecycle.spec.ts"
      to: "src/components/WorkspaceNav.svelte"
      via: "Playwright measures the rendered brand-child rectangles before and after interaction at representative viewports"
      pattern: "CAHMLS home"
---

# Quick Task 260803-t7q: Align the compact CAHMLS header brand cluster

<objective>
Make the compact CAHMLS brand read as one evenly spaced, truly vertically centered camel/status/wordmark cluster without disturbing the coordinator-state semantics or the surrounding shared-worktree changes.

Purpose: The current WIP positions `.brand-status-dot` absolutely, reserves an oversized box and translates `.brand-camel` upward, and lets the block `::after` underline increase `.brand-wordmark` flex geometry. Those three independent offsets prevent the parent flex row from producing equal gaps and a common center.

Output: Focused CSS corrections in `WorkspaceNav.svelte`, a rendered-geometry Playwright regression in the existing workspace lifecycle suite, and compact/desktop screenshot evidence from the current build.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@src/components/WorkspaceNav.svelte
@tests/e2e/workspace-lifecycle.spec.ts
@playwright.config.ts

<interfaces>
- `WorkspaceNav.svelte` currently renders `.brand` as a `CAHMLS home` link whose direct visible children are `.brand-camel`, `.brand-status-dot`, and `.brand-wordmark`, in that order; retain that accessible link and child order.
- `homeCoordinatorReachability` and the existing `online`, `connecting`, and `offline` class directives own status truth. This task changes only layout geometry, not state derivation or status colors/animation.
- The current dirty diff also relocates coordinator reachability semantics, removes the old context dot, and preserves a screen-reader-only coordinator label. Keep all of that WIP intact.
- `.brand` already has `display: flex`, `align-items: center`, and a `.5rem` gap. The defect comes from children bypassing or distorting that shared geometry: an absolute dot, a translated/oversized camel box, and an in-flow block pseudo-underline.
- `tests/e2e/workspace-lifecycle.spec.ts` already uses `getBoundingClientRect`, compact viewport loops, `expect.poll`, and `expectNoDocumentOverflow`; extend those established patterns rather than adding a visual-test dependency or snapshot framework.
- The checkout is heavily dirty shared WIP. Modify only the named brand selectors and the focused new test; do not rewrite `WorkspaceNav.svelte`, replace existing tests, normalize unrelated formatting, or stage/revert other changes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Center and evenly space the rendered CAHMLS brand cluster</name>
  <files>src/components/WorkspaceNav.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <behavior>
    - Compact geometry: at 390x667, the horizontal gap from the camel box to the dot equals the gap from the dot to the wordmark within 1 CSS pixel.
    - Vertical geometry: camel, dot, and wordmark bounding-box center Y values differ by no more than 1 CSS pixel, with no child-specific translate used to manufacture the result.
    - Interaction stability: hovering and keyboard-focusing the CAHMLS home link still expands the underline and exposes the existing tooltip, but does not change either gap or any child center.
    - Responsive stability: the same alignment contract holds at 1440x900, while the compact viewport has no document overflow and every visible header control remains inside the viewport.
    - Status fidelity: the in-flow dot keeps the current online/connecting/offline class-driven color, halo, and pulse behavior without becoming accessible duplicate content.
  </behavior>
  <action>
Start by adding a focused Playwright test named `centers the compact CAHMLS brand cluster with equal spacing` in `tests/e2e/workspace-lifecycle.spec.ts`. Visit `/`, locate the `CAHMLS home` link and its three direct visible children, and use one browser-side geometry helper to return their rectangles, the two edge-to-edge gaps, center-Y deltas, brand bounds, and document/header fit. Assert equal gaps and center alignment within a 1px rendering tolerance at 390x667 and 1440x900. Record the initial geometry, then hover the link and focus it by keyboard; assert the existing tooltip becomes visible and the same geometry remains unchanged while the underline expands. Also assert the brand remains within its header and call the existing overflow helper for the compact viewport. The test must fail against the current absolute/translated/in-flow-underline geometry before changing CSS.

In the `WorkspaceNav.svelte` style block, keep `.brand` as the sole flex layout owner and retain its existing `.5rem` gap. Make `.brand-status-dot` a normal in-flow flex item in its existing DOM position: remove absolute coordinates and compensating translation, give it a fixed `.4rem` flex basis matching its rendered diameter, and retain its state classes, colors, halo, border radius, and connecting animation. Normalize `.brand-camel` to a square flex item sized from its existing 1.45rem emoji rather than the current 2.35rem-by-1rem reservation; center the glyph inside that box and remove the upward transform. Make `.brand-wordmark` a positioned, centered inline flex item whose line box participates in the row. Move `.brand-wordmark::after` out of flow with absolute positioning anchored below the wordmark, remove its layout-producing margin, and preserve its resting/hover widths, color, glow, transform origin, transitions, and reduced-motion rule. Do not add per-child horizontal margins, absolute offsets, or new transforms to imitate equal spacing; all horizontal spacing must come from `.brand`'s one gap.

Run the focused test until green, then run the project checks. Preserve the current Svelte state/markup, CAHMLS link semantics, tooltip relationship, dirty reachability changes, and all unrelated workspace lifecycle WIP exactly as found.
  </action>
  <verify>
    <automated>CI=1 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "centers the compact CAHMLS brand cluster with equal spacing" && pnpm lint && pnpm exec tsc --noEmit && pnpm test && pnpm build && git diff --check -- src/components/WorkspaceNav.svelte tests/e2e/workspace-lifecycle.spec.ts</automated>
    <human-check>Serve the just-built checkout and capture `.brand` screenshots at 390x667 and 1440x900 in Chromium, both at rest and with the CAHMLS home link focused. Inspect the four images at native scale: the camel, dot, and wordmark must read on one center line with matching whitespace on both sides of the dot; the underline must sit below the wordmark without pulling the text down; focus/hover must not jump the cluster; adjacent header content must remain unclipped. Record screenshot paths and the measured gap/center deltas in `260803-t7q-SUMMARY.md`.</human-check>
  </verify>
  <done>The current build renders camel, status dot, and CAHMLS text with equal flex-controlled gaps and a shared vertical center at compact and desktop widths; underline interaction leaves geometry unchanged; the existing status, tooltip, link, and responsive header behavior remains intact; focused automated checks and visual screenshot proof are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS layout -> user interaction | Brand geometry must not clip, displace, or cover the shared header's navigational controls at narrow widths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-t7q-01 | Denial of Service | `WorkspaceNav.svelte` compact header layout | low | mitigate | Measure brand and header bounds at compact and desktop widths, retain in-flow hit targets, and assert no page/header overflow after the CSS change. |
| T-t7q-SC | Tampering | package supply chain | low | accept | No package or lockfile change is required; implementation uses existing CSS, Svelte markup, and Playwright infrastructure only. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Task | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Align the compact CAHMLS brand cluster with equal spacing and true vertical centering | 1 | COVERED | One rendered slice corrects the three geometry causes and proves the resulting layout. |
| CONTEXT | — | Status dot is absolute and bypasses the parent gap | 1 | COVERED | The dot becomes the fixed-size middle flex item. |
| CONTEXT | — | Camel is manually translated upward and reserves asymmetric width | 1 | COVERED | The emoji receives a centered square box with no transform. |
| CONTEXT | — | Wordmark pseudo-underline affects flex geometry | 1 | COVERED | The underline is absolutely anchored to a positioned wordmark without changing its measured height. |
| CONTEXT | — | Preserve heavy shared dirty WIP | 1 | COVERED | Actions and diff checks are explicitly scoped to the two named files and the current selectors/test. |
| CONTEXT | — | Visual browser proof is required | 1 | COVERED | Compact/desktop rest/focus screenshots plus recorded gap/center measurements are completion evidence. |
| REQ | — | Roadmap requirement IDs | N/A | N/A | This is an isolated quick task outside roadmap requirement assignment. |
| RESEARCH | — | Research findings | N/A | N/A | Established CSS and Playwright patterns suffice; no external dependency or research phase is needed. |

<verification>
## Verification Matrix

| Capability | Evidence | Expected result |
|------------|----------|-----------------|
| Equal spacing and vertical centering | Focused Playwright geometry test | Both edge gaps match within 1px and all three center-Y values match within 1px at compact and desktop sizes. |
| Stable underline interaction | Same test before/after hover and keyboard focus | Tooltip/underline interaction remains visible without changing the measured item rectangles. |
| Responsive safety | `expectNoDocumentOverflow`, brand/header bounds, visible-control checks | The 390px header and document do not overflow or clip interactive controls. |
| Current project behavior | `pnpm lint && pnpm exec tsc --noEmit && pnpm test && pnpm build` | Existing lint, types, unit tests, and production build remain green. |
| Visual quality | Four Chromium screenshots from the current build | Compact and desktop brand clusters show matching whitespace, one optical center line, and no hover/focus jump. |
| Shared-worktree hygiene | Scoped `git diff --check` plus manual diff review | Only the intended selector block and focused test are added; all pre-existing dirty WIP remains present. |
</verification>

<success_criteria>
- The camel, coordinator-status dot, and CAHMLS wordmark are three in-flow flex items separated only by the parent `.5rem` gap.
- Their rendered vertical centers align within 1 CSS pixel at 390x667 and 1440x900.
- The wordmark underline remains below the text and animates on hover/focus without contributing to flex height or moving the cluster.
- Online, connecting, and offline status visuals, screen-reader text, home navigation, tooltip, and reduced-motion behavior remain unchanged.
- The compact header fits the viewport with no clipped controls or document overflow.
- Focused e2e coverage, project checks, and compact/desktop screenshot evidence are complete without a dependency or lockfile change.
</success_criteria>

<output>
Create `.planning/quick/260803-t7q-align-the-compact-cahmls-header-brand-cl/260803-t7q-SUMMARY.md` when done.
</output>
