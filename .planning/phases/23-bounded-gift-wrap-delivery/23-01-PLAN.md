---
phase: 23-bounded-gift-wrap-delivery
plan: "01"
status: checked
requirements: [RELAY-01, RELAY-02, RELAY-04]
---

# Plan 23-01: Bounded relay publication

1. Add a browser-safe bounded publication owner in `src/lib/relay-pool.ts` with linked cancellation, explicit primary/local budgets, tracked in-flight controllers, deterministic retry backoff, and injected test seams.
2. Readiness-gate optional localhost publication with a short WebSocket probe and positive-only cache; retain detached local fan-out without letting it affect primary success.
3. Emit secret-safe diagnostic records for attempts and terminal outcomes, and wire coordinator transport diagnostics without logging encrypted content.
4. Add focused unit tests for offline localhost, healthy remote isolation, transient primary recovery, terminal budgets, disconnect abort, and diagnostic fields.

## Goal-backward check

Every immortal path is owned by an abort controller, every outer loop is finite, and optional-local readiness is proven before fan-out. The plan changes no ContextVM wire schema. **Verdict: PASS.**
