---
phase: 23-bounded-gift-wrap-delivery
plan: "02"
status: checked
requirements: [RELAY-03, RELAY-05, RELAY-06]
---

# Plan 23-02: Lifecycle and interoperability proof

1. Link each `ChatCoordinatorClient` lifecycle to request cancellation so room stop and transport replacement terminate pending calls and publications.
2. Extend room-session concurrency tests to prove multiple timer ticks cannot overlap an unresolved sync and stop closes the pending client exactly once.
3. Run focused transport/room tests, full project gates, live upstream parity, and pinned canonical Cordn interoperability; record requirement evidence and any non-blocking warnings.

## Goal-backward check

The plan tests both the low-level leak and the user-visible polling amplifier while preserving the required wire paths. **Verdict: PASS.**
