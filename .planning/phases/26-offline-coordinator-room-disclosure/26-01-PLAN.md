---
phase: 26-offline-coordinator-room-disclosure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CoordinatorRoomCard.svelte
  - tests/e2e/workspace-lifecycle.spec.ts
autonomous: true
requirements:
  - SIDE-07

must_haves:
  truths:
    - "SIDE-07: only a remote coordinator card in the exact offline state with at least one room rests as the grammatically correct `1 chat offline` or `{N} chats offline` summary; the count uses every known active room, not the five-row subset."
    - "Hovering anywhere inside an eligible card or focusing its single group stop reveals the preserved room rows, and pointer/focus traversal across rows, favorites, continuation controls, and menus does not collapse them."
    - "At rest, hidden historical-room controls are absent from the accessibility tree and tab order; after card focus they are available to the next Tab, and leaving the whole card restores the compact summary without trapping or moving focus."
    - "Disclosure enter motion uses only opacity plus translateY(-4px) over 150ms ease; exit fades opacity over 150ms before removal, while reduced-motion insertion and removal are immediate and transform-free."
    - "The existing five-room visible subset, active/reveal-room substitution, Show-N-more expansion, room navigation/actions, and favorite focus restoration remain operational after disclosure."
    - "Local, Favorites, online, connecting, unknown, and zero-room cards retain their existing presentation and behavior, and disclosure writes no room, favorite, ordering, reachability, membership, or history state."
  artifacts:
    - path: "src/components/CoordinatorRoomCard.svelte"
      provides: "Exact offline eligibility, compact count, pointer/focus lifecycle, accessible group instruction, inert exit state, and layout-safe motion around the existing room renderer"
    - path: "src/components/HostWorkspace.svelte"
      provides: "Existing authoritative local/presentation/status/full-room inputs and programmatic favorite reveal wiring; consumed unchanged"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Browser evidence for copy, accessibility tree/tab order, hover/focus lifecycle, motion preferences, five-room behavior, favorite restoration, and non-offline regressions"
  key_links:
    - from: "src/components/HostWorkspace.svelte"
      to: "src/components/CoordinatorRoomCard.svelte"
      via: "existing local, presentation, externalCoordinatorReachability status, full server.rooms, activeRoomKey, and revealRoomKey props"
      pattern: "CoordinatorRoomCard|externalCoordinatorReachability|revealRoomKey"
    - from: "src/components/CoordinatorRoomCard.svelte"
      to: "its existing visibleRooms/room action renderer"
      via: "the disclosure gates whether the current room-list block is mounted without changing room identity, ordering, callbacks, or the five-room projection"
      pattern: "visibleRooms|hiddenCount|roomIdentityKey|onOpen|onFavorite"
    - from: "tests/e2e/workspace-lifecycle.spec.ts"
      to: "src/components/CoordinatorRoomCard.svelte"
      via: "real HostWorkspace rendering seeded by seedJoinedRoom and exact coordinator-card/room roles"
      pattern: "seedJoinedRoom|coordinator-card|channel-row"
---

<objective>
Make offline remote coordinator cards compact at rest while keeping every historical chat immediately reachable through deliberate pointer or keyboard interaction, satisfying SIDE-07 and the approved Phase 26 UI contract.

Purpose: Reduce sidebar weight without changing room authority, reachability, navigation, favorites, or any non-offline coordinator presentation.
Output: One production card disclosure lifecycle plus focused and regression browser evidence in the existing workspace suite.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-offline-coordinator-room-disclosure/26-CONTEXT.md
@.planning/phases/26-offline-coordinator-room-disclosure/26-UI-SPEC.md
@.planning/phases/25-favorite-groups-invite-availability/25-01-SUMMARY.md
@src/components/CoordinatorRoomCard.svelte
@src/components/HostWorkspace.svelte
@tests/e2e/workspace-lifecycle.spec.ts

<interfaces>
Existing contracts the executor must preserve:

From src/components/CoordinatorRoomCard.svelte:
```typescript
interface Props {
  pubkey: string;
  label: string;
  status: "online" | "connecting" | "offline" | "unknown";
  statusLabel: string;
  local?: boolean;
  presentation?: "coordinator" | "favorites";
  rooms: CoordinatorRoomItem[];
  favoriteRoomKeys?: readonly string[];
  unreadCount?: number;
  activeRoomKey?: string;
  revealRoomKey?: string | null;
  onOpen: (room: StoredRoom) => void | Promise<void>;
  onRemove: (room: StoredRoom, origin: HTMLButtonElement) => void;
  onFavorite?: (room: StoredRoom, origin?: HTMLButtonElement) => void;
  onRevealHandled?: (roomKey: string) => void;
}
```

The current five-room projection is authoritative: Favorites, explicit expansion, or at most five rooms returns all applicable rows; otherwise the first five are shown with an active/reveal-room substitution, and `hiddenCount` is derived from the resulting subset.

From src/components/HostWorkspace.svelte:
```typescript
type CoordinatorReachability = "online" | "connecting" | "offline" | "unknown";
function externalCoordinatorReachability(pubkey: string): CoordinatorReachability;
```

HostWorkspace already renders Favorites with `presentation="favorites"`, the home card with `local`, and remote cards with `status={externalCoordinatorReachability(server.pubkey)}`, full `server.rooms`, `activeRoomKey`, and `revealRoomKey`. These existing inputs are sufficient; keep HostWorkspace read-only.

From tests/e2e/workspace-lifecycle.spec.ts:
```typescript
async function seedJoinedRoom(page: Page, title: string, coordinatorPubkey?: string): Promise<void>;
async function expectNoDocumentOverflow(page: Page, viewport: { width: number; height: number }): Promise<void>;
```

Installed Svelte 5.56.3 supports top-level `$props.id()`; use it to create a stable component-local instruction ID rather than deriving DOM IDs from coordinator or room identifiers.
</interfaces>
</context>

<task_dependencies>

| Task | Needs | Creates | Dependency reason |
|------|-------|---------|-------------------|
| 1 | Existing card props, room renderer, HostWorkspace reachability wiring, and `seedJoinedRoom` fixture | One real offline summary → focus/hover → exact room navigation tracer with accessible lifecycle | Leads with the riskiest end-to-end boundary: rows must enter the DOM and tab order before onward keyboard navigation |
| 2 | Task 1 disclosure state machine and selectors | Motion/reduced-motion closure plus five-room, favorite, responsive, and non-offline regression coverage | Expands the proven lifecycle and updates existing workspace scenarios that intentionally access offline remote rows |

Execution order: Task 1 → Task 2. Both tasks are test-first and commit RED browser evidence before GREEN implementation. There are no package installs, external services, user setup steps, or HostWorkspace changes.
</task_dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one offline summary through keyboard and pointer room navigation</name>
  <files>src/components/CoordinatorRoomCard.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>26-CONTEXT.md in full; 26-UI-SPEC.md Scope and Disclosure Rule, Interaction and Accessibility Contract, Copywriting Contract, and Non-Goals; CoordinatorRoomCard Props, visibleRooms, revealRoomKey effect, fieldset markup, and room callbacks; HostWorkspace remote/local/Favorites CoordinatorRoomCard call sites; workspace-lifecycle seedJoinedRoom and exact room-card locators.</read_first>
  <behavior>
    - "RED browser case: two exact-offline remote cards with one and multiple seeded rooms show `1 chat offline` and `{N} chats offline`, using total room count; neither exposes a room button, favorite, action menu, or continuation control at rest."
    - "The eligible fieldset is one `tabindex=0` group whose existing legend remains its accessible name and whose unique aria-describedby text uses correct `Focus to reveal 1 offline historical chat.` / `Focus to reveal N offline historical chats.` grammar."
    - "Focusing the card mounts its current room list before the next Tab; Tab reaches the first exact room primary button, and Shift+Tab/pointer movement among card descendants keeps the list mounted."
    - "Hovering the legend or card body reveals the same rows, moving onto a row/star/menu does not collapse them, and leaving the whole card collapses only when focus is also outside."
    - "Clicking the non-interactive summary/background does not navigate; activating an existing room primary button still opens that exact cached historical chat."
  </behavior>
  <action>Write and commit the focused Playwright failure `offline coordinator disclosure keeps history reachable by pointer and keyboard` before production changes. In CoordinatorRoomCard, derive eligibility from the complete conjunction `presentation === "coordinator"`, `local === false`, `status === "offline"`, and `rooms.length > 0`; no other state may enter the disclosure path. Add a top-level `$props.id()`-based description ID and one card-owned lifecycle with compact, entering/revealed, and exiting states plus pointer-inside/focus-inside facts. Make only eligible fieldsets `tabindex="0"` and attach the visually hidden, singular/plural keyboard instruction through `aria-describedby`; preserve the fieldset's current legend-derived accessible group name. At compact rest, mount the neutral 44px summary and do not mount the room list or continuation control, making existing room controls absent from both the accessibility tree and tab sequence. On pointer enter or focus in, mount the unchanged `visibleRooms` renderer in normal flow; on focus out, use `relatedTarget` and fieldset containment, and collapse only when the new focus target and pointer are both outside. Keep the summary text in the accessibility tree while it visually recedes during disclosure, without a live region or toggle semantics. Preserve every existing room key, callback, disabled state, unread/favorite/action control, active-row substitution, and navigation path. Extend the existing revealRoomKey effect so a matching programmatic favorite restoration reveals the offline card before `onRevealHandled` lets HostWorkspace query and focus the source row. Do not modify HostWorkspace, persist disclosure state, add click-to-toggle behavior, move focus, trap focus, or add an Escape handler.</action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4261 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "offline coordinator disclosure keeps history reachable by pointer and keyboard" --workers=1</automated>
  </verify>
  <done>SIDE-07 tracer is green: exact-offline remote cards alone collapse with grammatical total counts, pointer and keyboard reveal the same real controls without traversal races, compact rows are absent from accessibility/tab order, and exact historical room navigation still works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close motion, five-room, favorite, and non-offline regressions</name>
  <files>src/components/CoordinatorRoomCard.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>26-UI-SPEC.md Visual States and Motion, Responsive and Content Constraints, zero-one-many/partial/overflow considerations, and Non-Goals; Task 1 lifecycle; CoordinatorRoomCard existing `expanded`, `visibleRooms`, `hiddenCount`, and CSS; workspace-lifecycle tests named `long room navigation stays operable and contained`, `coordinator cards keep stable order and collapse dead groups into history`, `favorite menu duplicates the exact room and survives reload`, `favorite star mirrors duplicate state and keeps sidebar controls touch-safe`, and `unfavoriting a collapsed source keeps its row visible and restores focus`.</read_first>
  <behavior>
    - "RED browser case: an offline remote card with seven rooms reports `7 chats offline`; first reveal shows the existing five rows and `Show 2 more`, expansion shows all seven plus the approved offline `Show fewer chats` copy, and exact active/reveal-room substitution remains intact."
    - "No-preference motion evidence: reveal animates only opacity 0→1 and transform translateY(-4px)→none for 150ms ease; exit animates only opacity to 0 for 150ms, with exiting controls inert, aria-hidden, and pointer-inert before removal."
    - "Reduced-motion evidence: reveal/collapse has no transition, animation, or transform and mounts/removes the final state immediately while keyboard navigation and room actions remain available."
    - "Online, connecting, unknown, local (including local offline), Favorites, and zero-room cards retain their current rows/empty state, tab behavior, copy, ordering, styling, and controls without receiving the offline summary."
    - "Existing favorite removal can reveal and focus an offline source row; existing long-room/five-row, active-room, menu, navigation, and 320px overflow assertions remain green after their tests deliberately focus or hover an offline card."
  </behavior>
  <action>Commit the failing browser coverage `offline coordinator disclosure preserves motion and five-room behavior` before expanding production code. Implement enter/exit presentation around the Task 1 lifecycle with normal-flow wrappers and CSS animation/transition limited to opacity and `translateY(-4px)` over exactly 150ms ease; do not animate height, max-height, grid tracks, width, spacing, borders, or positioning. When collapse starts, retain the fading pixels for 150ms but immediately mark the room-list/continuation subtree inert and aria-hidden and disable pointer events so transparent controls cannot be focused, clicked, or exposed; cancel the pending removal if pointer/focus deliberately re-enters. Use the runtime reduced-motion media result to skip lifecycle delay as well as a `prefers-reduced-motion: reduce` rule that removes animation, transition, and transform. Keep the approved compact 4px gap, 4px/8px padding, 44px minimum, 10px semibold count, one-line summary, and neutral `#718277`/`#9aac9f`/`#59675f` palette; retain the existing card border and apply only the sparse green `:focus-visible` outline without layout shift. Preserve `visibleRooms` and `hiddenCount` rather than slicing a second time; use `Show fewer chats` only in the eligible offline disclosure and retain existing non-offline continuation copy/presentation. Update the named existing workspace tests wherever their intent requires an offline remote row so they deliberately focus or hover the owning card before querying that row; do not weaken their original row count, navigation, favorite focus-restoration, action geometry, ordering, or overflow assertions. Add explicit checks for status changes out of exact offline, local offline, Favorites, empty remote cards, 320px bounds, repeated enter/exit cancellation, and absence of any localStorage mutation from disclosure.</action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4262 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "offline coordinator disclosure|long room navigation stays operable and contained|coordinator cards keep stable order|favorite menu duplicates|favorite star mirrors|unfavoriting a collapsed source" --workers=1</automated>
  </verify>
  <done>SIDE-07 is fully evidenced with exact 150ms layout-safe motion, immediate reduced motion, preserved five-room/favorite/focus/navigation behavior, 320px containment, and unchanged non-eligible card presentations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HostWorkspace projection → coordinator card | Asynchronous reachability plus full stored-room presentation crosses into the disclosure eligibility decision. |
| Pointer/focus events → mounted interactive subtree | Browser event ordering determines whether historical room controls exist in the tab order and remain reachable. |
| Exiting visual subtree → user input/accessibility APIs | Fading pixels may still be present in DOM while they must no longer accept activation or keyboard focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | Offline eligibility in `CoordinatorRoomCard.svelte` | medium | mitigate | Gate only on the exact existing presentation/local/status/room-count props; perform no reachability inference and add browser cases proving each excluded presentation remains unchanged. |
| T-26-02 | Denial of Service | focusout/pointerleave disclosure lifecycle | medium | mitigate | Track both pointer and focus containment, inspect focusout `relatedTarget`, cancel stale exit completion on re-entry, and test Tab/Shift+Tab plus pointer traversal through descendant controls. |
| T-26-03 | Elevation of Privilege | hidden/exiting room actions | medium | mitigate | Do not mount controls at compact rest; mark the exit subtree inert, aria-hidden, and pointer-inert before its 150ms visual removal so hidden controls cannot activate navigation or mutation callbacks. |
| T-26-04 | Information Disclosure | offline count and historical room labels | low | accept | The card displays only already-authorized local room history in its existing sidebar; disclosure adds no storage, logging, snapshots, transport, membership, or capability exposure. |
| T-26-05 | Repudiation | browser evidence | low | accept | Tests assert public UI roles, styles, counts, and navigation only; no invite secrets, private keys, decrypted messages, or raw transport errors are logged or snapshotted. |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Offline coordinators stay compact while historical chats remain reachable on deliberate hover or keyboard interaction | 26-01 | COVERED | Tasks 1-2 deliver the complete rest/reveal/exit outcome. |
| REQ | SIDE-07 | Compact grammatical offline count, pointer/focus reveal, restrained motion, and reduced-motion fallback | 26-01 | COVERED | Task 1 proves interaction/accessibility; Task 2 closes motion and regression behavior. |
| RESEARCH | — | No Phase 26 RESEARCH.md exists; Level 1 verification confirmed the installed Svelte 5.56.3 `$props.id()` rune and existing card/workspace/test patterns, with no new dependency | 26-01 | EXCLUDED | No research feature or constraint is missing. |
| CONTEXT | — | Replace exact-offline remote room rows at rest with `1 chat offline` / `{N} chats offline` | 26-01 | COVERED | Task 1 eligibility and copy. |
| CONTEXT | — | Card-wide hover/focus reveal remains open during descendant traversal | 26-01 | COVERED | Task 1 containment lifecycle. |
| CONTEXT | — | Existing navigation/actions survive without room, favorite, order, reachability, membership, or history mutation | 26-01 | COVERED | Both tasks preserve shared renderer/wiring and assert no persistence. |
| CONTEXT | — | Local, Favorites, online, connecting, and unknown cards remain unchanged | 26-01 | COVERED | Task 2 exclusion matrix. |
| CONTEXT | — | Layout-safe restrained motion becomes immediate under reduced motion | 26-01 | COVERED | Task 2 exact property/duration/media checks. |
| UI-SPEC | — | Resting controls absent from accessibility tree/tab order; card is one described group stop; focus reveal precedes onward Tab | 26-01 | COVERED | Task 1 browser contract. |
| UI-SPEC | — | 150ms opacity/4px-transform entry, opacity-only exit, inert fade, and immediate reduced motion | 26-01 | COVERED | Task 2 browser/CSS contract. |
| UI-SPEC | — | Total-count grammar, 44px compact summary, neutral tokens, 320px containment, and existing five-room continuation | 26-01 | COVERED | Task 2 geometry/style/five-room checks. |

Deferred/non-goal audit: no click toggle, persistent/pinned expansion, auto-expansion, new fetch/sync/join/leave/favorite behavior, room or coordinator reordering, live-region announcement, focus trap, Escape shortcut, new mobile control, overlay, package, or design-system change is planned.
</source_coverage_audit>

<verification>
Focused RED/GREEN gates:

1. `CI=1 PLAYWRIGHT_PORT=4261 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "offline coordinator disclosure keeps history reachable by pointer and keyboard" --workers=1`
2. `CI=1 PLAYWRIGHT_PORT=4262 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "offline coordinator disclosure|long room navigation stays operable and contained|coordinator cards keep stable order|favorite menu duplicates|favorite star mirrors|unfavoriting a collapsed source" --workers=1`

Complete repository gate before Phase 26 verification:

1. `pnpm lint`
2. `pnpm exec tsc --noEmit`
3. `pnpm test`
4. `CI=1 PLAYWRIGHT_PORT=4263 pnpm exec playwright test --workers=1`
5. `pnpm build`
6. `git diff --check`

`pnpm check:upstream` and `pnpm test:upstream-interop` are not required because this plan changes no `src/cordn/` code, coordinator method/contract, chat admission, encryption, or wire-protocol path.
</verification>

<success_criteria>

- SIDE-07: exact-offline remote cards with rooms rest as the correct total-count summary and reveal the established real room controls on card-wide pointer hover or one keyboard group stop.
- Accessibility: compact controls are absent from tree/tab order, description grammar is correct, onward/reverse Tab traversal is stable, exit never traps/moves focus, and the fading exit subtree cannot receive input.
- Motion: normal preference uses only the approved 150ms opacity/4px-transform entry and opacity exit; reduced motion has no animation/transform/delay.
- Regression: five-room expansion/substitution, favorite source focus restoration, exact navigation/actions, long labels, 320px containment, and all non-eligible presentations remain green.
- State/privacy: disclosure mutates no storage, room/favorite/order/reachability/membership/history data and introduces no package or secret-bearing test evidence.
- Focused commands and every complete repository quality gate pass before independent GSD verification and the required frontend UI review.
</success_criteria>

<output>
Create `.planning/phases/26-offline-coordinator-room-disclosure/26-01-SUMMARY.md` when done.
</output>
