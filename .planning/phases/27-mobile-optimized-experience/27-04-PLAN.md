---
phase: 27-mobile-optimized-experience
plan: "04"
type: execute
wave: 4
depends_on: [27-01, 27-02, 27-03]
files_modified:
  - playwright.config.ts
  - tests/e2e/mobile-optimized-experience.spec.ts
  - .planning/phases/27-mobile-optimized-experience/27-VERIFICATION.md
  - .planning/phases/27-mobile-optimized-experience/27-UI-REVIEW.md
  - .planning/STATE.md
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-05, MOBILE-06, MOBILE-07]
must_haves:
  truths:
    - "Dedicated mobile Chromium and mobile WebKit projects run with `isMobile: true`, `hasTouch: true`, and touch input rather than resized desktop mouse tests."
    - "Both engines complete fresh host setup, durable start/create/stop/restart/reload, and a real independent two-client invite/admit/encrypted-message/reaction journey by tap."
    - "Both engines prove room browser, sheets/dialogs, personal controls, Favorites, History, offline disclosure, room/participant/reaction actions, focus, dismissal, and target geometry."
    - "The required portrait, landscape, short-height, reduced visual viewport, 320px overflow, safe-area, and reduced-motion states remain usable."
    - "IndexedDB continuity and the full unavailable/denied/write/quota/corrupt/legacy/flush failure matrix terminate truthfully without secrets or recursive startup."
    - "All repository quality gates and both Cordn upstream interoperability gates pass after the phase."
    - "Independent phase verification and UI review pass, the tested branch is pushed, and one non-duplicate pull request is open and recorded."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Real touch-enabled mobile Chromium and WebKit projects alongside the desktop project"
    - path: "tests/e2e/mobile-optimized-experience.spec.ts"
      provides: "Complete host, two-client, overlay, geometry, accessibility, persistence, and failure-recovery acceptance evidence"
    - path: ".planning/phases/27-mobile-optimized-experience/27-VERIFICATION.md"
      provides: "Independent requirement-by-requirement verifier evidence"
    - path: ".planning/phases/27-mobile-optimized-experience/27-UI-REVIEW.md"
      provides: "Independent mobile visual and interaction review evidence"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/e2e/mobile-optimized-experience.spec.ts"
      via: "Named device projects supply actual touch/mobile contexts required by locator.tap()"
      pattern: "mobile-chromium|mobile-webkit|hasTouch|isMobile"
    - from: "tests/e2e/mobile-optimized-experience.spec.ts"
      to: "tests/e2e/mock-relay.ts"
      via: "Existing mock relay carries real Nostr/ContextVM invitation, admission, message, and reaction traffic between independent contexts"
      pattern: "startMockRelay"
    - from: "tests/e2e/mobile-optimized-experience.spec.ts"
      to: "src/cordn/coordinator/storage/indexedDbSnapshotStorage.ts"
      via: "Page-level IndexedDB/failure injection proves production storage lifecycle without reading secret-bearing payloads"
      pattern: "indexedDB|Storage needs attention"
---

<objective>
Prove the entire Phase 27 outcome in real touch-enabled mobile Chromium and WebKit contexts, then run every repository and Cordn interoperability gate.

Purpose: MOBILE-07 forbids claiming mobile support from resized desktop tests. The phase is complete only when real host and independent two-client journeys exercise every touch, navigation, responsive, persistence, failure, and wire invariant delivered by Plans 01-03.
Output: Two mobile Playwright projects, one dedicated acceptance suite with an explicit control/viewport/failure inventory, and full quality/upstream evidence.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-mobile-optimized-experience/27-CONTEXT.md
@.planning/phases/27-mobile-optimized-experience/27-UI-SPEC.md
@.planning/phases/27-mobile-optimized-experience/27-RESEARCH.md
@.planning/phases/27-mobile-optimized-experience/27-01-SUMMARY.md
@.planning/phases/27-mobile-optimized-experience/27-02-SUMMARY.md
@.planning/phases/27-mobile-optimized-experience/27-03-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-06-SUMMARY.md
@playwright.config.ts
@tests/e2e/workspace-lifecycle.spec.ts
@tests/e2e/established-installation-fixture.ts
@tests/e2e/mock-relay.ts
@tests/e2e/chat-user-interactions-fixture.ts

<interfaces>
Reuse these existing test seams; do not create a CAHMLS-only mock protocol:

- `installEstablishedInstallation(page)` seeds only already-approved public/local installation configuration and is suitable for post-setup cases; the fresh-host journey must still exercise the real first-run UI.
- `startMockRelay()` supplies the existing browser Nostr relay fixture used by workspace/chat suites. Use independent browser contexts/pages and production invite/admission/encryption code on both sides.
- Existing workspace helpers demonstrate start, stop, create, coordinator settings, exact room actions, viewport bounds, room seeding, and public coordinator identity reads. Extract/share only if doing so does not broaden file ownership; otherwise implement dedicated secret-safe helpers in the new spec.
- Mobile acceptance must interact through public roles/names and `.tap()` for primary mobile actions. Direct page evaluation is reserved for deterministic browser capability/failure injection and public geometry/state inspection, never for bypassing product interactions.
</interfaces>
</context>

<task_dependencies>

| Task | Needs | Creates | Dependency reason |
|------|-------|---------|-------------------|
| 1 | Completed Plans 01-03 plus installed Playwright | Real mobile projects and one complete Chromium host tracer | Proves touch config and the full single-client architecture before doubling engines/clients |
| 2 | Task 1 project/config/helpers and host tracer | WebKit parity, independent two-client journey, full interaction/viewport/storage matrix, and complete gates | Expansion reuses a proven journey and closes every requirement/source item |
| 3 | Task 2 green gates and committed implementation | Independent verifier/UI-review evidence, pushed branch, open PR, and recorded remote state | Shipping may occur only after the proven commit is independently reviewed and synchronized |

Execution order: Task 1 → Task 2 → Task 3. The suite may factor secret-safe local helpers inside its owned file, but it must not replace production invite, admission, encryption, persistence, or action paths with test-only equivalents.
</task_dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one complete durable host journey in a real mobile Chromium touch context</name>
  <files>playwright.config.ts, tests/e2e/mobile-optimized-experience.spec.ts</files>
  <precondition>The Playwright Chromium and WebKit browser binaries required by the repository's pinned @playwright/test version are installed; `pnpm exec playwright test --list` can enumerate projects.</precondition>
  <read_first>27-UI-SPEC.md Responsive Proof Contract host journey and interaction inventory; 27-RESEARCH.md Real touch projects example, Pitfall 6, A4, and validation map; current Playwright webServer/project config; workspace lifecycle fresh setup/start/create/stop/settings helpers and selectors; established-installation and mock-relay fixture APIs; Plan 01-03 summary evidence and new public accessible names.</read_first>
  <behavior>
    - "RED config assertion: `mobile-chromium` and `mobile-webkit` enumerate as distinct projects whose resolved contexts have `isMobile` and `hasTouch` true; desktop Chromium remains available."
    - "RED Chromium tracer starts from fresh browser storage, chooses an operator identity, names and starts the coordinator, observes truthful startup/persistence progress, creates a room, and opens its invite using `.tap()` for every primary action."
    - "The tracer opens/closes the shared Room browser, Favorites, History, notification feed/settings, profile/presence, coordinator settings, and one room/reaction action surface by tap, with only one modal at a time."
    - "It sends a real encrypted message, toggles a supported reaction through the mobile sheet, stops through `Stopping and saving…`, restarts, reloads, and returns to the same coordinator identity and exact room once from IndexedDB."
    - "At 390x844 the touched controls are each at least 44x44, document width does not overflow, drawer/sheets remain within the visual viewport, and no required action depends on hover."
    - "The test records/asserts only public UI state and opaque record counts/keys; it never emits a private key, invite URL, room secret, raw snapshot, raw browser exception, or decrypted message payload into logs/snapshots."
  </behavior>
  <action>Write the failing project/config and host-journey assertions before altering Playwright configuration. Add `mobile-chromium` and `mobile-webkit` projects using device descriptors present in the installed Playwright 1.61.0 package or equivalent explicit contexts; both must set mobile/touch semantics and use their real browser engines. Keep the existing desktop project. Confirm resolved project names with `--list`; do not install or upgrade packages. Create the dedicated spec and one Chromium host tracer that uses public accessible roles and `locator.tap()` for all primary mobile operations. Use direct evaluation only to inject the mock relay URL before load, inspect public geometry, count/index exact IndexedDB records without serializing their contents, and drive the documented VisualViewport/storage test seam. Exercise the real first-run identity/name, start, room creation, invite surface, shared browser, personal surfaces, message/reaction, flush-before-stop, restart, and reload paths. Fail on console/page errors after filtering only documented browser noise; explicitly scan safe UI/diagnostic output for prohibited secret categories without ever placing real secret values into failure messages. Keep retries off for the focused command and use deterministic fixture ownership/cleanup so a failure cannot leak contexts or relay servers.</action>
  <verify>
    <automated>pnpm exec playwright test --list &amp;&amp; CI=1 PLAYWRIGHT_PORT=4301 pnpm exec playwright test tests/e2e/mobile-optimized-experience.spec.ts --project=mobile-chromium --grep "complete durable host journey" --workers=1</automated>
  </verify>
  <done>MOBILE-07's tracer is green: a true touch/mobile Chromium context completes the real durable host lifecycle, exercises the shared mobile surfaces by tap, meets core geometry/containment requirements, and restores the exact IndexedDB-backed coordinator/room without secret-bearing evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close WebKit, two-client encrypted chat, responsive, storage-failure, and full-gate acceptance</name>
  <files>playwright.config.ts, tests/e2e/mobile-optimized-experience.spec.ts</files>
  <precondition>The focused mobile Chromium host tracer passes and both mobile projects enumerate.</precondition>
  <read_first>27-CONTEXT.md all five mobile acceptance journeys and non-goals; 27-UI-SPEC.md complete Responsive Proof Contract, overlay evidence, interaction target inventory, storage copy/failures, motion/accessibility evidence, and secret exclusions; 27-RESEARCH.md validation map, A4-A5, exact implementation sequence step 6, and upstream gates; Phase 24 two/three-context fixture summary; mock-relay and chat-user-interactions fixture public APIs.</read_first>
  <behavior>
    - "The complete host journey from Task 1 passes unchanged on mobile WebKit as well as mobile Chromium."
    - "In each engine, an independent host and invitee use a real canonical invite, request/approve admission, exchange encrypted messages and reactions both directions, navigate away/back through Room browser, use participant/room actions, and preserve active conversation across offline/reload transitions."
    - "In each engine, the host opens the public invite surface and taps its Copy invite action; the test proves success through a secret-safe copied/status signal without printing, snapshotting, or embedding the invite capability in an assertion message."
    - "Tap-only personal controls cover profile/presence, feed/settings, sound/channel overrides, invitation actions, Favorites, History, identity rotation entry/confirmation safety, offline disclosure, room management, and all setup/lifecycle controls."
    - "Drawer and every sheet/dialog category prove visible close/safe controls, safe outside tap, Escape focus restoration, platform Back top-overlay closure, one modal/inert background, internal long-content scroll, and destructive outside-tap refusal."
    - "The explicit target inventory proves every required control class is 44x44 at 390x844, 844x390, 390x520, 390x430 focused, and the 320px-width backstop; no document overflow or obscured focused input/primary action occurs."
    - "Reduced motion produces immediate drawer/offline/reaction final states with visible focus and unchanged operation."
    - "Persistence cases prove exact-key restore, ordered writes, flush-before-stop, open unavailable/denied, write/quota, corrupt/version, unscoped legacy ignore, flush failure, bounded retry, explicit temporary session, scoped corrupt deletion, and no recursive startup/false durable status."
    - "Full lint, type, unit, desktop/mobile E2E, build, diff, upstream parity, and pinned upstream interoperability commands pass."
  </behavior>
  <action>Commit the failing WebKit, two-client, viewport/target, overlay, and storage-failure cases before any production deviation; Plans 01-03 should already implement the behavior, so treat failures as evidence to fix only within those plans' owned contracts through normal deviation handling, not by weakening assertions. Parameterize the host journey over both named mobile projects. Build a real two-context flow per engine using the repository mock relay and production canonical invite/admission/MLS/ContextVM/Nostr paths: open the public invite surface, tap `Copy invite`, assert only the approved success status plus a boolean/non-empty clipboard result without placing the capability in output, retain the capability in memory only long enough to open it in the invitee, and never print or snapshot it. Approve through public host UI; assert visible sent/received message text only as end-user UI evidence and avoid dumping transport payloads. Exercise both-direction reactions and exact participant/room actions, shared browser navigation, offline/reload recovery, and state continuity. Add tables/helpers in the spec for every required touch-control class, overlay category, viewport dimension, and storage failure so omissions are visible and each row owns a concrete assertion. Drive 390x430 through the injected VisualViewport seam where a real virtual keyboard is unavailable while keeping a real focused-composer assertion in each device context. Storage injection may replace `indexedDB.open`/transaction results before application load but must exercise the production typed recovery state and product controls afterward. Assert exact approved copy, bounded attempts, no recursive start, record isolation/count only, and prohibited diagnostic categories. Finally run every complete repository and Cordn gate exactly as listed; do not mark the phase complete if a browser binary, service, or gate is unavailable—record the exact blocker and retain locally proven results.</action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4302 pnpm exec playwright test tests/e2e/mobile-optimized-experience.spec.ts --project=mobile-chromium --project=mobile-webkit --workers=1 &amp;&amp; pnpm lint &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm test &amp;&amp; pnpm test:e2e &amp;&amp; pnpm build &amp;&amp; pnpm check:upstream &amp;&amp; pnpm test:upstream-interop &amp;&amp; git diff --check</automated>
  </verify>
  <done>All MOBILE-01 through MOBILE-07 acceptance is green in real mobile Chromium and WebKit: complete host and independent encrypted two-client journeys work by tap, every target/overlay/viewport/storage case is inventoried and passing, and every repository plus Cordn interoperability gate succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Independently verify, visually review, push, and open the Phase 27 pull request</name>
  <files>.planning/phases/27-mobile-optimized-experience/27-VERIFICATION.md, .planning/phases/27-mobile-optimized-experience/27-UI-REVIEW.md, .planning/STATE.md</files>
  <precondition>Task 2 and every listed repository/Cordn gate pass on committed code with a clean diff check.</precondition>
  <read_first>AGENTS.md completion and shipping standard; GIT-PROCEDURES.md remote synchronization and PR procedure; 27-CONTEXT.md stop condition; 27-UI-SPEC.md proof contract; all 27-0X-SUMMARY.md files; Task 2 command evidence; current branch, remote, and open PR state.</read_first>
  <behavior>
    - "A separate GSD verifier checks every MOBILE-01..07 must-have against current code and test evidence and writes a passing 27-VERIFICATION.md; failed or missing proof returns to gap closure."
    - "A separate UI reviewer audits the implemented mobile UI against the approved six-dimension contract and writes a passing 27-UI-REVIEW.md; blocking visual/touch findings return to gap closure."
    - "The exact verified commit is pushed to the configured feature branch after fetching and confirming its base/remote relationship; no unrelated work is included."
    - "Exactly one pull request for the mobile branch is created or updated without duplicating an existing PR, and its URL/number, base, head, commit, and passing gate evidence are recorded in STATE.md."
    - "Remote CI is inspected after push/PR creation; the phase is not declared complete while required checks are failing, pending without a terminal result, or unavailable without an explicitly recorded blocker."
  </behavior>
  <action>After Task 2 is green and committed, orchestrate a separate `gsd-verifier` and `gsd-ui-auditor`/UI-review pass; do not self-author passing verdicts. Resolve every blocker through tracked gap closure and rerun affected gates. Read `GIT-PROCEDURES.md`, fetch the remote, inspect branch ancestry and existing PRs, then push the verified feature branch. Use `gh pr view`/`gh pr list` to avoid duplicates; create or update the single Phase 27 PR with a summary generated from plans, summaries, verification, UI review, security considerations, and exact test commands. Record PR URL/number/base/head/verified commit and gate results in STATE.md, commit and push that state update, and inspect remote check status to a terminal passing result. If credentials, browser binaries, external services, verifier/UI review, push, PR creation, or CI are unavailable, record the exact blocker and do not claim the stop condition.</action>
  <verify>
    <automated>test -f .planning/phases/27-mobile-optimized-experience/27-VERIFICATION.md &amp;&amp; test -f .planning/phases/27-mobile-optimized-experience/27-UI-REVIEW.md &amp;&amp; git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; git fetch origin &amp;&amp; test "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" &amp;&amp; test "$(gh pr view --json state --jq .state)" = "OPEN" &amp;&amp; test "$(gh pr view --json headRefOid --jq .headRefOid)" = "$(git rev-parse HEAD)" &amp;&amp; gh pr checks --required --watch --fail-fast</automated>
  </verify>
  <done>Independent verifier and UI review pass, the verified commit and planning evidence are pushed, one Phase 27 PR is open and recorded, and all required remote checks are terminal green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright fixture → production browser app | Test injection configures relay/browser failures but must not bypass the user-facing production path under test. |
| Host context → invitee context | A real invite capability and encrypted transport cross independent browser identities without entering logs or fixtures. |
| Test output → repository/CI artifacts | Failure messages, traces, screenshots, and console capture can accidentally preserve secrets or decrypted material. |
| Browser project label → support claim | A misconfigured resized desktop context could falsely pass while lacking mobile/touch/WebKit behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-19 | Spoofing | Playwright mobile projects | high | mitigate | Assert resolved project browser engine, `isMobile`, and `hasTouch`; require `locator.tap()` on primary paths and retain separate Chromium/WebKit names. |
| T-27-20 | Tampering | Test-only relay/storage/viewport injection | high | mitigate | Limit injection to browser capability boundaries before load and assert through production controls/state afterward; prohibit direct business-state mutation or CAHMLS-on-both-sides shortcuts. |
| T-27-21 | Information Disclosure | Invite, MLS, IndexedDB, console, trace, and assertion output | high | mitigate | Keep capabilities/keys in memory only, inspect opaque counts/public UI, avoid payload dumps, and fail on prohibited diagnostic categories without echoing their values. |
| T-27-22 | Repudiation | Mobile acceptance inventory | medium | mitigate | Encode every control class, overlay category, viewport, engine, lifecycle, and storage failure as explicit named automated cases and require full gate commands. |
| T-27-23 | Denial of Service | Multi-context/browser fixture cleanup | medium | mitigate | Own relay/context/page cleanup in finally/fixture teardown, use deterministic workers/ports for focused runs, and avoid hidden retries in tracer evidence. |
| T-27-24 | Elevation of Privilege | Host/invitee admission and action authority | high | mitigate | Use independent identities and the real canonical invite/request/approval/MLS path; assert participant/room actions target exact public context and run pinned upstream interop. |
</threat_model>

<source_coverage_audit>

## Complete Phase 27 Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Complete phone coordinator and participant lifecycle with touch, responsive navigation, and mobile-safe durability | 27-01..27-04 | COVERED | Storage, shared UI, responsive/touch hardening, then cross-engine acceptance. |
| REQ | MOBILE-01 | Every primary interaction works by one tap without hover/keyboard/invisible targets | 27-02, 27-03, 27-04 | COVERED | Shared surfaces, touch hardening, full inventory/journeys. |
| REQ | MOBILE-02 | One predictable drawer/sheet model with close, outside tap, Back/Escape | 27-02, 27-03, 27-04 | COVERED | Controller, contained geometry, cross-engine proof. |
| REQ | MOBILE-03 | 44px targets and portrait/landscape/short/keyboard containment | 27-03, 27-04 | COVERED | VisualViewport/touch system plus all dimensions/engines. |
| REQ | MOBILE-04 | Fresh host and two-client lifecycle works by touch after reload | 27-01, 27-04 | COVERED | Durable lifecycle implementation and real mobile journeys. |
| REQ | MOBILE-05 | Exact-identity async IndexedDB full snapshot durability | 27-01, 27-04 | COVERED | Validated serialized store plus browser reload evidence. |
| REQ | MOBILE-06 | Safe unavailable/denied/corrupt/quota/stop/restart behavior | 27-01, 27-04 | COVERED | Typed bounded recovery and full failure matrix. |
| REQ | MOBILE-07 | Real touch Chromium/WebKit host and two-client proof | 27-04 | COVERED | Both tasks. |
| RESEARCH | — | Native IndexedDB with normalized key, versioned Zod envelope, complete snapshot | 27-01 | COVERED | Plan 01 Task 1. |
| RESEARCH | — | Serialized mutation queue, transaction-complete writes, additive flush lifecycle | 27-01 | COVERED | Plan 01 Tasks 1-2. |
| RESEARCH | — | Visible durable/temporary/attention/flushing state and safe static errors | 27-01 | COVERED | Plan 01 Task 3. |
| RESEARCH | — | Remove sqlite-wasm/localStorage snapshot fallback without new package | 27-01 | COVERED | Plan 01 Task 3. |
| RESEARCH | — | Shared RoomBrowser with route-owned host/guest data and one state owner | 27-02 | COVERED | Plan 02 Tasks 1-2. |
| RESEARCH | — | One overlay focus/inert/history controller and existing viewportOverlay sheet mode | 27-02 | COVERED | Plan 02 Tasks 1 and 3. |
| RESEARCH | — | VisualViewport/100dvh/safe-area/scroll-containment primitive | 27-03 | COVERED | Plan 03 Task 1. |
| RESEARCH | — | Explicit offline tap disclosure and coarse-pointer favorite/action/reaction visibility | 27-03 | COVERED | Plan 03 Task 2. |
| RESEARCH | — | Real Playwright touch projects and dedicated host/two-client suite | 27-04 | COVERED | Tasks 1-2. |
| CONTEXT | 1 | Touch is first-class; pointer and keyboard remain supported | 27-02, 27-03, 27-04 | COVERED | Shared triggers, touch-visible controls, both input regressions. |
| CONTEXT | 2 | Conversation primary; navigation moves into drawers/sheets | 27-02, 27-04 | COVERED | Shared Room browser and one modal model. |
| CONTEXT | 3 | Preserve dark terminal visual identity | 27-02, 27-03, 27-04 | COVERED | Component directives and visual assertions. |
| CONTEXT | 4 | Coarse-pointer targets are at least 44x44 | 27-03, 27-04 | COVERED | Explicit inventory in both focused/full browser suites. |
| CONTEXT | 5 | Portrait, landscape, short, safe-area, and keyboard-reduced states | 27-03, 27-04 | COVERED | VisualViewport implementation and required matrix. |
| CONTEXT | 6 | Offline/favorite/reaction/room actions are discoverable by tap | 27-03, 27-04 | COVERED | Explicit controls and full journey. |
| CONTEXT | 7 | Durable snapshots move to normalized-pubkey IndexedDB | 27-01, 27-04 | COVERED | Store plus reload proof. |
| CONTEXT | 8 | Ordered validated writes, safe failures, flush before stop success, no legacy inheritance | 27-01, 27-04 | COVERED | Storage tasks and failure matrix. |
| CONTEXT | 9 | Unavailable/denied/corrupt/exhausted storage gets actionable recovery; temporary is explicit | 27-01, 27-04 | COVERED | Exact recovery UI and cross-engine cases. |
| CONTEXT | 10 | Cordn wire/interoperability remains unchanged | 27-01, 27-04 | COVERED | Additive lifecycle plus upstream gates. |
| UI-SPEC | — | Exact Room browser ordering/copy, overlay lifecycle, touch surfaces, visual height, safe areas, persistence copy, motion, and proof contract | 27-01..27-04 | COVERED | Each plan cites/implements its assigned contract; this plan proves the whole contract. |

Deferred/non-goal audit: native iOS/Android packages, push backends, gesture-only shortcuts, a new visual brand/component framework, protocol semantic changes, weakened encrypted-room authority, secret-bearing diagnostics, synchronous localStorage snapshot fallback, and resized-desktop-only claims are all excluded from every task.
</source_coverage_audit>

<verification>
Task 1 proves project correctness and one real tracer. Task 2 is the implementation phase gate and must run all of:

1. `pnpm exec playwright test --list`
2. `CI=1 PLAYWRIGHT_PORT=4302 pnpm exec playwright test tests/e2e/mobile-optimized-experience.spec.ts --project=mobile-chromium --project=mobile-webkit --workers=1`
3. `pnpm lint`
4. `pnpm exec tsc --noEmit`
5. `pnpm test`
6. `pnpm test:e2e`
7. `pnpm build`
8. `pnpm check:upstream`
9. `pnpm test:upstream-interop`
10. `git diff --check`

Task 3 then requires passing independent `27-VERIFICATION.md` and `27-UI-REVIEW.md` artifacts, a synchronized upstream branch, one open PR returned by `gh pr view`, and terminal-green remote checks.

Do not substitute screenshots, desktop-resize tests, CAHMLS client code on both sides of an interoperability claim, skipped engines, or unavailable-browser assumptions for these commands.
</verification>

<success_criteria>

- MOBILE-01 through MOBILE-04: both engines complete all host, participant, chat, room, invitation, admission, reaction, navigation, notification, profile/settings, identity, stop/restart/reload, and recovery operations by touch.
- MOBILE-03: every required target class and viewport state is explicitly inventoried and green, with contained scroll/overlays, safe areas, visible focused input/actions, no document overflow, and reduced-motion support.
- MOBILE-05/MOBILE-06: exact IndexedDB restore, ordering, flush, isolation, legacy rejection, bounded failure recovery, explicit temporary operation, and secret-free diagnostics are proven through the rendered application.
- MOBILE-07: projects are genuinely mobile/touch-enabled Chromium and WebKit, and independent clients use real canonical Cordn invitation/admission/encrypted transport.
- Quality: lint, strict TypeScript, unit, all E2E, build, diff, live upstream parity, and pinned upstream interop all pass; an unavailable external capability is reported as a blocker rather than waived.
</success_criteria>

<output>
Create `.planning/phases/27-mobile-optimized-experience/27-04-SUMMARY.md` when done.
</output>
