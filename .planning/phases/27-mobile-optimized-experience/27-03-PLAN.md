---
phase: 27-mobile-optimized-experience
plan: "03"
type: execute
wave: 3
depends_on: [27-02]
files_modified:
  - src/components/app-visual-viewport.ts
  - src/lib/viewport-overlay.ts
  - src/components/RoomBrowser.svelte
  - src/components/HostWorkspace.svelte
  - src/components/ChatRoute.svelte
  - src/components/CoordinatorRoomCard.svelte
  - src/components/RoomActionsMenu.svelte
  - src/components/MessageReactions.svelte
  - src/components/MessageGroup.svelte
  - tests/unit/viewport-overlay.test.ts
  - tests/e2e/workspace-lifecycle.spec.ts
autonomous: true
requirements: [MOBILE-01, MOBILE-02, MOBILE-03]
must_haves:
  truths:
    - "Every required control has an independent 44-by-44 CSS-pixel hit target on coarse pointers and remains perceptible without hover or focus-within."
    - "The offline coordinator summary is a tap-operated disclosure with exact singular/plural labels and unchanged room state/navigation."
    - "Room, participant, targeted-invite, and reaction actions use viewport-contained bottom sheets at 900px and below while desktop keeps anchored overlays."
    - "The shell, conversation, drawer, sheets, and composer follow one VisualViewport-driven app height and keep focused input plus primary action reachable."
    - "Portrait, landscape, short-height, reduced visual viewport, safe-area, reduced-motion, long-content, and 320px-width layouts have no document overflow or hidden required action."
  artifacts:
    - path: "src/components/app-visual-viewport.ts"
      provides: "Ref-counted VisualViewport height/listener/focus-visibility owner with 100dvh fallback"
    - path: "src/components/CoordinatorRoomCard.svelte"
      provides: "Explicit tap disclosure and independent touch-safe room/favorite/action targets"
    - path: "src/components/RoomActionsMenu.svelte"
      provides: "Desktop popover/mobile bottom-sheet action presentation"
    - path: "src/components/MessageReactions.svelte"
      provides: "Persistent coarse-pointer reaction entry and 44px mobile emoji selection"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Focused geometry, hoverless discovery, viewport, safe-area, and keyboard-reduction evidence"
  key_links:
    - from: "src/components/app-visual-viewport.ts"
      to: "src/components/HostWorkspace.svelte"
      via: "Application root receives --app-visual-height updates batched on VisualViewport/window events"
      pattern: "--app-visual-height"
    - from: "src/lib/viewport-overlay.ts"
      to: "src/components/RoomActionsMenu.svelte"
      via: "Sheet mode below 900px ignores obscured anchors and consumes the shared visual-height boundary"
      pattern: "sheet|visualViewport"
    - from: "src/components/MessageGroup.svelte"
      to: "src/components/MessageReactions.svelte"
      via: "Existing reaction state/callbacks enter a touch-visible trigger and contained emoji sheet without wire changes"
      pattern: "MessageReactions"
---

<objective>
Make every required action physically touch-safe and keep the entire shell, overlays, forms, and composer usable across mobile viewport, safe-area, keyboard, and orientation changes.

Purpose: MOBILE-01 through MOBILE-03 are not satisfied by a shared drawer alone; formerly hover-revealed actions must remain discoverable, all coarse-pointer targets must meet 44px geometry, and visual viewport reduction must not hide input or primary actions.
Output: One VisualViewport owner, safe-area/scroll containment, explicit offline disclosure, mobile action sheets, touch target system, and failing-first geometry/interaction proof.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-mobile-optimized-experience/27-CONTEXT.md
@.planning/phases/27-mobile-optimized-experience/27-UI-SPEC.md
@.planning/phases/27-mobile-optimized-experience/27-RESEARCH.md
@.planning/phases/27-mobile-optimized-experience/27-02-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-06-SUMMARY.md
@.planning/phases/26-offline-coordinator-room-disclosure/26-01-SUMMARY.md
@src/components/RoomBrowser.svelte
@src/components/HostWorkspace.svelte
@src/components/ChatRoute.svelte
@src/components/CoordinatorRoomCard.svelte
@src/components/RoomActionsMenu.svelte
@src/components/MessageReactions.svelte
@src/components/MessageGroup.svelte
@src/lib/viewport-overlay.ts
@tests/unit/viewport-overlay.test.ts
@tests/e2e/workspace-lifecycle.spec.ts

<interfaces>
Preserve these production contracts while changing presentation:

- Plan 02 `RoomBrowser` and mobile overlay controller own navigation/modal sequencing. This plan adds geometry, visual-height, safe-area, and action-sheet behavior without introducing another state owner.
- `CoordinatorRoomCard` continues to receive exact room objects, favorite keys, unread state, active/reveal keys, and room/favorite/action callbacks. Offline disclosure changes only whether already-known rows are revealed.
- `RoomActionsMenu` continues to invoke existing exact-room callbacks and destructive dialogs; only the below-900px presentation changes from anchored popover to sheet.
- `MessageGroup` remains the shared host/guest message renderer and `MessageReactions` retains aggregate counts/toggle callbacks. Presentation changes must not alter authenticated kind-9 content or reaction transport.
- `viewportOverlay` retains desktop anchored flipping and cleanup; sheet mode and VisualViewport subscriptions are additive.
</interfaces>
</context>

<task_dependencies>

| Task | Needs | Creates | Dependency reason |
|------|-------|---------|-------------------|
| 1 | Plan 02 shared drawer/sheet structure and viewportOverlay mode | One real focused-composer path that survives a reduced VisualViewport | Proves the keyboard/viewport architecture before applying it to every surface |
| 2 | Task 1 app height and sheet containment | Explicit touch controls, 44px targets, mobile action/reaction sheets, and full responsive geometry matrix | All component expansion consumes one proven viewport boundary |

Execution order: Task 1 → Task 2. Both tasks are failing-first and preserve pointer/keyboard/desktop behavior as parallel supported input modes.
</task_dependencies>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep one focused composer and Send action visible through VisualViewport reduction</name>
  <files>src/components/app-visual-viewport.ts, src/lib/viewport-overlay.ts, src/components/RoomBrowser.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, tests/unit/viewport-overlay.test.ts, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>27-UI-SPEC.md Phone viewport/safe area/keyboard, Sheets/dialogs containment, supported proof viewports, scroll containers, and motion; 27-RESEARCH.md Pattern 2, VisualViewport example, Resize-only anti-pattern, A5, and validation map; Plan 02 RoomBrowser/controller geometry; current host/guest shell sizing, composer form/action, and viewportOverlay listener/placement cleanup; existing viewport-overlay unit fakes and workspace viewport helpers.</read_first>
  <behavior>
    - "RED tracer: at 390x430 with the composer focused, `--app-visual-height` equals the injected VisualViewport height, the composer and 44px Send action remain fully inside it, and the message log does not jump to an unrelated position."
    - "VisualViewport resize and scroll plus window resize/scroll batch one next-animation-frame update; repeated events do not create duplicate work, and teardown cancels the frame and every listener."
    - "When VisualViewport is absent, the shell uses a 100dvh CSS fallback and remains functional without stale innerHeight assumptions."
    - "Focused form input and its primary action scroll into nearest view on the next frame, without hiding the active conversation, drawer/sheet close, submit, or Send control."
    - "Shell, host chat, fixed guest route, Room browser, and sheet maximum height consume the same variable; message log, drawer body, sheet body, and settings body remain the only vertical scroll containers."
    - "Portrait/landscape headers and drawer/sheet tops use safe-area top padding; composer and footer actions use safe-area bottom padding; no page-level document scrolling is introduced."
  </behavior>
  <action>Commit failing unit and focused browser tests first. Create `app-visual-viewport.ts` as one ref-counted browser-only action/controller installed by both route roots without duplicate global listeners. Initialize the CSS custom property from dynamic viewport CSS, then when VisualViewport exists publish its height on the next animation frame for resize and scroll; retain a window-event fallback and fully remove/cancel resources after the final owner unmounts. Provide a focus helper that finds the focused input's associated primary action within its form/composer and calls nearest `scrollIntoView` after the geometry update, while preserving the message log's prior scroll position unless the focused control itself is obscured. Extend viewportOverlay sheet mode to consume the shared height and visualViewport resize/scroll scheduling while leaving desktop anchor flipping unchanged. Update host, guest, and RoomBrowser sizing to use the property, name the allowed scroll containers, add overscroll containment, and apply safe-area top/bottom padding at the UI-SPEC edges. In short-height landscape, compact decorative brand/rate copy before any labelled action. Do not depend on a real software keyboard in CI: expose only an injected VisualViewport-like read/listen seam in tests, with no production test flag or Node runtime dependency.</action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/viewport-overlay.test.ts &amp;&amp; CI=1 PLAYWRIGHT_PORT=4291 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "visual viewport keeps focused composer reachable" --workers=1</automated>
  </verify>
  <done>MOBILE-03's viewport tracer is green: one shared visual height follows reduced viewport events, keeps focused input and Send/submit reachable, preserves intentional scroll, applies safe areas, cleans up fully, and falls back to 100dvh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every hover-revealed action touch-visible, 44px, sheet-contained, and responsive</name>
  <files>src/components/RoomBrowser.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, src/components/CoordinatorRoomCard.svelte, src/components/RoomActionsMenu.svelte, src/components/MessageReactions.svelte, src/components/MessageGroup.svelte, src/lib/viewport-overlay.ts, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>27-UI-SPEC.md Spacing 44px exception, Tap discovery/action surfaces, mobile sheet categories, 320px rule, typography/color, responsive proof inventory, and reduced motion; 27-RESEARCH.md Pitfall 5 and Implementation Sequence step 5; Phase 25 favorite behavior; Phase 26 offline card implementation and summary; current room row/favorite/action markup, RoomActionsMenu compact breakpoint, MessageGroup reaction entry, MessageReactions emoji controls, and workspace geometry/reduced-motion helpers.</read_first>
  <behavior>
    - "RED offline case: a normal touchscreen tap on `Show 1 offline chat` / `Show {N} offline chats` toggles `aria-expanded`, reveals existing rows in document flow, and changes to `Hide offline chats` without fetching, joining, or mutating room state."
    - "On coarse/hover-none input, favorite stars, room more-actions, reaction-add, and every other required formerly hover-revealed trigger remain visibly perceptible at rest; fine-pointer hover/focus polish remains supported."
    - "Every inventoried control class has an independent computed rectangle at least 44x44: top bar/browser, lifecycle/settings/create, room open/favorite/more/offline, invitation/admission, reaction/add/emoji, Send, profile/presence/notification/settings, sheet/dialog close/safe/destructive, and first-run/identity."
    - "Room, participant, targeted-invite, and reaction selection open a named contained bottom sheet at 900px and below; desktop retains anchored popovers. Reaction sheets expose one 44px emoji button per supported emoji and preserve aggregate counts/toggle rules."
    - "At 390x844, 844x390, 390x520, 390x430 focused, and 320px wide, document scrollWidth never exceeds clientWidth; long labels/QR/forms/lists wrap or internally scroll without overlapping independent targets."
    - "Drawer/sheet/dialog close and primary actions remain visible; outside tap closes only safe surfaces; inert/focus/history behavior from Plan 02 remains green."
    - "Under reduced motion, drawer/disclosure/reaction surfaces reach final state immediately with no animation or transform while all actions remain operable."
  </behavior>
  <action>Commit a failing touch/geometry inventory and responsive matrix before production changes. Replace the passive Phase 26 offline summary with an actual 44px button using the UI-SPEC's exact Show/Hide singular/plural copy and `aria-expanded`; keep optional fine-pointer hover/focus reveal only as enhancement and never make it the sole path. Preserve the existing five-room projection, favorites, exact navigation, and room action callbacks. In all touched components, make primary row open, favorite, more-actions, reaction-add, emoji, close, safe, destructive, Send, and paired form actions independent non-overlapping 44px targets under coarse/hover-none media; keep compact glyphs centered and use neutral/default styling except active/selected/unread/focus states. Change RoomActionsMenu and reaction/participant/targeted-invite chooser presentation to the Plan 02 sheet mode below 900px, with contextual headings, first enabled action focus for keyboard, touch tap-location preservation, internal scroll, safe-area footer, and existing native destructive dialogs. Keep MessageGroup/MessageReactions data and wire callbacks unchanged. Apply wrapping, `min-width: 0`, independent action columns, max visual height, safe-area padding, and named overflow containers so long names, QR controls, forms, and composer never create document overflow at 320px. Add an explicit Playwright inventory that queries each class by role/test contract and checks computed rectangles; do not count passive badges/status dots. Run it at all required dimensions, hover-none/coarse input, and reduced motion; retain existing mouse/keyboard assertions and the restrained terminal visual system without gradients, rounded cards, or a mobile-only icon set.</action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4292 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --grep "touch target inventory|offline chat|mobile action sheet|reduced visual viewport|no document overflow" --workers=1</automated>
  </verify>
  <done>MOBILE-01/MOBILE-03 responsive hardening is green: every required action is visibly tappable at 44px, offline/favorite/reaction/actions no longer depend on hover, sheets remain contained, all required viewport states preserve focused actions and scroll ownership, and desktop/pointer/keyboard behavior is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport/focus events → application geometry | Event timing and spoofed/incomplete VisualViewport values influence visible controls and scroll position. |
| Hidden/hover-only presentation → touch activation | Required actions may exist in DOM yet remain undiscoverable or overlap another target on coarse pointers. |
| Message/room context → mobile action sheet | Exact action authority must survive a presentation change without label-based target selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-13 | Denial of Service | `app-visual-viewport.ts` listeners/frame scheduling | medium | mitigate | Batch updates in one frame, validate finite positive height, ref-count owners, cancel pending work, and test repeated mount/unmount and event bursts. |
| T-27-14 | Tampering | Room/participant/reaction sheet callbacks | high | mitigate | Carry existing typed exact room/pubkey/message callbacks into presentation; never derive targets from sheet heading or visible text; retain transport tests in Plan 04. |
| T-27-15 | Elevation of Privilege | Transparent/overlapping touch targets | medium | mitigate | Require independent 44px rectangles, persistent perceptibility, non-overlap geometry assertions, and inert hidden/exiting surfaces. |
| T-27-16 | Denial of Service | Keyboard-reduced viewport and scroll ownership | medium | mitigate | Use one visual-height owner, named internal scroll containers, nearest focused-control adjustment, safe-area padding, and required short-height/browser matrices. |
| T-27-17 | Information Disclosure | Context headings and accessible names | low | accept | Sheets display only already-authorized room/person/message presentation names; no key, invite capability, raw storage record, or decrypted fixture data is added. |
| T-27-18 | Repudiation | Touch support claim | medium | mitigate | Automate target rectangles, hover-none visibility, tap activation, exact viewport sizes, reduced motion, and cross-engine journeys in Plan 04 rather than relying on screenshots. |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Reliable touch and responsive phone lifecycle | 27-03, 27-04 | COVERED | This plan owns physical interaction/layout; Plan 04 owns complete journeys. |
| REQ | MOBILE-01 | Single-tap primary interactions without hover/invisible targets | 27-03 | COVERED | Task 2. |
| REQ | MOBILE-02 | Contained sheets and predictable overlay behavior | 27-02, 27-03 | COVERED | Plan 02 owns lifecycle; this plan owns geometry/action categories. |
| REQ | MOBILE-03 | 44px targets, portrait/landscape/short/keyboard containment | 27-03 | COVERED | Tasks 1-2. |
| RESEARCH | — | VisualViewport + 100dvh owner and viewportOverlay extension | 27-03 | COVERED | Task 1. |
| RESEARCH | — | Explicit offline disclosure and coarse-pointer favorite/action/reaction visibility | 27-03 | COVERED | Task 2. |
| RESEARCH | — | Mobile bottom sheets below 900px, anchored desktop overlays above | 27-02, 27-03 | COVERED | Task 2 consumes Plan 02 sheet mode. |
| CONTEXT | — | 44px targets, phone orientation/short/keyboard/safe-area support | 27-03 | COVERED | Both tasks. |
| CONTEXT | — | Offline/favorite/reaction/room actions discoverable on hoverless devices | 27-03 | COVERED | Task 2. |
| CONTEXT | — | Preserve terminal brand and pointer/keyboard behavior | 27-03 | COVERED | Task actions and browser regression. |
| UI-SPEC | — | Exact disclosure copy, bottom-sheet categories, visual-height variable, safe areas, scroll ownership, proof dimensions, reduced motion | 27-03 | COVERED | Tasks 1-2. |

Deferred/non-goal audit: no gesture-only shortcut, native wrapper, new brand, new icon system, new overlay package, hidden target, protocol change, or resized-desktop-only acceptance is planned.
</source_coverage_audit>

<verification>
Focused RED/GREEN commands are embedded in each task. Before producing the Plan 03 summary, also run:

1. `pnpm lint`
2. `pnpm exec tsc --noEmit`
3. `pnpm test`
4. `CI=1 PLAYWRIGHT_PORT=4293 pnpm exec playwright test tests/e2e/workspace-lifecycle.spec.ts --workers=1`
5. `pnpm build`
6. `git diff --check`

Plan 04 repeats the required touch inventory and complete journeys in dedicated mobile Chromium and WebKit contexts.
</verification>

<success_criteria>

- MOBILE-01: every required action is visible and single-tap operable on coarse pointers, including offline disclosure, favorites, room/participant actions, reactions, and personal controls.
- MOBILE-03: every inventoried action is at least 44x44 CSS pixels and the mandated portrait, landscape, short, reduced-viewport, safe-area, long-content, and 320px cases remain contained.
- Keyboard resilience: focused composer/form plus primary action stay reachable through VisualViewport resize/scroll without unexpected message-log movement.
- Overlay continuity: action/reaction/participant/targeted-invite surfaces use contained mobile sheets and retain exact callbacks, focus, safe dismissal, inertness, and desktop anchored behavior.
- Brand/accessibility: dark terminal styling, sparse green accent, monospace density, visible focus, exact accessible names, and reduced-motion functionality remain intact.
</success_criteria>

<output>
Create `.planning/phases/27-mobile-optimized-experience/27-03-SUMMARY.md` when done.
</output>
