---
phase: 16-resilient-rooms-recovery
plan: 04
type: execute
wave: 4
depends_on:
  - 16-03
files_modified:
  - src/chat/room-store.ts
  - src/components/HostWorkspace.svelte
  - src/components/WorkspaceNav.svelte
  - src/components/ChatRoute.svelte
  - src/components/RoomRemovalDialog.svelte
  - tests/unit/room-navigation.test.ts
  - tests/e2e/phase-one.spec.ts
autonomous: true
gap_closure: true
requirements:
  - ROOM-01
  - ROOM-02
must_haves:
  truths:
    - "After exact removal, the selected coordinator opens the previous adjacent room, otherwise the next adjacent room, otherwise its explicit empty state; the removed composite room is no longer remembered (D-02, D-04, D-15)."
    - "Selecting any home, remote, previous-local, or current-invite coordinator opens its valid remembered exact room, otherwise its first deterministic room, otherwise its explicit empty state (D-14, D-15)."
    - "Every selected coordinator with zero rooms shows `No rooms for this coordinator` and `Create a room or open a current invite to add one here.` instead of stale room context (D-04, D-14)."
    - "Delete and leave confirmation names the exact room plus coordinator/host, disables duplicate submission, and turns every failure or missing-target no-op into approved generic room-named copy without exposing the caught error (D-02, D-03, D-04)."
  artifacts:
    - path: "src/chat/room-store.ts"
      provides: "Exact versioned last-open invalidation during composite room removal"
      exports: ["removeStoredRoom", "loadLastOpenRoom", "forgetLastOpenRoom"]
    - path: "src/components/HostWorkspace.svelte"
      provides: "Per-coordinator remembered/first/empty selection and previous-then-next removal fallback"
    - path: "src/components/RoomRemovalDialog.svelte"
      provides: "Coordinator/host-aware confirmation and generic room-named failure projection"
    - path: "tests/unit/room-navigation.test.ts"
      provides: "Exact last-open invalidation and same-ID isolation regression evidence"
    - path: "tests/e2e/phase-one.spec.ts"
      provides: "Adjacent fallback, coordinator empty-state, safe failure/no-op, and duplicate-submit browser evidence"
  key_links:
    - from: "src/components/HostWorkspace.svelte"
      to: "src/chat/room-store.ts"
      via: "frozen composite removal target, pre-removal sibling index, and exact last-open invalidation"
      pattern: "removeStoredRoom|loadLastOpenRoom|forgetLastOpenRoom|sameRoomIdentity"
    - from: "src/components/HostWorkspace.svelte"
      to: "src/components/RoomRemovalDialog.svelte"
      via: "boolean success/failure result plus exact room, host, and coordinator labels"
      pattern: "onConfirm|roomTitle|hostLabel|coordinatorLabel"
    - from: "src/components/ChatRoute.svelte"
      to: "src/components/RoomRemovalDialog.svelte"
      via: "all delete/leave exceptions are converted to a failed outcome before presentation"
      pattern: "onConfirm|removeCurrentRoom"
---

<objective>
Close the failed navigation/removal behaviors from `16-VERIFICATION.md` for ROOM-01 and ROOM-02 while preserving frozen composite authority (D-02 through D-04, D-14, D-15).

Purpose: A room action must leave users in the intended coordinator context, never retain a removed remembered target, and never disclose internal failure details.
Output: Correct adjacent/first/empty selection, exact empty-state copy, coordinator-aware confirmation, safe failed outcomes, and focused unit/browser regressions.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
@.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
@.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
@.planning/phases/16-resilient-rooms-recovery/16-01-SUMMARY.md
@.planning/phases/16-resilient-rooms-recovery/16-02-SUMMARY.md

<gap_root_cause>
Execution miss: Plan 16-01 specified adjacent fallback, exact preference cleanup, universal coordinator fallback, contextual host copy, and safe failure behavior, but the implementation retained first-room/early-return shortcuts, compared a JSON preference with a raw room ID, omitted the empty copy and coordinator label, and let caught messages reach the dialog.
</gap_root_cause>

<interfaces>
`removeStoredRoom({ id, coordinatorPubkey })` deletes exact v2/legacy storage and emits `ROOMS_CHANGED_EVENT`. `rememberLastOpenRoom`, `loadLastOpenRoom`, and `forgetLastOpenRoom` own the per-coordinator `{ version: 1, coordinatorPubkey, roomId }` record. `HostWorkspace.selectCoordinator(pubkey)` owns root-shell context switching. `RoomRemovalDialog` currently receives mode/title/count plus `onConfirm`; HostWorkspace, WorkspaceNav, and ChatRoute are its three removal callers.
</interfaces>
</context>

## Artifacts this phase produces

- Exact remembered-room invalidation inside `removeStoredRoom`, retaining same-ID preferences for every other coordinator.
- One deterministic `previous -> next -> empty` post-removal rule shared by hosted, joined, remote, and previous-local room collections.
- One `remembered -> deterministic first -> empty` coordinator-selection rule for home, remote, previous-local, and current-invite coordinator entries.
- The exact coordinator empty heading/body in the selected rail and content context.
- A boolean confirmation outcome contract: callers catch operational failures and return failure; the dialog renders only `Couldn’t delete # {roomName}. Try again.` or `Couldn’t leave # {roomName}. Try again.`.
- Automated failure, no-op, and double-submit evidence; no dependency or package changes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 16-04-01: Remove one active room into the exact adjacent or coordinator-empty destination</name>
  <reversibility rating="costly">D-04 and D-15 couple post-removal navigation to the persisted composite last-open contract across every coordinator room collection.</reversibility>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
    @.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/chat/room-store.ts
    @src/components/HostWorkspace.svelte
    @tests/unit/room-navigation.test.ts
    @tests/e2e/phase-one.spec.ts
  </read_first>
  <files>src/chat/room-store.ts, src/components/HostWorkspace.svelte, tests/unit/room-navigation.test.ts, tests/e2e/phase-one.spec.ts</files>
  <behavior>
    - Test first: removing the middle of three rooms selects the immediately previous sibling; removing the first selects the immediately next sibling; removing the sole room preserves the selected coordinator and shows its exact empty state.
    - Test first: removing the exact remembered composite record deletes that coordinator preference at mutation time while a same-ID room and preference under another coordinator survive.
    - Test first: direct selection of home, remote, previous-local, and current-invite coordinators follows valid remembered room, deterministic displayed-first room, then explicit zero-room state without stale content.
  </behavior>
  <action>
    Extend the unit and Playwright cases before source edits. In `removeStoredRoom`, resolve the selected coordinator's validated versioned last-open record while the exact room still exists; when both identity fields match the removal target, call `forgetLastOpenRoom` before deleting storage and emitting the removal event. Do not compare the serialized preference value with a room ID. Prove another coordinator's same-ID preference is untouched per D-02 and D-15.

    In `HostWorkspace`, capture the target's ordered coordinator-specific sibling collection and target index before mutation. Use the existing displayed order for that coordinator. After successful exact deletion/leave, choose index minus one when present, otherwise the entry now occupying the removed index, otherwise keep `selectedServerPubkey` and clear the removed room/route into the coordinator empty state. Route hosted targets through `selectRoom`, stored participant/previous-local targets through `openStoredRoomFromRail`, and a current invite through its exact invite route; never jump to another coordinator or use `remainingRooms[0]` as a substitute for adjacency (D-04, D-14).

    Replace `selectCoordinator`'s no-remembered early return with one inventory resolver used by every coordinator menu branch: validated `loadLastOpenRoom(pubkey)`, then the first room in that coordinator's deterministic rendered list, then an explicit selected-coordinator empty state. Route the invite-only coordinator entry through the same function. Render the exact heading `No rooms for this coordinator` and body `Create a room or open a current invite to add one here.` in both the zero-room channel list and main content context; retain the home create action without changing the required copy (D-14, D-15).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/room-navigation.test.ts -t "last-open|removes only the exact" &amp;&amp; pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "adjacent room fallback|coordinator room fallback and empty state" --workers=1 --timeout=60000</automated>
  </verify>
  <acceptance_criteria>
    - Middle, first, and sole-room removals prove previous, next, and selected-coordinator empty outcomes respectively.
    - The exact versioned preference is absent when removal events fire; foreign same-ID storage/preferences remain intact.
    - Every coordinator type follows remembered, deterministic first, and empty behavior without reload or stale prior-room content.
    - The required empty heading and body match character-for-character in the rendered browser.
  </acceptance_criteria>
  <done>Successful removal and direct coordinator selection always land in the deterministic exact room or documented coordinator-empty state, with no removed remembered target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-04-02: Make every removal confirmation contextual, safe on failure, and single-submit</name>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
    @.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/components/RoomRemovalDialog.svelte
    @src/components/HostWorkspace.svelte
    @src/components/WorkspaceNav.svelte
    @src/components/ChatRoute.svelte
    @tests/e2e/phase-one.spec.ts
  </read_first>
  <files>src/components/RoomRemovalDialog.svelte, src/components/HostWorkspace.svelte, src/components/WorkspaceNav.svelte, src/components/ChatRoute.svelte, tests/e2e/phase-one.spec.ts</files>
  <behavior>
    - Test first: delete and leave dialogs name `# {roomName}` plus the resolved coordinator and host, while retaining the approved cached/history impact copy.
    - Test first: a coordinator/storage exception containing relay, key-like, invite, MCP, and message-like detail renders only the matching generic room-named failure; the dialog remains open and storage, URL, selection, and focus context are unchanged.
    - Test first: a target removed between menu-open and confirm follows the same safe failed outcome and performs no fallback navigation.
    - Test first: two rapid confirm activations invoke the callback and mutation once; Cancel, close, and repeated confirm stay disabled until the one pending result settles.
  </behavior>
  <action>
    Change `RoomRemovalDialog` to require `hostLabel` and `coordinatorLabel`, and render both in the impact body without weakening the exact delete/leave impact sentences. Change `onConfirm` to return a boolean success outcome. On `true`, close as confirmed; on `false` or any defensive catch, retain the modal and render exactly `Couldn’t delete # {roomTitle}. Try again.` or `Couldn’t leave # {roomTitle}. Try again.`. Never project the caught value. Keep the existing busy guard and disabled close/cancel/confirm controls until the single callback settles (D-03, D-04).

    Update HostWorkspace, WorkspaceNav, and ChatRoute to re-load and revalidate the frozen composite target, perform the authorized mutation ordering, catch every control-plane/storage failure locally, and return `false` without removal, navigation, selection change, or target reset. Return `true` only after coordinator deletion when applicable, exact local removal, and required session cleanup succeed. Supply host from `hostIdentityForRoom(target)` and coordinator from the selected/local coordinator label already rendered by that surface; do not derive either from the active room or an error. Missing/replaced targets are failed no-ops, not success (D-02 through D-04).

    Expand Playwright coverage using an unselected sidebar target and controlled failing/no-op confirmation. Assert the exact contextual labels, one callback/removal event under rapid activation, unchanged URL/selection/storage on failure, retained dialog, retryable controls after settlement, and absence of every injected internal detail from the rendered page. Cover both host delete and participant leave call paths.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "room removal failure is safe|room removal cannot double submit|room removal names coordinator and host" --workers=1 --timeout=60000</automated>
  </verify>
  <acceptance_criteria>
    - All three dialog callers provide exact room, coordinator, and host context and share the boolean success/failure contract.
    - Delete failure, leave failure, and disappeared-target no-op keep the dialog and all room/navigation state unchanged.
    - Visible failure text is the exact approved generic room-named copy and contains none of the injected internal details.
    - Rapid confirm produces one callback, one mutation/removal event, and no duplicate coordinator delete.
  </acceptance_criteria>
  <done>Every room removal surface is contextual and single-submit, and every operational or stale-target failure is a safe no-op with generic room-named copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar/menu/dialog -> room mutation | Selection and storage can change while the frozen exact target awaits confirmation. |
| stored preference -> post-removal navigation | Serialized or foreign last-open state must not route after deletion. |
| coordinator/storage exception -> dialog | Untrusted internal detail must become a fixed room-named user outcome. |
| repeated user activation -> delete/leave callback | Double submission must not duplicate privileged deletion or local removal. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-G01 | Tampering / Elevation of privilege | frozen target and delete/leave callers | high | mitigate | Re-load both identity fields at confirmation, re-run active-local host authorization, and mutate only the exact stored record. |
| T-16-G02 | Tampering | last-open record and adjacent fallback | high | mitigate | Strict composite reconciliation, exact preference invalidation before removal event, and same-coordinator indexed fallback tests. |
| T-16-G03 | Information disclosure | removal failures and modal alert | high | mitigate | Callers collapse all causes to boolean failure; dialog renders one fixed room-named string and browser tests inject sensitive-looking details. |
| T-16-G04 | Denial of service / Tampering | pending confirmation | medium | mitigate | Busy guard plus disabled dismissal/actions and a browser assertion that callback/removal counts remain one. |
| T-16-GSC | Tampering | package supply chain | low | accept | No package installation or upgrade is planned. |
</threat_model>

<verification>
- Exact storage/navigation: `pnpm exec vitest run tests/unit/room-navigation.test.ts`.
- Contextual/safe removal browser proof: `pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "adjacent room fallback|coordinator room fallback and empty state|room removal" --workers=1 --timeout=60000`.
- Plan gate: `pnpm lint &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm test &amp;&amp; git diff --check`.
</verification>

<success_criteria>
- ROOM-01 keeps menu actions exact and non-navigating through safe failure and retry.
- ROOM-02 removes only after contextual confirmation and selects the mandated same-coordinator adjacent/empty outcome.
- D-14/D-15 coordinator selection and remembered-state cleanup have unit plus rendered browser evidence.
- No removal failure exposes caught detail, mutates state, navigates, or submits twice.
</success_criteria>

## Gap Source Coverage

| SOURCE | ID | Failed or missing behavior | Plan | Status |
|--------|----|----------------------------|------|--------|
| GOAL | — | Navigate and leave the exact intended room without misleading state | 16-04 | COVERED |
| REQ | ROOM-01 | Unselected contextual action remains exact on success/failure | 16-04 | COVERED |
| REQ | ROOM-02 | Contextual exact removal and deterministic post-removal destination | 16-04 | COVERED |
| RESEARCH | — | Frozen composite target, exact persistence deletion, native dialog busy/focus contract | 16-04 | COVERED |
| CONTEXT | D-02–D-04 | Immutable target, authorized mode, contextual confirmation and nearest fallback | 16-04 | COVERED |
| CONTEXT | D-14–D-15 | Remembered/first/empty coordinator selection and exact last-open reconciliation | 16-04 | COVERED |

<output>
Create `.planning/phases/16-resilient-rooms-recovery/16-04-SUMMARY.md` when done.
</output>
