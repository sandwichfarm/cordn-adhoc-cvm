---
phase: 16-resilient-rooms-recovery
plan: 06
type: execute
wave: 6
depends_on:
  - 16-05
files_modified:
  - src/coordinator/coordinator.svelte.ts
  - src/components/HostWorkspace.svelte
  - tests/unit/state-machine.test.ts
  - tests/e2e/phase-one.spec.ts
autonomous: true
gap_closure: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
must_haves:
  truths:
    - "Public coordinator start processes deduplicated exact targets in `roomIdentityKey` order, publishes monotonic completed/total state once per room, and becomes running only after all rooms recover (D-08, D-09)."
    - "Concurrent start calls share one lease, transport, and transaction; stop/restart cancels and awaits old work, and a late stale completion cannot publish, attach, regress progress, fail the replacement, or release its resources (D-12)."
    - "Recovery preserves exactly three 4000ms attempts with 250ms then 750ms injected waits; an exhausted manual retry keeps completed rooms, starts at the first incomplete target, and duplicate retry activation launches one retry transaction (D-10, D-12)."
    - "In the browser, automatic retry remains inside startup with monotonic completed/total, and `Retry recovery` is absent until exhaustion, then acts as the one primary recovery action (D-09 through D-11)."
    - "Startup, retry, and exhaustion render only the named-room generic diagnostic and never a disconnected local hosted chat, local offline panel/banner, or internal recovery detail (D-10, D-11)."
  artifacts:
    - path: "src/coordinator/coordinator.svelte.ts"
      provides: "Fully injected public startup/recovery runtime and generation-owned transaction behavior"
      exports: ["CoordinatorStore", "ROOM_RECOVERY_POLICY", "CoordinatorStoreRuntime"]
    - path: "src/components/HostWorkspace.svelte"
      provides: "Stable recovery-state/progress hooks and startup-only retry/exhaustion rendering"
    - path: "tests/unit/state-machine.test.ts"
      provides: "Public start/restart/stop/retry ordering, monotonicity, cancellation, stale generation, and idempotence evidence"
    - path: "tests/e2e/phase-one.spec.ts"
      provides: "Two-room retry/exhaustion/manual-recovery UI evidence with safe diagnostics and no disconnected chat"
  key_links:
    - from: "src/coordinator/coordinator.svelte.ts#CoordinatorStore.start"
      to: "HostedRoomRecoveryAdapter"
      via: "injected lease/transport runtime followed by sorted exact recovery before `started`"
      pattern: "startupGeneration|recoverHostedRooms|roomIdentityKey|transitionCoordinator"
    - from: "src/coordinator/coordinator.svelte.ts#CoordinatorStore.retryRoomRecovery"
      to: "recoveryCompleted"
      via: "one shared retry transaction skips completed exact targets and retains the live transport/lease"
      pattern: "startupPromise|recoveryCompleted|retryRoomRecovery"
    - from: "src/components/HostWorkspace.svelte"
      to: "CoordinatorStartupProgress.roomRecovery"
      via: "the startup branch is the sole renderer until complete and connected"
      pattern: "roomRecovery|startup-progress-panel|localRoomReady"
---

<objective>
Close all public-transaction and rendered recovery evidence gaps for BOOT-01, BOOT-02, and BOOT-03 without changing the locked retry policy or Phase 17 motion boundary (D-08 through D-12).

Purpose: Recovery must be proven through the same public start/restart/stop/retry surface users exercise, then proven in-browser through automatic retry, exhaustion, and successful manual recovery.
Output: Injectable coordinator lifecycle dependencies, public multi-room/race tests, stable browser progress hooks, and a two-room retry/exhaustion regression.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
@.planning/phases/16-resilient-rooms-recovery/16-RESEARCH.md
@.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
@.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
@.planning/phases/16-resilient-rooms-recovery/16-03-SUMMARY.md
@.planning/phases/16-resilient-rooms-recovery/16-05-SUMMARY.md

<gap_root_cause>
Evidence miss: Plan 16-03 tested the private `recoverHostedRooms` method with one target and browser-tested only zero rooms. Those checks bypassed public lease/transport start, multi-target ordering, start sharing, stop/restart cancellation, stale-generation ownership, retained completed progress, manual retry idempotence, and the hosted-room retry/exhaustion UI. An older browser assertion still accepts running plus a local offline panel, directly opposing BOOT-03.
</gap_root_cause>

<interfaces>
`CoordinatorStore` already accepts `RoomRecoveryRuntime { wait, runAttempt }`, owns `startupGeneration`, `startupController`, `startupPromise`, `recoveryTargets`, and `recoveryCompleted`, and exposes `start`, `stop`, `restart`, `registerHostedRoomRecovery`, and `retryRoomRecovery`. `ROOM_RECOVERY_POLICY` is locked at three attempts, 4000ms timeout, and 250/750ms delays. HostWorkspace gates `localRoomReady` on running + recovery complete + connected exact session and renders `startup-progress-panel` from `CoordinatorStartupProgress.roomRecovery`.
</interfaces>
</context>

## Artifacts this phase produces

- `CoordinatorStoreRuntime`, an injected lifecycle seam covering instance lease acquisition, transport creation/close, resource monitor start/stop, recovery attempt timeout, and recovery wait; the default delegates to the current browser implementations unchanged.
- Public-method unit scenarios using deferred injected runtime operations rather than private-method casts or wall-clock waits.
- Snapshot histories for multi-room target order, completed/total monotonicity, shared start, cancellation/restart ownership, and retained-progress retry.
- Stable non-sensitive `data-recovery-state`, `data-recovery-completed`, and `data-recovery-total` values on the existing startup panel for held-out Playwright observation.
- A browser scenario that corrupts only a second hosted room's signer ownership to drive safe deterministic automatic retry/exhaustion, restores the valid record, and completes via manual retry.
- No changes to the three-attempt/4000ms/250ms+750ms policy and no Phase 17 animation work.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 16-06-01: Drive multi-room start, cancellation, and retained-progress retry through public APIs</name>
  <reversibility rating="costly">D-08 and D-12 make one injected runtime the testable ownership boundary for lease, transport, recovery, and generation cleanup.</reversibility>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
    @.planning/phases/16-resilient-rooms-recovery/16-RESEARCH.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/coordinator/coordinator.svelte.ts
    @src/coordinator/types.ts
    @tests/unit/state-machine.test.ts
    @tests/unit/room-session-concurrency.test.ts
  </read_first>
  <files>src/coordinator/coordinator.svelte.ts, tests/unit/state-machine.test.ts</files>
  <behavior>
    - Test first: public `start()` deduplicates unsorted targets, recovers them in `roomIdentityKey` order, exposes 0/N through N/N without decrease or duplicate completion, and transitions to running only after the final deferred target resolves.
    - Test first: two concurrent `start()` calls acquire/create/recover once; `stop()` and `restart()` abort and await old work, and a late non-abortable old completion cannot alter replacement progress/status or release the replacement lease/transport.
    - Test first: target one succeeds and target two exhausts with exact 4000ms attempt values plus 250ms/750ms waits; two `retryRoomRecovery()` calls start one transaction, retain 1/2, skip target one, and complete target two once.
    - Test first: zero targets still produce observable 0/0 completion without a retry path.
  </behavior>
  <action>
    Replace the partial recovery-only constructor seam with exported `CoordinatorStoreRuntime` injection sufficient to run the public lifecycle deterministically: instance lease acquisition/release, transport creation/close, resource monitor start/stop, `runAttempt`, and `wait`. Keep a default runtime that calls the existing `SingleInstanceGuard`, `transportFactory`, and `resourceMonitor` with the same options/callbacks. Preserve the existing constructor's default behavior and browser-only dependencies; tests supply fakes and deferred promises without private-field writes or private-method casts.

    Strengthen generation ownership only where public tests expose a failure: every lease/transport assignment, progress publish, recovered-session completion, error transition, monitor start/stop, and release must verify the owning generation or occur after the old transaction is awaited. Concurrent start and retry calls reuse `startupPromise`; stop/restart abort then await it before replacement. Keep exact target dedupe/sort by `roomIdentityKey`, the completed-key set, and the locked `ROOM_RECOVERY_POLICY` values unchanged (D-08 through D-12).

    Rewrite the coordinator recovery tests around public methods and the injected runtime. Use unsorted multi-target adapters and deferred recovery operations to record every progress snapshot at await boundaries. Assert one lease/transport for shared start, stale completion isolation across stop/restart, exact order, nondecreasing completed with stable total, one completion per key, exhaustion after three 4000ms attempts with 250/750 waits, and a duplicate manual retry that preserves completed keys and does not reacquire transport/lease. Retain the existing ChatRoomSession abort/no-offline tests as the adapter-side complement.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/state-machine.test.ts tests/unit/room-session-concurrency.test.ts -t "recovery|discarded room"</automated>
  </verify>
  <acceptance_criteria>
    - Tests call only public start/stop/restart/retry registration surfaces and injected runtime methods; no private recovery invocation or startup-generation mutation remains.
    - Unsorted/duplicate targets yield one stable ordered queue and monotonic 0/N through N/N progress.
    - Shared start, stop/restart, and late completion prove one owning generation for resource assignment, publication, and cleanup.
    - Manual retry retains prior completion, skips completed rooms, shares duplicate activation, and preserves exactly 3/4000/250+750 policy evidence.
  </acceptance_criteria>
  <done>The full coordinator lifecycle is deterministically proven through public APIs with injected resources, generation ownership, ordered progress, and idempotent retained-progress retry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-06-02: Prove browser retry, exhaustion, safe diagnostics, and no disconnected local chat</name>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/components/HostWorkspace.svelte
    @src/coordinator/coordinator.svelte.ts
    @tests/e2e/phase-one.spec.ts
  </read_first>
  <files>src/components/HostWorkspace.svelte, tests/e2e/phase-one.spec.ts</files>
  <behavior>
    - Test first: two deterministic hosted rooms expose stable total 2 and a nondecreasing completed history; the second room's controlled ownership failure produces restoring/retrying before exhaustion.
    - Test first: no Retry recovery button exists during start or automatic retry; exhaustion shows one primary Retry recovery button with named-room generic guidance only.
    - Test first: restoring, retrying, and exhausted DOM never contains host message list, local-room offline panel/banner, relay URL, signer/MLS detail, invite material, decrypted content, or an MCP error.
    - Test first: restoring the valid room record and activating retry completes from retained 1/2 to 2/2, transitions to running, and reveals only the connected exact hosted chat.
  </behavior>
  <action>
    Add stable non-sensitive state hooks to the existing startup recovery panel: `data-recovery-state`, `data-recovery-completed`, and `data-recovery-total`. Values must come directly from `CoordinatorStartupProgress.roomRecovery`; do not reconstruct progress or expose composite IDs/errors. Ensure the startup branch remains dominant for restoring, retrying, and exhausted state, and remove/disable the running-plus-`room-connection-panel` path for a local hosted room that has not satisfied recovery. Only recovery complete plus connected exact session may reach local chat (D-08, D-10, D-11).

    Replace the older browser expectation that accepts a running coordinator with `Local room offline`. Build a deterministic two-room case using the existing persisted host and mock-relay helpers: retain valid snapshots, sort their composite storage identities, corrupt only the second target's signer ownership before reload, and install a MutationObserver before Start to record the new safe progress attributes. This drives automatic retry/exhaustion without relay timing or production test hooks. Assert every recorded total is 2, completed values never decrease, the first room advances once, Retry recovery is absent through automatic attempts and appears once only at exhaustion, and the status never becomes running.

    At exhaustion assert the exact approved named-room generic connection guidance and scan the rendered page for none of the controlled/internal storage and recovery details. Assert `host-message-list`, `room-connection-panel`, offline banner/copy, and normal local composer remain absent. Restore the saved valid second-room record, activate `Retry recovery`, and prove progress resumes from 1/2, completes 2/2, status becomes running, and the exact connected hosted chat appears. Keep all assertions on semantic copy/state/geometry; do not add or test Phase 17 animation behavior.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "multi-room recovery retries and exhausts safely|does not render disconnected local chat during recovery" --workers=1 --timeout=60000</automated>
  </verify>
  <acceptance_criteria>
    - Browser history proves stable total, deterministic room progression, and nondecreasing completed values through retry, exhaustion, and manual recovery.
    - Retry recovery is absent before exhaustion, appears once at exhaustion, and successful retry resumes from retained progress.
    - Exhausted copy contains only the room display name and approved generic connection guidance; internal failure/storage material is absent.
    - Startup/retry/exhaustion never render local hosted chat, offline connection panel/banner, or running status; successful connected completion does.
  </acceptance_criteria>
  <done>The real browser recovery surface proves truthful multi-room progress, automatic retry, safe actionable exhaustion, retained manual recovery, and no disconnected local-chat escape hatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected/default runtime -> coordinator lifecycle | Test seams must preserve production lease, transport, monitor, timeout, and cleanup semantics. |
| startup generation -> deferred async work | Late old work must not mutate or release the current transaction's state/resources. |
| persisted room/signer -> recovery adapter | Exact room authority must be revalidated before a session can count as recovered. |
| recovery failures -> startup DOM/test artifacts | Internal details must collapse to named-room generic guidance. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-G09 | Tampering / Denial of service | generation, start/stop/restart/retry | high | mitigate | Public deferred tests require ownership checks before every publish/assignment/cleanup and await old transactions before replacement. |
| T-16-G10 | Spoofing / Elevation of privilege | target list and recovered session | high | mitigate | Deduplicate/sort exact keys, revalidate signer ownership, and require connected exact session before running/chat. |
| T-16-G11 | Information disclosure | exhausted diagnostic and browser DOM | high | mitigate | Fixed room-named guidance only plus rendered-page assertions excluding controlled internal details. |
| T-16-G12 | Tampering | injected runtime parity | medium | mitigate | Default runtime delegates to existing production implementations; injected tests assert the same acquire/create/close/release ordering. |
| T-16-GSC | Tampering | package supply chain | low | accept | No package installation or upgrade is planned. |
</threat_model>

<verification>
- Public transaction/concurrency: `pnpm exec vitest run tests/unit/state-machine.test.ts tests/unit/room-session-concurrency.test.ts -t "recovery|discarded room"`.
- Browser recovery boundary: `pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "multi-room recovery retries and exhausts safely|does not render disconnected local chat during recovery" --workers=1 --timeout=60000`.
- Phase gate: `pnpm lint &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm test &amp;&amp; pnpm test:e2e &amp;&amp; pnpm build &amp;&amp; git diff --check`.
</verification>

<success_criteria>
- BOOT-01 has public-unit and browser evidence for deterministic multi-room order and monotonic current/completed/total progress.
- BOOT-02 preserves the exact locked policy, contains automatic retry in startup, and exposes one safe retry action only after exhaustion.
- BOOT-03 has browser proof that running/local chat/offline UI are unreachable until connected exact recovery completes.
- Start, stop, restart, stale completion, and duplicate manual retry are deterministic under injected runtime control.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Exact intended-room navigation/removal and reliable restoration state | 16-04, 16-05, 16-06 | COVERED | Implementation defects and missing evidence both close. |
| REQ | ROOM-01 | Hover/focus menu without opening or acknowledging an unselected room | 16-01, 16-04, 16-05 | COVERED | Safe failure plus pointer/keyboard unread invariant added. |
| REQ | ROOM-02 | Exact contextual composite removal and deterministic destination | 16-01, 16-04 | COVERED | Adjacent/empty, preference cleanup, copy, and failure evidence added. |
| REQ | ROOM-03 | Exact durable unread increment, projection, and visible read clearing | 16-02, 16-05 | COVERED | Same-ID visibility, menu, announcement, and badge browser proof added. |
| REQ | BOOT-01 | Current-room and aggregate deterministic restoration progress | 16-03, 16-06 | COVERED | Public multi-room unit plus browser histories added. |
| REQ | BOOT-02 | Bounded automatic retry and actionable safe exhaustion | 16-03, 16-06 | COVERED | Locked timing plus retry/exhaustion UI proved. |
| REQ | BOOT-03 | No disconnected local hosted chat during recovery | 16-03, 16-06 | COVERED | Conflicting offline acceptance is replaced by recovery-only evidence. |
| RESEARCH | — | Frozen composite target, strict last-open record, authorized deletion | 16-01, 16-04 | COVERED | Exact removal-time invalidation and failure no-op added. |
| RESEARCH | — | Receive-time ledger, visibility guard, bounded announcement | 16-02, 16-05 | COVERED | Browser evidence closes the behavioral seam. |
| RESEARCH | — | Generation-owned injected recovery, stable order, safe diagnostics | 16-03, 16-06 | COVERED | Public runtime and browser paths close private/zero-room-only evidence. |
| RESEARCH | — | No package install; browser APIs/Svelte patterns only | 16-04, 16-05, 16-06 | COVERED | All gap work uses existing stack. |
| CONTEXT | D-01–D-04 | Non-navigating actions, frozen target, delete/leave, contextual adjacent fallback | 16-01, 16-04, 16-05 | COVERED | Includes unselected unread and failed/double-submit cases. |
| CONTEXT | D-05–D-07 | Composite unread, exact visible clear, capped informational badges/announcement | 16-02, 16-05 | COVERED | Exact cross-coordinator browser evidence added. |
| CONTEXT | D-08–D-12 | Unified recovery, monotonic progress, retry/exhaustion, generation ownership | 16-03, 16-06 | COVERED | Public lifecycle and nonzero browser recovery added. |
| CONTEXT | D-13–D-15 | Reachability separation, remembered/first/empty selection, exact persistence | 16-01, 16-04 | COVERED | Zero-room copy and exact invalidation added; no reachability contract changed. |
| CONTEXT | deferred | GSAP ASCII masking/motion, notifications, message grouping/reactions | — | EXCLUDED | Explicitly assigned to Phases 17–19. |

<output>
Create `.planning/phases/16-resilient-rooms-recovery/16-06-SUMMARY.md` when done.
</output>
