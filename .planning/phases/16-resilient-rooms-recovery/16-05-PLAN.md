---
phase: 16-resilient-rooms-recovery
plan: 05
type: execute
wave: 5
depends_on:
  - 16-04
files_modified:
  - src/components/HostWorkspace.svelte
  - src/components/WorkspaceNav.svelte
  - src/components/ChatRoute.svelte
  - tests/e2e/phase-one.spec.ts
autonomous: true
gap_closure: true
requirements:
  - ROOM-01
  - ROOM-03
must_haves:
  truths:
    - "Opening an unselected unread room menu by pointer or keyboard preserves the URL, active composite room, focused trigger, and exact unread count; closing the menu does not acknowledge the target (D-01, D-06)."
    - "Only the exact visible readable composite room clears unread across same-ID rooms on different coordinators; hidden documents, coordinator selection, and menu focus preserve every count (D-06)."
    - "Each zero-to-unread transition produces one polite announcement, later increments produce none, and room/coordinator badges remain informational, non-focusable, non-clickable, and exactly 16px high (D-07)."
    - "At desktop and compact widths, long coordinator/room labels ellipsize without covering badges or action controls, long room collections scroll inside their navigation surface, and every action remains reachable."
    - "The broad operator-shell regression passes with `.host-topbar` no taller than 100px at every compact viewport while preserving 44px interactive targets."
  artifacts:
    - path: "src/components/HostWorkspace.svelte"
      provides: "Single unread announcement owner, exact readable-room acknowledgement, rail overflow, and compact topbar contract"
    - path: "src/components/WorkspaceNav.svelte"
      provides: "Informational badge markup and bounded switcher rows without duplicate unread announcements"
    - path: "src/components/ChatRoute.svelte"
      provides: "Exact visible/readable composite acknowledgement for embedded and standalone routes"
    - path: "tests/e2e/phase-one.spec.ts"
      provides: "Pointer/keyboard menu invariants, same-ID visibility clearing, announcement/badge semantics, long-list layout, and topbar regression proof"
  key_links:
    - from: "src/components/HostWorkspace.svelte"
      to: "src/chat/room-store.ts#markRoomRead"
      via: "visible exact selected session guard before acknowledgement"
      pattern: "visibilityState|sameRoomIdentity|markRead"
    - from: "src/components/WorkspaceNav.svelte"
      to: "src/components/RoomActionsMenu.svelte"
      via: "sibling menu trigger remains outside the room navigation control and outside unread acknowledgement"
      pattern: "room-row-primary|unread-badge|RoomActionsMenu"
    - from: "tests/e2e/phase-one.spec.ts"
      to: "src/components/HostWorkspace.svelte"
      via: "held-out geometry and accessibility assertions at desktop and compact widths"
      pattern: "host-topbar|room-switcher|channel-row|unread"
---

<objective>
Convert the Phase 16 unread/navigation/layout backstops into executable browser evidence and repair the known compact topbar failure for ROOM-01 and ROOM-03 (D-01, D-06, D-07).

Purpose: Users need proof that menu interaction never acknowledges another room, read clearing is exact and visibility-gated, accessibility announcements are bounded, and crowded navigation remains operable.
Output: One announcement path, exact composite interaction coverage, long-list/long-label layout assertions, and a passing <=100px compact topbar contract.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
@.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
@.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
@.planning/phases/16-resilient-rooms-recovery/16-02-SUMMARY.md
@.planning/phases/16-resilient-rooms-recovery/16-04-SUMMARY.md

<gap_root_cause>
Plan/evidence miss plus one implementation regression: Plan 16-02 established the ledger and projection but browser coverage seeded counts without exercising visibility, same-ID coordinator isolation, menu lifecycle, live announcements, or badge interaction. Plan 16-01 declared overflow backstops, yet the held-out shell check finds a 112px compact topbar against the existing 100px contract.
</gap_root_cause>

<interfaces>
`HostWorkspace.acknowledgeVisibleHostRoom()` clears only a matching live host session; `ChatRoute.acknowledgeVisibleRoom()` handles embedded/standalone stored rooms; `ROOM_UNREAD_CHANGED_EVENT` carries only composite IDs and previous/current counts. HostWorkspace and WorkspaceNav currently both own unread announcement state, while badges are rendered as spans beside sibling action buttons. `.room-switcher` and `#host-room-rail` are the bounded navigation containers; compact `.host-topbar` is a two-row grid at widths <=900px.
</interfaces>
</context>

## Artifacts this phase produces

- One page-level polite unread announcement for a composite room's zero-to-nonzero transition; no duplicate live-region message from the second navigation surface.
- Stable composite `data-*`/accessible hooks on rows and badges sufficient to prove same room IDs under different coordinators remain isolated.
- Pointer and keyboard browser scenarios for an unselected unread row that assert route, selection, focus, and count invariants.
- Desktop and compact geometry assertions for scroll ownership, ellipsis, and non-overlapping badge/action clusters.
- A compact topbar layout whose measured height is <=100px while every visible button/link remains at least 44px on its operable axis.
- No Phase 17 GSAP, mask, animation, or reduced-motion changes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 16-05-01: Prove one exact unread room survives menu interaction and clears only when visible</name>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-CONTEXT.md
    @.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/components/HostWorkspace.svelte
    @src/components/WorkspaceNav.svelte
    @src/components/ChatRoute.svelte
    @tests/e2e/phase-one.spec.ts
  </read_first>
  <files>src/components/HostWorkspace.svelte, src/components/WorkspaceNav.svelte, src/components/ChatRoute.svelte, tests/e2e/phase-one.spec.ts</files>
  <behavior>
    - Test first: two unread records sharing a room ID under different coordinators remain independently addressable by composite row hooks.
    - Test first: pointer-open and keyboard-open of the unselected target menu leave URL, active row, focused trigger, selected coordinator, and both counts unchanged; closing the menu also preserves them.
    - Test first: opening the target room while the document is hidden preserves its unread count; making that exact readable route visible clears only its count and leaves the same-ID foreign count intact.
    - Test first: dispatching count changes 0->1->2 yields one `New messages in # {roomName}` live-region announcement, while badges never enter the tab order or activate navigation.
  </behavior>
  <action>
    Add held-out Playwright setup that writes two valid same-ID `StoredRoom` records with independent composite `readState`, then reloads through the production parser. Give rendered room rows and unread badges stable composite identity hooks derived from `roomIdentityKey` so tests select exact targets rather than titles. Keep badges as informational spans with full exact accessible count; do not add button, link, tabindex, click, or key handlers (D-06, D-07).

    Make HostWorkspace the single page-level owner of `ROOM_UNREAD_CHANGED_EVENT` announcements and keep one polite live region there. Remove WorkspaceNav's duplicate unread transition announcement while retaining its unrelated coordinator-online notice. Track announcement transitions by composite key outside reactive render state, emit only when `previousCount === 0` and `unreadCount > 0`, and clear/replace announcement text in a way that one event produces one accessibility-tree update. Later increments and read clearing remain silent (D-07).

    Preserve and, where the tests expose ambiguity, tighten the acknowledgement guards: HostWorkspace requires the exact selected host room, matching session, readable connection, and visible document; ChatRoute requires the exact routed stored room, readable connection, and visible document. Coordinator selection, row focus, action-menu open/close, and a hidden document must not call `markRoomRead` or `session.markRead` (D-01, D-06). Use Playwright visibility transitions plus pointer and keyboard activation to assert persisted storage and rendered badges, not only in-memory DOM text.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "unselected unread room menu|same-id unread visibility|announces unread once" --workers=1 --timeout=60000</automated>
  </verify>
  <acceptance_criteria>
    - Pointer and keyboard menu paths preserve URL, selected composite row, focus context, and both persisted counts.
    - Hidden/readable transitions clear only the exact active composite room; coordinator selection and menu focus clear none.
    - Exactly one polite zero-to-unread announcement is observed for one event sequence, with no duplicate from WorkspaceNav.
    - Room/coordinator badges are 16px-high informational elements, absent from sequential focus, and cannot select or acknowledge a room.
  </acceptance_criteria>
  <done>Rendered browser behavior proves exact unread isolation, visibility-gated reading, bounded announcements, and a menu lifecycle with no acknowledgement side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-05-02: Keep long navigation usable and restore the compact topbar height contract</name>
  <read_first>
    @.planning/phases/16-resilient-rooms-recovery/16-UI-SPEC.md
    @.planning/phases/16-resilient-rooms-recovery/16-VERIFICATION.md
    @src/components/HostWorkspace.svelte
    @src/components/WorkspaceNav.svelte
    @tests/e2e/phase-one.spec.ts
  </read_first>
  <files>src/components/HostWorkspace.svelte, src/components/WorkspaceNav.svelte, tests/e2e/phase-one.spec.ts</files>
  <behavior>
    - Test first: a seeded many-room coordinator produces vertical overflow inside the room switcher/rail while the document retains viewport ownership at 1440x900 and compact widths.
    - Test first: extreme coordinator/room/host labels report ellipsis in compact rows; their boxes never intersect the unread badge or 44px action trigger, and full text remains in accessible labels/title.
    - Test first: the existing operator-shell viewport matrix measures `.host-topbar` at <=100px and every visible topbar control remains inside the viewport with a >=44px operable target.
  </behavior>
  <action>
    Extend the existing overflow test with a generated long room collection and extreme room/coordinator/host labels at 1440px plus representative <=900px widths. Assert the switcher or rail has `scrollHeight > clientHeight`, scroll to first/last exact row, and verify both remain reachable. Measure the label, badge, and menu-trigger rectangles to prove no intersection; verify computed `overflow`, `text-overflow`, and `white-space` establish ellipsis and that accessible labels/title retain the complete string.

    Adjust HostWorkspace and WorkspaceNav grid contracts so label columns are always `minmax(0, 1fr)`, badge/count/action columns remain intrinsic, and long text cannot expand a row. Bound the navigation list to its existing viewport-owned scroll container instead of clipping or expanding the page. Preserve 44px action/menu targets, visible focus outlines, and the UI-SPEC 16px badge; do not hide badges/actions to satisfy geometry.

    Repair the existing compact `.host-topbar` rule itself: keep its two logical rows, reduce only gap/padding/chrome needed to measure <=100px, and set visible compact topbar controls to a 44px minimum target. Re-run the unmodified broad viewport assertion together with the new long-list case. Do not touch the Phase 17 startup field, mask, GSAP, or motion contract.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "long room navigation stays operable|operator shell does not overflow common viewports" --workers=1 --timeout=60000</automated>
  </verify>
  <acceptance_criteria>
    - Long room collections scroll inside navigation at desktop and compact widths without increasing document dimensions.
    - Long labels ellipsize and retain full accessible text; badge/action boxes remain visible, reachable, and non-overlapping.
    - The existing viewport matrix passes unchanged with `.host-topbar` <=100px and all visible controls inside the viewport.
    - Every pointer-operable topbar/room action retains a 44px minimum target and Phase 17 motion files/behavior are untouched.
  </acceptance_criteria>
  <done>Room navigation remains operable under long content and compact viewports, and the broad topbar regression is closed without sacrificing action accessibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted unread records -> rendered composite rows | Same room IDs under different coordinators must not share counts or acknowledgement. |
| document/route/focus state -> read mutation | Visibility and readability, not selection or focus, authorize clearing unread. |
| unread event -> live region | Activity metadata must produce one bounded announcement without message content. |
| untrusted long labels/counts -> navigation geometry | Content must not obscure destructive actions or force controls outside the viewport. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-G05 | Tampering / Information disclosure | composite unread projection and acknowledgement | high | mitigate | Composite identity hooks, production storage assertions, and same-ID cross-coordinator visibility tests. |
| T-16-G06 | Spoofing | focus/coordinator selection as readable state | high | mitigate | Require exact routed/selected room plus readable connection and visible document before read mutation. |
| T-16-G07 | Information disclosure | duplicate live regions | medium | mitigate | One page-level announcement owner, metadata-only event, and one observed accessibility-tree update per zero transition. |
| T-16-G08 | Denial of service | long labels/lists and compact topbar | medium | mitigate | Bounded scroll containers, intrinsic action columns, ellipsis, 44px targets, and viewport geometry tests. |
| T-16-GSC | Tampering | package supply chain | low | accept | No package installation or upgrade is planned. |
</threat_model>

<verification>
- Exact unread/menu browser proof: `pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "unselected unread room menu|same-id unread visibility|announces unread once" --workers=1 --timeout=60000`.
- Layout/browser proof: `pnpm exec playwright test tests/e2e/phase-one.spec.ts --grep "long room navigation stays operable|operator shell does not overflow common viewports" --workers=1 --timeout=60000`.
- Plan gate: `pnpm lint &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm test &amp;&amp; git diff --check`.
</verification>

<success_criteria>
- ROOM-01 menu activation remains independent of navigation and unread state for an unselected row.
- ROOM-03 has rendered evidence for exact visibility clearing, same-ID isolation, one announcement, and informational badges.
- Long-list/long-label backstops pass at desktop and compact widths.
- The pre-existing `.host-topbar` <=100px assertion passes across its complete viewport matrix.
</success_criteria>

## Gap Source Coverage

| SOURCE | ID | Failed or missing behavior | Plan | Status |
|--------|----|----------------------------|------|--------|
| GOAL | — | Accurate room navigation/unread state without misleading interaction | 16-05 | COVERED |
| REQ | ROOM-01 | Unselected action-menu pointer/keyboard invariant | 16-05 | COVERED |
| REQ | ROOM-03 | Exact visible read, bounded announcement, informational badges | 16-05 | COVERED |
| RESEARCH | — | Visibility-gated composite acknowledgement and browser-held overflow proof | 16-05 | COVERED |
| CONTEXT | D-01 | Menu open does not select/navigate/acknowledge | 16-05 | COVERED |
| CONTEXT | D-06–D-07 | Exact visible read, composite badges/totals, bounded accessibility announcement | 16-05 | COVERED |

<output>
Create `.planning/phases/16-resilient-rooms-recovery/16-05-SUMMARY.md` when done.
</output>
