---
phase: 25-favorite-groups-invite-availability
plan: "02"
type: gap_closure
wave: 2
depends_on: ["25-01"]
requirements: [FAV-01, FAV-02, INVMSG-02]
status: complete
---

# Plan 25-02: Close Favorites and Invite Availability Review Gaps

## Goal

Prove the original Phase 25 outcome in the runtime states missed by Plan 25-01: invite-only coordinators that later become reachable, collapsed source rows after unfavorite, multiple invite streak accessibility, and the approved visual contract.

## Task 1 — Make invite-only reachability observable and accessible

**Files:** `src/components/HostWorkspace.svelte`, `src/components/ChatRoute.svelte`, `src/components/MessageGroup.svelte`, `tests/e2e/workspace-lifecycle.spec.ts`

- Parse already viewer-visible invite messages and merge their validated coordinator/relay targets into the existing reachability target set without logging invite capabilities.
- Re-probe when displayed message collections introduce or remove targets.
- Use a component-unique opaque prefix for every offline description ID.
- Prove offline/unknown is disabled, a live heartbeat enables the unchanged action, navigation uses the existing autojoin route, and separate invite streaks have unique resolving descriptions in host and embedded invitee paths.

## Task 2 — Keep source visibility and focus deterministic

**Files:** `src/components/CoordinatorRoomCard.svelte`, `src/components/HostWorkspace.svelte`, `tests/e2e/workspace-lifecycle.spec.ts`

- When removing a favorite duplicate whose source lies beyond the collapsed five-row limit, reveal the source coordinator card before the duplicate disappears.
- Restore focus to the exact source row or a stable reveal control only after it exists.
- Prove the source remains visible, the duplicate alone disappears, and keyboard focus lands deterministically with at least six rooms.

## Task 3 — Close the Phase 25 visual contract

**Files:** `src/components/CoordinatorRoomCard.svelte`, `src/components/RoomActionsMenu.svelte`, `src/components/MessageGroup.svelte`, `tests/e2e/workspace-lifecycle.spec.ts`

- Normalize Phase 25 additions to the approved 400/600 weights, 8/10/12/14px scale, and 4px spacing grid without mechanically changing unrelated legacy styles.
- Ensure favorite/menu/invite transitions are suppressed under reduced motion.
- Re-run 320px containment, 44px targets, computed styles, and reduced-motion assertions.

## Verification

```bash
pnpm lint
pnpm exec tsc --noEmit
pnpm test
pnpm test:e2e
pnpm build
pnpm check:upstream
pnpm test:upstream-interop
git diff --check
```

Independent code review, verifier, and UI audit must be rerun after the gap commits. No Phase 25 requirement is complete while any blocker remains.
