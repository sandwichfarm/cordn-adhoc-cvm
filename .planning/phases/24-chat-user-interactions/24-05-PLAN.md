---
phase: 24-chat-user-interactions
plan: 05
type: execute
wave: 4
depends_on:
  - 24-04
files_modified:
  - src/components/MessageGroup.svelte
  - src/components/HostWorkspace.svelte
  - src/components/ChatRoute.svelte
  - tests/unit/viewport-overlay.test.ts
  - tests/e2e/chat-user-interactions.spec.ts
autonomous: true
gap_closure: true
requirements:
  - USER-01
  - IGNORE-01
  - HILITE-01
must_haves:
  truths:
    - "Every non-self author action trigger is at least 44×44px in the shared host/invitee renderer without moving the author streak or message bubbles (USER-01, D-01)."
    - "Every ignored-streak disclosure visibly says `Show messages` while collapsed and `Hide messages` while expanded, while preserving its count copy and `aria-expanded` state in both panes (IGNORE-01, D-06)."
    - "The participant menu persistently names the current choice as `Highlight: {color}`; Default and palette choices expose selected semantics plus a non-color-only visual state that survives menu reopen and reload (HILITE-01, D-11)."
    - "Phase 24 participant surfaces use only the approved 12/14/16/20px roles, 400/600 weights, 4px spacing scale, declared color roles, and a 16px inset on each compact viewport side."
    - "Existing Phase 19 message geometry, author alignment, timestamps, reactions, invite presentation, mention precedence, and high-contrast message text remain unchanged."
  artifacts:
    - path: src/components/MessageGroup.svelte
      provides: "44px author trigger, persistent named highlight state, selected palette semantics, and approved participant-menu/chooser tokens"
    - path: src/components/HostWorkspace.svelte
      provides: "Visible host ignored-streak Show/Hide action copy and approved disclosure styling"
    - path: src/components/ChatRoute.svelte
      provides: "Visible invitee ignored-streak Show/Hide action copy and approved disclosure styling"
    - path: tests/unit/viewport-overlay.test.ts
      provides: "Deterministic 16px-per-side compact overlay geometry proof"
    - path: tests/e2e/chat-user-interactions.spec.ts
      provides: "Host/invitee interaction, computed-style, selected-state, persistence, touch-target, and compact-gutter regression evidence"
  key_links:
    - from: src/components/MessageGroup.svelte
      to: src/chat/chat-participant-preferences.svelte.ts
      via: "The existing `highlight?: ParticipantHighlight` prop supplies the persisted symbolic name and approved palette value used for the menu label and selected option"
      pattern: "highlight?.name"
    - from: src/components/MessageGroup.svelte
      to: src/lib/viewport-overlay.ts
      via: "Both Phase 24 overlay actions pass an explicit 16px gutter while retaining the shared flip, top-layer, and internal-scroll behavior"
      pattern: "gutter: 16"
    - from: src/components/HostWorkspace.svelte
      to: src/components/ChatRoute.svelte
      via: "Both pane-owned ignored disclosures render the same visible count plus state-dependent action copy"
      pattern: "Show messages|Hide messages"
  prohibitions:
    - "Do not add a second participant component, third-party package, icon library, or design system."
    - "Do not change recipient metadata, shared messages, room records, wire data, local preference schema, or highlight palette values."
    - "Do not change the global viewport-overlay default or unrelated overlays; set the Phase 24 participant menu and chooser gutter explicitly."
    - "Do not restyle existing Phase 19 timestamps, reactions, message bodies, bubbles, shared-invite action, author alignment, or streak width while correcting Phase 24 controls."
    - "Do not communicate the active highlight by color alone; keep its name visible and expose programmatic selected state."
    - "Do not render raw participant pubkeys, invite capabilities, decrypted content, keys, or signer material in labels, attributes, tests, screenshots, or logs."
---

<objective>
Close the three blocking Phase 24 UI verification gaps left after Plan 24-04: touch-accessible author actions, contract-compliant participant-surface styling, and visible persistent ignore/highlight feedback.

Purpose: Make the already-working shared interaction surface visibly and accessibly satisfy the approved UI contract, with focused evidence that prevents the exact execution miss from recurring.
Output: Corrected shared markup/styles, explicit compact-overlay geometry, and focused unit/browser visual assertions.

Root cause: execution miss — Plan 24-04 named the UI contract, but its tests asserted containment and behavior without asserting touch dimensions, exact token roles, visible Show/Hide copy, or the selected highlight state.
No package installation, protocol change, preference migration, or schema push is applicable.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-chat-user-interactions/24-CONTEXT.md
@.planning/phases/24-chat-user-interactions/24-UI-SPEC.md
@.planning/phases/24-chat-user-interactions/24-UI-REVIEW.md
@.planning/phases/24-chat-user-interactions/24-VERIFICATION.md
@.planning/phases/24-chat-user-interactions/24-04-SUMMARY.md
@src/components/MessageGroup.svelte
@src/components/HostWorkspace.svelte
@src/components/ChatRoute.svelte
@src/lib/viewport-overlay.ts
@tests/unit/viewport-overlay.test.ts
@tests/e2e/chat-user-interactions.spec.ts

<decision_map>
- D-01: The one participant menu belongs to a non-self visible author identity and must remain reliably accessible without shifting streak geometry.
- D-06: Ignore is private and exact-room; each streak disclosure must visibly communicate its reversible expanded/collapsed action.
- D-11: Highlight is private, persisted, named, accessible, palette-constrained, and clearable.
</decision_map>

<interfaces>
- `MessageGroup` already receives `highlight?: ParticipantHighlight`, whose `name` is one of `lime|gold|cyan|violet|rose` and whose `value` comes from `PARTICIPANT_HIGHLIGHT_PALETTE`; derive visible and selected state from this prop rather than reading storage inside the component.
- `viewportOverlay` already accepts `gutter?: number`; pass `gutter: 16` from the participant menu and chooser instead of changing its global 8px default used by other phases.
- `HostWorkspace` and `ChatRoute` own separate ephemeral expanded-streak sets but must render behavior-identical disclosure copy around the same `projectMessagePresentation` entries.
- Existing Playwright fixtures exercise the shared `MessageGroup` for both `host` and `guest`; seeded guest and admitted-host flows exercise the pane-owned disclosure markup.
</interfaces>
</context>

<preflight>
Run `git status --short` and inspect `git diff -- src/components/MessageGroup.svelte src/components/HostWorkspace.svelte src/components/ChatRoute.svelte tests/unit/viewport-overlay.test.ts tests/e2e/chat-user-interactions.spec.ts`. Preserve all compatible Phase 19/24 and unrelated user edits. Never reset, check out, or overwrite shared files; stop and report the exact incompatible hunk if the approved UI contract cannot be integrated safely.
</preflight>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one shared participant flow visibly accessible and stateful end to end</name>
  <reversibility rating="reversible">The change is confined to shared presentation markup/CSS and consumes the existing typed preference callback without changing persistence or wire contracts.</reversibility>
  <read_first>
    - `24-VERIFICATION.md` and `24-UI-REVIEW.md` — the three actionable failed truths; ignore behavior-unverified and deferred items outside this gap plan.
    - `24-UI-SPEC.md` — 44px exception, exact disclosure copy, named highlight label, selected-state semantics, and non-color-only requirement.
    - `src/components/MessageGroup.svelte` — current author trigger, highlight action/palette, `highlight` prop, focus restoration, and shared host/invitee surface.
    - `src/components/HostWorkspace.svelte` and `src/components/ChatRoute.svelte` — duplicated pane-owned ignored-streak disclosure branches.
    - `tests/e2e/chat-user-interactions.spec.ts` — existing host/guest fixture, admitted-host flow, and reload-persistence case to extend rather than duplicate.
  </read_first>
  <files>src/components/MessageGroup.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - RED first: host and guest author triggers report bounding boxes of at least 44px in both dimensions, while the corresponding streak width and first bubble position do not change between closed and open states (USER-01, D-01).
    - Collapsed ignored streaks visibly contain their singular/plural count plus `Show messages`; expansion changes the visible action to `Hide messages`, keeps `aria-expanded` synchronized, and remains independent per streak in both panes (IGNORE-01, D-06).
    - With no preference, the menu action reads `Highlight: Default`; after choosing Gold it reads `Highlight: Gold`, Gold exposes `aria-pressed="true"`, every other choice exposes false, and a visible selected marker/accent distinguishes Gold without relying on color alone (HILITE-01, D-11).
    - Closing/reopening the menu and reloading the room preserve the named Gold state; choosing Default clears it and moves selected semantics back to Default.
    - Highlight selection still returns focus to the Highlight action and retains the existing polite set/clear announcement.
  </behavior>
  <action>
Add one focused failing Playwright scenario first, using the existing host/guest fixture plus the existing admitted-host and seeded-reload paths; assert dimensions, geometry stability, visible disclosure action copy, highlight label, selected semantics/marker, focus return, reopen, reload, and Default clear. In `MessageGroup.svelte`, give `.participant-trigger` a 44px minimum inline and block size with border-box sizing while leaving the streak grid, width, avatar, author alignment, and bubble position untouched. Derive a title-cased current highlight label from the existing `highlight?.name`, render `Highlight: Default` or `Highlight: {Name}` on the persistent menu action, and give Default plus all named options explicit `aria-pressed` values and a text/checkmark-or-equivalent selected marker with an accent selected class. Continue to call the existing `onHighlight` callback, close only the palette after selection, restore focus to the persistent Highlight action, and retain the live status. In both pane components, keep the existing count text and `aria-expanded`, then add visible state-dependent `Show messages` / `Hide messages` copy inside the disclosure button; preserve independent expansion, chronology, recipient filtering, and local-only state.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "participant feedback remains visible and persistent" --workers=1 &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - USER-01 has automated host/guest 44×44 geometry evidence and no streak/bubble shift.
    - IGNORE-01 has visible Show/Hide copy and synchronized expanded-state evidence in both panes.
    - HILITE-01 has persistent named copy, selected semantics, non-color-only treatment, reload proof, and clear proof.
  </acceptance_criteria>
  <done>The complete shared entry flow is touch-accessible and visibly communicates ignored/highlight state across host, invitee, reopen, and reload paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the approved participant visual tokens and compact gutter</name>
  <reversibility rating="reversible">Phase-specific component tokens and an explicit overlay option can be changed locally without affecting stored data or other overlay consumers.</reversibility>
  <read_first>
    - `24-UI-SPEC.md` Spacing Scale, Typography, Color, Responsive, and Follow/highlight sections — exact token values and permitted exceptions.
    - `24-UI-REVIEW.md` Pillars 3–5 — undeclared surface colors, sub-12px roles, 700/760 weights, fractional spacing, and the 16px-total gutter failure.
    - `src/components/MessageGroup.svelte` style block — isolate Phase 24 participant/mention selectors from Phase 19 message/reaction/invite selectors.
    - `src/lib/viewport-overlay.ts` and `tests/unit/viewport-overlay.test.ts` — existing explicit-gutter calculation seam and current 8px-default expectations.
    - `tests/e2e/chat-user-interactions.spec.ts` compact-width case — replace containment-only proof with exact inset and computed-style assertions.
  </read_first>
  <files>src/components/MessageGroup.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, tests/unit/viewport-overlay.test.ts, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - Phase 24 supporting text uses 12px/400; menu actions, palette choices, room rows, and ignored disclosures use 14px/400; section/emphasized state labels use 16px/600; chooser heading uses 20px/600.
    - `Mentioned you` uses the approved emphasized-state 16px/600 role; pre-existing message body, timestamp, reaction, author/badge, and shared-invite typography remains unchanged.
    - Phase 24 layout gaps, padding, and margins use 4/8/16/24/32px values; 1px hairlines and the 44px target exception remain permitted.
    - Participant menu/chooser surfaces use `#101614`, divider/subdued/error/mention roles remain `#293832`/`#82958a`/`#ffaaa3`/`#f1f58f`, and accent `#7cf59d` marks focus and the selected highlight without becoming every action's default color.
    - At 320px and every width at or below 520px, participant menu and chooser are at most `calc(100vw - 32px)` and have at least 16px clearance on both left and right while retaining top-layer flip/internal scroll.
    - The normal and reduced-motion paths retain zero horizontal document overflow and existing focus behavior.
  </behavior>
  <action>
Extend `viewport-overlay.test.ts` first with explicit `gutter: 16` compact-sheet and constrained-panel cases that assert the calculated left inset, width, right inset, and scrollable max height. Add a focused Playwright visual-contract case that reads computed styles for one supporting label, action row, emphasized state label, chooser heading, ignored disclosure, selected highlight, surface/divider, and 320px menu/chooser geometry; assert exact font size/weight, representative 4px-scale gaps/padding, declared colors, 44px targets, and 16px side insets in host and guest fixtures. Refactor only Phase 24 selectors in `MessageGroup.svelte`, `HostWorkspace.svelte`, and `ChatRoute.svelte`: use local role variables or direct approved values consistently, map typography to the four roles/two weights, replace fractional spacing with the declared scale, and style the selected highlight with accent plus its Task 1 marker. Set the menu and chooser CSS width cap to `calc(100vw - 32px)` and pass `gutter: 16` to both `viewportOverlay` actions. Keep the global overlay default and all unrelated overlay call sites unchanged. Preserve Phase 19 streak, bubble, reaction, timestamp, shared-invite, and composer styles byte-for-byte except where a Phase 24 selector is inseparable; document any such unavoidable shared declaration before changing it.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/viewport-overlay.test.ts &amp;&amp; pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "participant visual contract" --workers=1 &amp;&amp; pnpm lint &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Unit evidence proves explicit 16px overlay geometry without changing the global overlay contract.
    - Browser evidence checks exact typography, weights, spacing, colors, selected treatment, target dimensions, and compact insets rather than containment alone.
    - No Phase 19 presentation regression or new horizontal overflow appears in either pane.
  </acceptance_criteria>
  <done>The Phase 24 interaction surface conforms to every approved visual token and compact-gutter rule with deterministic unit and browser evidence.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Participant actions remain directly usable and personalized from encrypted chat without presentation leakage | 24-05 | COVERED | Closes only the failed access/feedback presentation edges; protocol and privacy behavior from 24-01..04 remain unchanged. |
| REQ | USER-01 | One keyboard- and touch-accessible non-self participant menu | 24-05 | COVERED | Task 1 proves 44×44 host/guest triggers and unchanged streak geometry. |
| REQ | IGNORE-01 | Reversible exact-room ignored streak disclosure | 24-05 | COVERED | Task 1 adds visible Show/Hide state while retaining count, independent expansion, and local state. |
| REQ | HILITE-01 | Persistent clearable private highlight with consistent accessible presentation | 24-05 | COVERED | Task 1 adds persistent named/selected feedback; Task 2 enforces the approved selected treatment. |
| RESEARCH | — | Reuse the existing typed highlight store and `viewportOverlay`; add no package or parallel state model | 24-05 | COVERED | Existing props/callbacks and explicit gutter option are consumed directly. |
| RESEARCH | — | Keep preferences presentation-only and preserve host/invitee shared rendering | 24-05 | COVERED | No store/schema/wire changes; shared MessageGroup plus pane-owned disclosures remain the seams. |
| CONTEXT | D-01 | Participant menu belongs only to the non-self visible author identity | 24-05 | COVERED | Touch target changes the existing trigger only and preserves self absence/alignment. |
| CONTEXT | D-06 | Ignore is local/exact-room and independently reversible per streak | 24-05 | COVERED | Visible action copy is added without changing preference or expansion identity. |
| CONTEXT | D-11 | Highlight is private, persistent, named, accessible, palette-bound, and clearable | 24-05 | COVERED | Current prop drives label/selected state; Default clears through the existing API. |

Deferred/non-goal exclusions: behavior-unverified items in `24-VERIFICATION.md`, cryptographic hiding of targeted messages, blocking/reporting/moderation authority, cross-device preferences, and historical mention rewriting are outside this blocking UI gap plan.

<threat_model>
## ASVS posture

OWASP ASVS L1. The participant UI is a presentation boundary; this plan mitigates the medium/high accessibility, state-integrity, and sensitive-output risks it touches.

## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted highlight prop → participant menu | Private symbolic state becomes visible and programmatic selected feedback. |
| ignored projection → pane disclosure | Ephemeral expansion state must remain synchronized with visible copy without changing shared messages. |
| participant trigger/overlay → pointer, keyboard, and compact viewport | Geometry and focus determine whether every action is actually reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-24 | Denial of Service | participant trigger and compact overlays | medium | mitigate | Enforce 44×44 entry target, explicit 16px compact gutters, internal scrolling, and host/guest geometry assertions. |
| T-24-25 | Tampering | highlight selected feedback | medium | mitigate | Derive label and `aria-pressed` only from the typed `highlight` prop, retain named text/marker, and prove reopen/reload/clear transitions. |
| T-24-26 | Repudiation | ignored disclosure state | medium | mitigate | Keep `aria-expanded`, visible Show/Hide copy, and the pane's exact expanded-key membership synchronized under browser tests. |
| T-24-27 | Information Disclosure | participant labels and test artifacts | high | mitigate | Render only display name, count, and palette name; keep pubkeys, invite capabilities, decrypted content, keys, and signer data out of DOM, logs, screenshots, and fixtures. |
</threat_model>

<verification>
- `pnpm exec vitest run tests/unit/viewport-overlay.test.ts`
- `pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "participant feedback remains visible and persistent|participant visual contract" --workers=1`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `pnpm test`
- `pnpm test:e2e`
- `pnpm build`
- `git diff --check`
</verification>

<success_criteria>
- The three actionable `gaps:` entries in `24-VERIFICATION.md` are closed without taking on any behavior-unverified or deferred item.
- USER-01, IGNORE-01, and HILITE-01 have focused host/invitee automated evidence for the exact failed presentation contracts.
- The UI review can observe a 44×44 trigger, visible Show/Hide copy, persistent named and selected highlight state, exact typography/weights/spacing/colors, and 16px compact side insets.
- No package, preference migration, shared message/wire change, sensitive output, or Phase 19 presentation regression is introduced.
</success_criteria>

<output>
Create `.planning/phases/24-chat-user-interactions/24-05-SUMMARY.md` when done.
</output>
