---
phase: 24-chat-user-interactions
plan: 06
type: execute
wave: 5
depends_on:
  - 24-05
files_modified:
  - src/App.svelte
  - tests/e2e/chat-user-interactions-fixture.ts
  - tests/e2e/chat-user-interactions.spec.ts
autonomous: true
gap_closure: true
requirements:
  - MENTION-01
  - MENTION-02
  - INVMSG-01
  - IGNORE-01
  - INVUSER-01
  - FOLLOW-01
must_haves:
  truths:
    - "A real admitted multi-member room proves exact-viewer mention emphasis in the authenticated host renderer and ordinary presentation for non-target viewers (MENTION-02, D-03)."
    - "Choosing another active room emits its current canonical invite through the production ChatRoomSession transport with exactly one authenticated recipient, while non-target viewers receive no invite presentation (INVUSER-01/INVMSG-01, D-04/D-05/D-07)."
    - "The mounted App lifecycle owns exactly one current authenticated kind-3 subscription, closes it on logout/replacement, and rejects late prior-identity events (FOLLOW-01, D-09)."
    - "Both production composers restore the exact edited text and structured recipient set after a signer failure, and retry delivers the canonical p target without deriving identity from display text (MENTION-01, D-02)."
    - "Authenticated host ignore disclosures expand independently after target filtering without restoring a hidden invite or changing chronology, reactions, timestamps, or exact-viewer mention treatment (IGNORE-01/INVMSG-01, D-04/D-06)."
  artifacts:
    - path: tests/e2e/chat-user-interactions-fixture.ts
      provides: "Controllable in-memory NIP-07 signer/social-relay fixture and reusable real-room browser helpers"
    - path: tests/e2e/chat-user-interactions.spec.ts
      provides: "Direct browser evidence for all five formerly human-needed Phase 24 behaviors"
    - path: src/App.svelte
      provides: "VITE_E2E-only public contact-lifecycle observation seam exercised while the real App effect remains mounted"
  key_links:
    - from: tests/e2e/chat-user-interactions.spec.ts
      to: src/components/HostWorkspace.svelte
      via: "real coordinator startup, room creation/admission, participant menus, and host composer interactions rather than a callback-only harness"
      pattern: "host-message-list|Message as host"
    - from: tests/e2e/chat-user-interactions.spec.ts
      to: src/components/ChatRoute.svelte
      via: "real invitee contexts send, decrypt, persist, render, fail, and retry through ChatRoomSession"
      pattern: "guest-message-list|chat-composer"
    - from: src/App.svelte
      to: src/invites/nostr-social.svelte.ts
      via: "the existing authenticated effect calls startContactList/stopContactList while the E2E-only probe projects only public contact state"
      pattern: "startContactList|stopContactList"
    - from: tests/e2e/chat-user-interactions.spec.ts
      to: src/chat/room-store.ts
      via: "recipient-side StoredMessage inspection occurs only after a real connected session delivers and decrypts the targeted mention/invite"
      pattern: "recipientPubkeys"
  prohibitions:
    - "Do not replace production send, projection, lifecycle, or transport calls with callback assertions for the five closure behaviors."
    - "Do not expose secret keys, invite capabilities, decrypted message bodies, signed event contents, or relay diagnostics through the App probe, DOM attributes, logs, snapshots, or committed fixtures."
    - "Do not infer mention/invite recipients from edited composer text, names, DOM labels, or local test metadata."
    - "Do not change chat protocol, room persistence, preference schema, kind-3 reducer semantics, or user-visible Phase 24 behavior unless a new failing automated test proves a real defect."
    - "Do not let E2E-only lifecycle diagnostics survive a normal production build."
---

<objective>
Replace the five remaining human-needed Phase 24 checks with direct automated behavior evidence across real host/invitee sessions and the mounted App identity lifecycle.

Purpose: Move Phase 24 from present-but-unverified behavior to repeatable proof without reopening product decisions or weakening the production boundaries the phase already implemented.
Output: A reusable browser fixture, five focused Playwright behavior closures, and one production-build-elided App lifecycle observation seam.

Root cause: plan deficiency — Plans 24-01 through 24-05 proved the underlying units and UI contracts, but their browser coverage stopped at an invitee-only target render, callback-only invite dispatch, direct social-store tests, mention insertion without failure, and guest-only ignore expansion. This plan exercises the missing composition boundaries rather than changing the accepted behavior.
No package installation, schema push, protocol migration, preference migration, or external user setup is applicable.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@GIT-PROCEDURES.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-chat-user-interactions/24-CONTEXT.md
@.planning/phases/24-chat-user-interactions/24-RESEARCH.md
@.planning/phases/24-chat-user-interactions/24-UI-SPEC.md
@.planning/phases/24-chat-user-interactions/24-VALIDATION.md
@.planning/phases/24-chat-user-interactions/24-VERIFICATION.md
@.planning/phases/24-chat-user-interactions/24-01-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-03-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-04-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-05-SUMMARY.md
@src/App.svelte
@src/components/HostWorkspace.svelte
@src/components/ChatRoute.svelte
@src/components/MessageGroup.svelte
@src/chat/room-store.ts
@src/invites/nostr-social.svelte.ts
@tests/e2e/chat-user-interactions.spec.ts
@tests/e2e/mock-relay.ts
@tests/e2e/identity-prohibitions.spec.ts
@tests/e2e/nip07-session-restoration.spec.ts

<decision_map>
- D-02: Editable mention text carries identity only in signed structured recipient metadata and must retain that target after visible text edits.
- D-03: Only the exact viewer targeted by authenticated recipient metadata receives the accessible Mentioned you presentation.
- D-04/D-05: Tagged valid invites are removed before non-target layout and never expose their capability or accompanying mention text.
- D-06: Ignore remains private and exact-room, with independent ephemeral expansion after target filtering.
- D-07: Another active room is revalidated at dispatch and its current canonical invite is sent solely to the chosen participant.
- D-09: App owns the active authenticated kind-3 query/subscription generation; invalid, foreign, or stale identity work cannot mutate current state.
</decision_map>

<interfaces>
- `HostWorkspace.send` and `ChatRoute.send` snapshot `composer` plus `pendingRecipientPubkeys`, clear both before `ChatRoomSession.send`, and restore both on rejection. Force a real signer rejection; do not duplicate that algorithm in a test component.
- `ChatRoomSession.send(content, { recipientPubkeys })` signs, MLS-encrypts, persists pending state, and synchronizes through the existing production queue. The recipient's decrypted `StoredMessage` is the authoritative assertion surface for the inner kind-9 content; public relay ciphertext must not be decoded or logged by the test.
- `projectMessagePresentation(messages, viewerPubkey, ignored)` filters valid non-target invites before it forms the stable ignored streak instances rendered by both panes.
- App's existing `$effect` starts `nostrSocialStore.startContactList(signer)` only for an initialized non-anonymous active signer and cleans up with `stopContactList()`; the test seam may project `contactPubkey`, contact status, and normalized following pubkeys only when `VITE_E2E=1`.
- Existing `MessageGroupTestHarness` remains appropriate for shared menu geometry/feedback, but it is not acceptable evidence for production composer recovery, encrypted invite dispatch, host projection, or App lifecycle ownership.
</interfaces>
</context>

<preflight>
Run `git status --short` and inspect `git diff -- src/App.svelte tests/e2e/chat-user-interactions-fixture.ts tests/e2e/chat-user-interactions.spec.ts`. Preserve concurrent Phase 24 documentation and compatible test/source edits. The fixture file is expected to be new; if it already exists, integrate without overwriting. Never reset or check out shared work, and stop with the exact conflicting hunk if these test seams cannot be added safely.
</preflight>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one targeted message and one room invite through real multi-member transport</name>
  <reversibility rating="reversible">The change adds browser fixtures and assertions around existing stable production interfaces; removing it changes evidence, not stored or wire behavior.</reversibility>
  <read_first>
    - `tests/e2e/chat-user-interactions.spec.ts` — current invitee-only mention trace, callback harness, admitted-host flow, and existing room seeding helpers.
    - `tests/e2e/mock-relay.ts` — public relay observation boundary; never treat its encrypted content as the inner chat event.
    - `tests/e2e/nip07-session-restoration.spec.ts` and `identity-prohibitions.spec.ts` — real in-memory NIP-07 signing, profile-relay routing, login/logout, and secret-free fixture patterns.
    - `src/components/HostWorkspace.svelte` and `ChatRoute.svelte` — participant chooser/send and host/invitee projection paths under test.
    - `src/chat/room-store.ts` — recipient-aware production send, pending persistence, MLS decrypt, and recipient-side StoredMessage shape.
  </read_first>
  <files>tests/e2e/chat-user-interactions-fixture.ts, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - A host plus target invitee plus unrelated invitee are admitted to one actual connected room; a targeted ordinary message delivered to the host produces one `Mentioned you` marker/rail in `host-message-list`, while both non-target contexts render the same ordinary body without that marker (MENTION-02, D-03).
    - The sender's visible mention token may differ from the recipient's display name; the decrypted recipient metadata, not that token, determines all three presentations (MENTION-01, D-02).
    - From the current connected room, the host selects a different active stored room through the production chooser. The target invitee receives a valid join action whose decrypted StoredMessage has exactly `[targetPubkey]`, valid auth, and a canonical URL matching the selected room's current composite identity (INVUSER-01, D-07).
    - The host and unrelated invitee receive no invite bubble, author row, timestamp, reaction control, placeholder, or spacing; the mock relay observes transport publication while its public event content exposes no canonical invite URL (INVMSG-01, D-04/D-05).
  </behavior>
  <action>
Create a reusable test fixture module that generates each NIP-07 secret in memory, exposes signing/encryption bridges only for that page lifetime, provisions an established installation, and offers helpers for profile authentication, room creation, admission, room selection, and recipient-side StoredMessage polling. Never serialize or print generated secrets. Extend the Phase 24 spec with a uniquely named real-transport tracer that uses three isolated browser contexts and the actual coordinator/room UI. Admit both guests, send a targeted ordinary message through a production composer, and compare the host target with both non-target renderers. Then create or retain a second active room, invoke `Invite to room` from the participant menu in the current room, and wait for actual delivery/decryption before inspecting the target room record. Assert exact canonical recipient/auth/invite shape at the target, zero presentation artifacts at both non-targets, and at least one new public transport publication without examining ciphertext as plaintext. Keep the existing callback harness tests as fast UI coverage, but do not cite them as this task's transport evidence.
  </action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4316 pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "real targeted message and room invite cross production transport" --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - The host target and two non-targets prove exact-viewer mention presentation through actual connected sessions.
    - The chosen-room invite reaches the selected recipient as one authenticated decrypted target and remains absent from every non-target layout.
    - Public relay evidence proves transport activity without leaking or falsely decoding the invite capability.
  </acceptance_criteria>
  <done>Host mention parity and sole-recipient canonical invite delivery are fully automated through the real production chat transport.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise both composer rollbacks and authenticated host ignore expansion</name>
  <reversibility rating="reversible">The controllable signer and browser assertions are confined to test code and do not alter composer, preference, or projection contracts.</reversibility>
  <read_first>
    - `tests/e2e/chat-user-interactions-fixture.ts` — Task 1 authenticated multi-context and controllable signer helpers.
    - `src/components/HostWorkspace.svelte` — host mention insertion, send snapshot/clear/catch restoration, ignored projection, and disclosure rendering.
    - `src/components/ChatRoute.svelte` — invitee-equivalent mention/send/disclosure implementation.
    - `src/chat/message-presentation.ts` — target filtering before ignored streak instances.
    - `tests/e2e/chat-user-interactions.spec.ts` — existing guest-only independent disclosure and mention-insertion cases to extend with production failure and host composition.
  </read_first>
  <files>tests/e2e/chat-user-interactions-fixture.ts, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - In the host composer, Mention inserts a structured participant target; after the operator edits/replaces the visible token, a one-shot NIP-07 signing rejection restores that exact edited text and retains the original canonical target. Retry succeeds and the recipient's decrypted message contains exactly that pubkey (MENTION-01, D-02).
    - The invitee composer proves the identical edit→sign failure→exact text/target restore→retry sequence against its own production `ChatRoute.send` path.
    - An authenticated host receives participant A ordinary/mentioned messages in two streak instances separated by participant B, while an A invite targeted only to B is filtered out before grouping. Ignoring A yields exactly two disclosures (IGNORE-01/INVMSG-01, D-04/D-06).
    - Expanding the first disclosure does not expand the second; expanding both never restores the hidden invite and preserves original message order, visible timestamps, reaction controls, message bodies, and the target host's `Mentioned you` treatment.
    - Failure/status assertions use safe UI copy and test state; no signer exception details, message content, recipient key, or invite capability is written to console assertions, snapshots, or test reports.
  </behavior>
  <action>
Extend the in-memory NIP-07 controller with a one-shot `signEvent` rejection flag that affects the next real envelope signature and then resets. Add a focused browser test that provisions authenticated host and invitee sessions, invokes Mention from the actual shared author menu, edits the composer value after insertion, arms the relevant page's signer failure, and presses the real Send control. Assert the exact edited value is restored, then retry and poll the receiving room record for one normalized recipient; repeat in the opposite direction so both parent composer implementations execute. Add a second focused scenario in the authenticated host renderer with two visible participant-A streaks separated by participant B and a valid A invite targeted elsewhere. Record the pre-expansion order, timestamps, reaction affordances, and mention marker; ignore A, expand each disclosure independently, and compare those invariants while asserting the hidden invite has no DOM/accessibility/layout node throughout. Use generated message/event data and actual session delivery where practical; fixture seeding is allowed only for deterministic ordering metadata and must retain valid authenticated recipient fields rather than bypassing the host renderer.
  </action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4317 pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "both composers restore edited mentions after signer failure|authenticated host expands filtered ignored streaks" --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - Host and invitee failures both restore exact edited text plus the original canonical recipient, and retries deliver through ChatRoomSession.
    - Two authenticated-host disclosures expand independently after target filtering.
    - Hidden invite absence and message presentation invariants remain true before, during, and after both expansions.
  </acceptance_criteria>
  <done>Mention failure recovery and the combined host ignore/filter/expansion behavior no longer require manual runtime checking.</done>
</task>

<task type="auto">
  <name>Task 3: Mount and observe App ownership across logout and signer replacement</name>
  <reversibility rating="reversible">The only source seam is compile-time gated to E2E builds and projects existing public store state; the normal App lifecycle and production bundle remain unchanged.</reversibility>
  <read_first>
    - `src/App.svelte` — existing VITE_E2E harness gate and authenticated contact-list `$effect` under test.
    - `src/invites/nostr-social.svelte.ts` — public contactPubkey/status/following state, generation increments, subscription close, query reducer, and stale-event rejection.
    - `src/identity/user-profile.svelte.ts` and `src/components/UserProfile.svelte` — NIP-07 adoption, logout to anonymous identity, and real Disconnect UI.
    - `tests/e2e/identity-prohibitions.spec.ts` — profile menu NIP-07 authentication/logout pattern.
    - `tests/e2e/nip07-session-restoration.spec.ts` — real event signing and routed WebSocket fixture pattern.
  </read_first>
  <files>src/App.svelte, tests/e2e/chat-user-interactions-fixture.ts, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - Mounting normal App anonymously owns zero contact-list subscriptions. Authenticating identity A settles to exactly one live own-kind-3 subscription for A after its bounded query closes (FOLLOW-01, D-09).
    - Disconnect closes A's subscription, clears the public contact identity, and returns to zero owners; changing the controllable NIP-07 signer to identity B and authenticating starts exactly one B owner without reopening A.
    - A valid current-generation B kind-3 event updates the E2E probe's normalized following projection; a delayed valid A event delivered to the prior subscription callback after replacement cannot change B's contact pubkey, following set, or status.
    - Final logout closes B and clears contact state. Repeated reactive evaluations for the same signer do not create duplicate live subscriptions.
    - The probe exposes only public pubkeys/status/counts under `VITE_E2E=1`, and a normal production build contains neither the probe marker nor the fixture bridge.
  </behavior>
  <action>
Add a visually hidden E2E-build-only lifecycle probe alongside the normal App branch, projecting only `nostrSocialStore.contactPubkey`, contact status, sorted normalized following pubkeys, and a public owner-ready flag. Do not add setters or expose the signer, selected event content/tags/signature, invite state, relay URLs, or diagnostics. Extend the browser fixture's routed profile/social WebSocket with deterministic REQ/EOSE/CLOSE tracking and an explicit late-event delivery control; distinguish temporary bounded queries from the one remaining live subscription by their close state. Add a browser test that mounts normal App, authenticates generated identity A through the existing profile UI, waits for one owner, disconnects, swaps the in-memory signer to generated identity B, authenticates again, accepts a valid B kind-3 event, and then injects a valid delayed A replacement to the prior callback. Assert the probe and subscription ledger remain B-owned and unchanged by A, then log out and assert teardown. Finish with a normal `pnpm build` and scan the output so the E2E probe/bridge cannot ship.
  </action>
  <verify>
    <automated>CI=1 PLAYWRIGHT_PORT=4318 pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "App owns one current kind-3 lifecycle across logout and replacement" --workers=1 &amp;&amp; pnpm build &amp;&amp; ! rg -q "contact-lifecycle-probe|phase24-social-test-bridge" dist</automated>
  </verify>
  <acceptance_criteria>
    - Normal App is mounted while authentication, logout, signer replacement, late ingress, and final teardown execute.
    - Exactly one steady live subscription belongs to the active identity and stale A work cannot alter B state.
    - Production output contains no lifecycle probe or browser test bridge marker.
  </acceptance_criteria>
  <done>Application-level kind-3 ownership, teardown, replacement, and stale-generation rejection have direct automated component-lifecycle evidence.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Address, personalize, moderate locally, invite, and follow from encrypted chat without leaking non-target invite presentation | 24-01..06 | COVERED | 24-06 closes the five remaining composition-evidence edges; accepted implementation remains in 24-01..05. |
| REQ | USER-01 | One non-self accessible participant menu in both panes | 24-04/05 | COVERED | Existing browser geometry/focus/menu evidence remains authoritative; 24-06 uses it without reopening the requirement. |
| REQ | MENTION-01 | Editable mention plus signed canonical recipient | 24-01/04/06 | COVERED | Task 2 adds both-composer edited failure/retry evidence. |
| REQ | MENTION-02 | Exact-viewer accessible emphasis in both panes | 24-01/06 | COVERED | Task 1 adds authenticated host target and multiple non-target renderers. |
| REQ | INVMSG-01 | Public/targeted invite projection with zero non-target presentation | 24-01/06 | COVERED | Tasks 1–2 prove production delivery plus filter-before-ignore expansion. |
| REQ | IGNORE-01 | Exact-room persisted independent streak disclosures | 24-02/04/05/06 | COVERED | Task 2 adds authenticated host/filter/expansion invariants. |
| REQ | INVUSER-01 | Select another active room and send its current invite solely to one participant | 24-04/06 | COVERED | Task 1 replaces callback-only evidence with production session delivery. |
| REQ | FOLLOW-01 | Earliest valid live active-identity kind-3 lifecycle | 24-03/06 | COVERED | Task 3 mounts App across logout/replacement and stale ingress. |
| REQ | FOLLOW-02 | Serialized lossless relay-accepted follow publication | 24-03 | COVERED | Existing real-signature fake-pool coverage is complete; no human-needed gap remained. |
| REQ | HILITE-01 | Persistent private accessible participant highlights | 24-02/04/05 | COVERED | Existing persistence/selected-state evidence is complete; no 24-06 change. |
| RESEARCH | — | Signed optional recipient metadata and queued MLS send | 24-01/06 | COVERED | Tasks 1–2 inspect recipient-side decrypted/authenticated results after production send. |
| RESEARCH | — | Filter valid targeted invites before grouping and ignored projection | 24-01/04/06 | COVERED | Task 2 asserts the combined host composition boundary. |
| RESEARCH | — | Composer recipients remain structured state, never display-text parsing | 24-04/06 | COVERED | Task 2 edits visible tokens, fails signing, and proves the structured target survives. |
| RESEARCH | — | One generation-safe App kind-3 owner with subscription-before/alongside-query | 24-03/06 | COVERED | Task 3 observes temporary query closure, one live owner, replacement, and late old ingress. |
| RESEARCH | — | Reuse existing SimplePool, signer, invite, projection, and transport primitives | 24-01..06 | COVERED | No package, relay client, crypto, URL format, or alternate renderer is introduced. |
| RESEARCH | — | Full quality gates and Cordn upstream interoperability | 24-06 | COVERED | Plan verification includes focused, full, upstream parity, and pinned interop commands. |
| CONTEXT | D-01 | Menu belongs only to non-self visible authors | 24-04/05 | COVERED | Existing shared-menu evidence; 24-06 invokes the same production surface. |
| CONTEXT | D-02 | Editable mention text plus signed structured target | 24-01/04/06 | COVERED | Both composer failure/retry paths are direct. |
| CONTEXT | D-03 | Exact viewer alone gets mention prominence | 24-01/06 | COVERED | Real host target plus non-target contexts. |
| CONTEXT | D-04 | Untagged invites public; tagged invites exact-viewer only with zero non-target subtree | 24-01/06 | COVERED | Production transport and combined ignore projection evidence. |
| CONTEXT | D-05 | Invite capability/mention text never renders as chat text | 24-01/04/06 | COVERED | Task 1 checks non-target DOM and public transport output without logging capability data. |
| CONTEXT | D-06 | Ignore is private/exact-room and independently reversible per streak | 24-02/04/05/06 | COVERED | Authenticated host combined scenario closes the last evidence gap. |
| CONTEXT | D-07 | Other-active-room chooser sends current canonical sole-target invite | 24-04/06 | COVERED | Recipient-side authenticated StoredMessage proves the production callback chain. |
| CONTEXT | D-08 | Authenticated kind-3 follow; anonymous disabled guidance | 24-03/04 | COVERED | Existing service/UI acceptance evidence; Task 3 retains anonymous zero-owner state. |
| CONTEXT | D-09 | Valid active-author newest deterministic live kind-3 | 24-03/06 | COVERED | App lifecycle transition and stale generation are mounted and observed. |
| CONTEXT | D-10 | Serialized lossless accepted follow publication | 24-03 | COVERED | Existing unit and safety review evidence remains complete. |
| CONTEXT | D-11 | Private persistent accessible global highlight palette | 24-02/04/05 | COVERED | Existing gap closure is verified; excluded from 24-06 edits. |

Deferred/non-goal exclusions: cryptographic hiding of targeted room messages; blocking, reporting, or moderation authority; server-side profiles; cross-device preference synchronization; and rewriting historical plaintext mentions. The plan does not implement any excluded item.

<threat_model>
## ASVS posture

OWASP ASVS L1. High risks at production/test trust boundaries are mitigated; no new package or external service is introduced.

## Trust Boundaries

| Boundary | Description |
|---|---|
| browser test identity → production signer/session | Generated in-memory signing authority drives real host/invitee code but must never enter artifacts or logs. |
| encrypted room transport → recipient-side browser state | Delivery proof may inspect authenticated decrypted metadata at the intended recipient, never expose it publicly. |
| old/current relay callbacks → mounted App social state | Prior-identity async ingress must not cross the generation boundary. |
| VITE_E2E diagnostics → production bundle | Public lifecycle observability is test-only and must be absent from normal output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-28 | Information Disclosure | browser identity/invite fixtures | high | mitigate | Generate keys in memory, never serialize/log them, inspect invite content only in target-side browser assertions, and keep public relay assertions ciphertext-safe. |
| T-24-29 | Tampering | transport evidence | high | mitigate | Use actual HostWorkspace/ChatRoute/ChatRoomSession paths and authenticated recipient records; retain callback harnesses only as supplemental UI checks. |
| T-24-30 | Spoofing/Tampering | App kind-3 lifecycle | high | mitigate | Use valid signed A/B events, track exact subscription authors, assert close/replacement, and deliver a late valid A event after B owns the current generation. |
| T-24-31 | Information Disclosure | App E2E probe | high | mitigate | Project public pubkey/status/following fields only behind VITE_E2E and scan normal production output for probe/bridge markers. |
| T-24-32 | Repudiation | mention rollback proof | medium | mitigate | One-shot signer failure and recipient-side decrypted retry evidence prove both text restoration and canonical target retention without relying on implementation traces. |
| T-24-33 | Denial of Service | multi-context browser suite | medium | mitigate | Use isolated contexts, deterministic EOSE/settlement controls, unique preview port, explicit teardown, focused grep runs, and one Playwright worker. |
</threat_model>

<verification>
- `CI=1 PLAYWRIGHT_PORT=4319 pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --workers=1`
- `pnpm exec vitest run tests/unit/chat-protocol.test.ts tests/unit/message-presentation.test.ts tests/unit/chat-participant-preferences.test.ts tests/unit/nostr-invites.test.ts tests/unit/room-session-concurrency.test.ts tests/unit/viewport-overlay.test.ts`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `pnpm test`
- `CI=1 PLAYWRIGHT_PORT=4320 pnpm test:e2e`
- `pnpm build`
- `! rg -q "contact-lifecycle-probe|phase24-social-test-bridge" dist`
- `pnpm check:upstream`
- `pnpm test:upstream-interop`
- `git diff --check`
</verification>

<success_criteria>
- All five `behavior_unverified_items` in `24-VERIFICATION.md` have direct automated evidence and require no human runtime procedure.
- MENTION-01, MENTION-02, INVMSG-01, IGNORE-01, INVUSER-01, and FOLLOW-01 are proven across their missing composition boundaries without product behavior or wire/schema changes.
- The targeted invite is observed after actual production transport/decryption, and App kind-3 ownership is observed while normal App is mounted.
- Generated signer material, invite capabilities, decrypted content, and E2E diagnostics do not enter logs, snapshots, commits, or the normal production bundle.
- Focused Phase 24 checks, complete project gates, live upstream parity, and pinned bidirectional Cordn interoperability pass.
</success_criteria>

<output>
Create `.planning/phases/24-chat-user-interactions/24-06-SUMMARY.md` when done.
</output>
