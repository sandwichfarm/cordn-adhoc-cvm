---
phase: 24-chat-user-interactions
plan: 04
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
  - 24-03
files_modified:
  - src/chat/message-presentation.ts
  - src/components/MessageGroup.svelte
  - src/components/HostWorkspace.svelte
  - src/components/ChatRoute.svelte
  - tests/e2e/chat-user-interactions.spec.ts
autonomous: true
requirements:
  - USER-01
  - INVUSER-01
must_haves:
  truths:
    - "Every non-self visible author streak exposes exactly one compact `Actions for {display name}` identity trigger in the shared MessageGroup; self streaks expose no trigger, hidden menu, or self-directed action per D-01."
    - "The participant menu order is Mention, Invite to room, Follow on Nostr, divider, Highlight, Ignore; click/Enter/Space opens it, first enabled action receives focus, and Escape/outside close returns focus to the exact trigger."
    - "Mention inserts editable `@{display name}` at the active caret with boundary whitespace and retains exact structured recipient metadata even if visible text changes; both composers restore text and targets on send failure per D-02."
    - "Invite chooser includes only other active stored composite rooms, excludes current/retired History, builds the selected room's current canonical invite, and sends it to the current conversation with the selected participant as sole recipient per D-07."
    - "Ignore immediately persists exact-room state and each consecutive ignored visible streak becomes one independent collapsed disclosure; expansion preserves chronology, target filtering, mention treatment, timestamps, and reactions per D-06."
    - "Authenticated Follow presents pending/error/success from the acceptance-aware API; anonymous Follow is visibly unavailable with exact sign-in guidance and never performs local imitation per D-08/D-10."
    - "Highlight Default/Lime/Gold/Cyan/Violet/Rose changes private global presentation immediately, announces the named result, survives reload, preserves text contrast, and never replaces `Mentioned you` per D-11."
  artifacts:
    - path: src/components/MessageGroup.svelte
      provides: "Single shared participant author trigger/menu, invite chooser, follow/highlight/ignore feedback, and mentioned/highlight presentation"
    - path: src/components/HostWorkspace.svelte
      provides: "Host composer recipient state, eligible-room invite dispatch, exact-room preference/disclosure state, and shared callbacks"
    - path: src/components/ChatRoute.svelte
      provides: "Invitee composer recipient state and behavior-identical participant callbacks/presentation"
    - path: src/chat/message-presentation.ts
      provides: "Ignored visible-streak/disclosure projection with stable instance keys after target filtering"
    - path: tests/e2e/chat-user-interactions.spec.ts
      provides: "Complete host/invitee parity, focus, empty/loading/error, privacy, persistence, ordering, and compact-viewport evidence"
  key_links:
    - from: src/components/MessageGroup.svelte
      to: src/components/HostWorkspace.svelte
      via: "Typed callbacks carry exact participant pubkey/menu intent while host parent owns composer, room, and session state"
      pattern: "onMention"
    - from: src/components/MessageGroup.svelte
      to: src/components/ChatRoute.svelte
      via: "The same callback contract and idPrefix-scoped DOM implement invitee parity without component forks"
      pattern: "onMention"
    - from: src/components/HostWorkspace.svelte
      to: src/chat/room-store.ts
      via: "listRooms + sameRoomIdentity qualify active choices; createInviteUrl constructs current canonical URL; session.send supplies sole recipient"
      pattern: "sameRoomIdentity"
    - from: src/components/MessageGroup.svelte
      to: src/invites/nostr-social.svelte.ts
      via: "Follow action consumes authenticated availability/pending and awaits acceptance-aware follow result"
      pattern: "follow"
    - from: src/chat/message-presentation.ts
      to: src/chat/chat-participant-preferences.svelte.ts
      via: "Both pane projections consult exact-room ignore while MessageGroup consumes global highlight"
      pattern: "isIgnored"
  ui_considerations:
    - { statement: "[populated / Participant author control] Every non-self visible streak exposes exactly one author-identity trigger; self streaks expose none.", verification: covered }
    - { statement: "[long-text / Participant author control and menu items] Long names truncate in one-line controls with complete accessible names/title; menu labels remain readable.", verification: covered }
    - { statement: "[empty / Invite-room chooser] No qualifying active room shows `No other active rooms` plus the documented body and no disabled room rows.", verification: covered }
    - { statement: "[loading / Invite-room chooser] Selected row reads `Sending invite…`, every choice is disabled, and status is announced while dispatch is pending.", verification: covered }
    - { statement: "[error / Invite-room chooser] Failure retains the chooser, shows the documented actionable error, and keeps a retryable room row.", verification: covered }
    - { statement: "[populated / Invite-room chooser] Every eligible active room is one title/coordinator row; current and historical rooms are excluded.", verification: covered }
    - { statement: "[partial / Invite-room chooser] Missing secondary coordinator label falls back to the stable shortened coordinator label without losing the selectable room title.", verification: covered }
    - { statement: "[zero-one-many / Invite-room chooser] Zero uses empty state, one is directly selectable, and many scroll internally without changing action semantics.", verification: covered }
    - { statement: "[populated / Ignored message streak disclosure] Every ignored consecutive streak becomes one centred count disclosure; independent expansion restores original bubbles in place.", verification: covered }
    - { statement: "[overflow / Message body, disclosure label, and hidden target filtering] Bodies/disclosures wrap anywhere and non-target tagged invites contribute no rendered or log-layout node.", verification: covered }
    - { statement: "[zero-one-many / Ignored message streak disclosure] Copy uses singular/plural count and one or many disclosures retain independent expanded state.", verification: covered }
    - { statement: "[long-text / Composer mention form] Editable visible mention text remains in the existing wrapping/scrolling composer and no raw pubkey is rendered.", verification: covered }
    - { statement: "[loading / Follow action] Pending follow is disabled and announced as `Following {name}…` until relay acceptance or failure.", verification: covered }
    - { statement: "[error / Follow action] Failure uses documented safe retry copy and exposes no event content, keys, or raw relay diagnostics.", verification: covered }
  backstops:
    - { statement: "[overflow / Invite-room chooser] Browser coverage proves chooser/menu viewport containment, internal scrolling, and no horizontal overflow at 320px width and short height.", verification: backstop }
    - { statement: "[overflow / Participant menu] Browser coverage proves keyboard opening, Escape focus return, vertical flip/top-layer containment, and 44px targets in host and invitee panes.", verification: backstop }
  prohibitions:
    - "Never infer recipients from display names, mention text, or any unsigned presentation field."
    - "Never render any subtree, layout gap, author chrome, metadata, reaction control, placeholder, or log node for a non-target tagged invite."
    - "Never place ignore or highlight preferences in shared room records, StoredMessage, ChatEnvelope, encrypted payloads, or other wire data."
    - "Never accept unverified, foreign-author, stale-generation, or deterministically older kind-3 events as active contact state."
    - "Never commit or report follow success before at least one configured relay accepts the signed replacement."
    - "Never expose invite capabilities, decrypted content, private keys, or signer material in UI text, attributes, logs, errors, snapshots, fixtures, or commits."
    - "Never expose participant actions for the viewer's own author identity."
---

<objective>
Deliver the one shared participant interaction surface across host and invitee chat: mention, invite, follow, private ignore disclosure, and persistent highlight with complete UI-state/accessibility/privacy proof.

Purpose: Turn the signed recipient, preference, and kind-3 foundations into the locked encrypted-conversation experience without host/invitee forks or capability leakage.
Output: Shared author menu/chooser, matching composer and room callbacks, ignored-streak projection, follow/highlight feedback, and complete browser evidence.

No package installation or database/schema push is applicable.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-chat-user-interactions/24-CONTEXT.md
@.planning/phases/24-chat-user-interactions/24-RESEARCH.md
@.planning/phases/24-chat-user-interactions/24-UI-SPEC.md
@.planning/phases/24-chat-user-interactions/24-VALIDATION.md
@.planning/phases/24-chat-user-interactions/24-PATTERNS.md
@.planning/phases/24-chat-user-interactions/24-01-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-02-SUMMARY.md
@.planning/phases/24-chat-user-interactions/24-03-SUMMARY.md
@.planning/phases/18-unified-presence-notifications-controls/18-04-SUMMARY.md
@.planning/phases/19-grouped-conversations-reactions/19-01-SUMMARY.md
@.planning/phases/22-coordinator-grouped-sidebar/22-01-SUMMARY.md
@.planning/phases/22-coordinator-grouped-sidebar/22-02-SUMMARY.md

<decision_map>
- D-01: One author-identity menu exists only for non-self participants.
- D-02: Mention text is editable; signed structured target identity remains authoritative.
- D-03: Exact-viewer mentions receive accessible prominence; other mentions remain ordinary.
- D-04: Public/targeted invites use recipient metadata and non-target presentation is entirely absent.
- D-05: A valid invite shows only its join action, never mention/capability text.
- D-06: Ignore is immediate, reversible, local, exact-room, and disclosure expansion is per streak.
- D-07: Invite chooser contains other active rooms only and sends the current canonical invite to the selected participant alone.
- D-08: Follow uses authenticated kind-3 mutation; anonymous viewers receive disabled sign-in guidance.
- D-09: Follow state comes only from newest valid active-author kind-3 query/live ingress.
- D-10: Follow refresh/merge/sign/publish is serialized and reports success after acceptance only.
- D-11: Highlight is private, global by participant pubkey, persisted, named, accessible, and clearable.
</decision_map>

<interfaces>
- Plan 24-01 provides recipient-aware `ChatRoomSession.send`, viewer-aware message projection, and shared `Mentioned you` rendering.
- Plan 24-02 provides singleton `chatParticipantPreferences` with exact-room ignore and global typed highlight APIs.
- Plan 24-03 provides identity-scoped follow availability/state and acceptance-aware serialized `follow(pubkey)`.
- `viewportOverlay` owns top-layer anchored placement, scroll/resize repositioning, flip, width clamp, and internal overflow; MessageGroup owns close/focus return.
- `listRooms`, `sameRoomIdentity`, `membershipStatus`, and `createInviteUrl` define eligible composite room choice/current canonical invite.
- `MessageGroup` already owns shared author/bubble/reaction/join rendering; host and invitee pass callbacks and `idPrefix` rather than branching inside the component.
</interfaces>
</context>

<preflight>
Run `git status --short` and inspect `git diff -- src/chat/message-presentation.ts src/components/MessageGroup.svelte src/components/HostWorkspace.svelte src/components/ChatRoute.svelte tests/e2e/chat-user-interactions.spec.ts`. Integrate the three dependency summaries and preserve all Phase 18 overlay/control, Phase 19 grouping/reaction/timestamp, Phase 22 room/history, QR/invite, and current dirty user changes. Never reset or check out shared files; stop with the exact incompatible hunk if necessary.
</preflight>

## Artifacts this phase produces

- New typed participant-action callback/availability props and local menu/chooser/focus/status state inside the existing `MessageGroup.svelte`; no duplicate host/invitee component.
- New author identity trigger, participant menu, labelled Highlight sub-menu, and compact `Invite {name} to a room` chooser compositions inside `MessageGroup.svelte`.
- New host/invitee `pendingRecipientPubkeys`, mention-at-selection helper, send recovery, eligible-room mapping, canonical targeted invite dispatch, expanded ignored-streak key set, and shared callback wiring.
- New/extended `MessagePresentationEntry` or equivalent group/disclosure projection and stable `${sender}:${firstVisibleMessageId}` instance key in `src/chat/message-presentation.ts`.
- Completed `tests/e2e/chat-user-interactions.spec.ts` with every covered/backstop state above, both pane paths, reload, geometry, accessibility, and sensitive-output assertions.
- No new dependency, schema, shared-room field, wire field beyond Plan 24-01, or separate participant component file.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver the shared non-self menu, mention composer, and targeted room chooser</name>
  <reversibility rating="costly">MessageGroup becomes the single participant-action owner and both pane controllers adopt one callback contract; changing that seam later coordinates three shared UI files and browser evidence.</reversibility>
  <read_first>
    - `src/components/MessageGroup.svelte` — shared author row, viewer alignment, invite join action, reaction callbacks, scoped IDs, and existing styles.
    - `src/components/HostWorkspace.svelte` and `src/components/ChatRoute.svelte` — composer refs/send recovery, room/session/viewer identity, current invite join callbacks, and message loop.
    - `src/lib/viewport-overlay.ts` — anchored top-layer action and containment contract.
    - `src/chat/room-store.ts` and `src/chat/invite.ts` — listRooms/sameRoomIdentity/membershipStatus plus canonical createInviteUrl fields.
    - `tests/e2e/chat-user-interactions.spec.ts` — Plan 24-01 tracer selectors/fixtures to extend rather than replace.
  </read_first>
  <files>src/components/MessageGroup.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - Every non-self streak has one compact identity trigger named `Actions for {name}`; every self streak has zero actionable/hidden triggers (USER-01, D-01).
    - Menu click/Enter/Space opens, focuses first enabled action, orders actions exactly, closes on Escape/outside, and restores exact trigger focus; closed trigger does not shift streak/bubble geometry.
    - Mention closes menu, focuses the active composer, inserts editable token at selection with only needed whitespace, and retains exact pubkey metadata through visible edits and send failure recovery (D-02).
    - Invite chooser zero/one/many choices exclude current composite room and every retired History record; missing coordinator name uses stable shortened coordinator label (INVUSER-01, D-07).
    - Choosing a room immediately enters `Sending invite…`, disables all rows, builds that room's current canonical invite, sends to the current conversation with exactly the selected participant, then success closes/restores/announces; failure stays retryable with locked copy.
    - Valid invite UI shows only join metadata/action; URL/token/mention text is absent from DOM attributes, accessibility, errors, logs, and captured artifacts.
  </behavior>
  <action>
Extend the browser file first with host/invitee non-self/self counts, keyboard and focus return, exact action order, editable mention/recipient preservation, chooser empty/partial/zero-one-many/current/history exclusion, pending/success/error, sole-target message, and raw-capability absence. Turn the existing non-self avatar/name/host badge author identity into one compact button without changing streak alignment/width; leave self identity plain. Add one anchored viewportOverlay participant menu with exact labels/order/divider and pending live-status semantics. Host and invitee controllers must expose the same typed callbacks. Each owns a composer element reference plus recipient array independent of visible text; implement insertion at selectionStart/selectionEnd with boundary-aware spaces, then restore focus/selection. On ordinary send, snapshot and clear text+targets together only for the attempt, and restore both if the current composer/target state is still empty after failure. Add the compact chooser within the shared surface: parent supplies choices derived from `listRooms()` filtered to active membership and `!sameRoomIdentity(current, candidate)`; use full room title plus coordinator/fallback label. Build `createInviteUrl` at selection time from that candidate's current id/coordinator/relays/title/origin/name/host/token metadata and call current session send with exactly one recipient. Implement locked empty/pending/error/success/focus behavior and never surface the URL.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "participant menu|mention composer|invite chooser" --workers=1 &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Host and invitee paths pass the same menu/mention/chooser assertions with no self action.
    - Exact action ordering, keyboard focus, empty/partial/zero-one-many, pending/success/error, and sole-target encoding are automated.
    - No invite capability or accompanying mention token appears outside the canonical join action.
  </acceptance_criteria>
  <done>One shared non-self author surface lets either pane mention and target a canonical active-room invite with exact accessibility, failure recovery, and privacy presentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete ignore disclosures, accepted follow feedback, highlights, and compact parity</name>
  <reversibility rating="reversible">All three behaviors consume isolated store/service contracts through shared callbacks and local ephemeral disclosure state; they do not migrate room or wire data.</reversibility>
  <read_first>
    - `24-02-SUMMARY.md` — exact-room ignore/global highlight store APIs and palette.
    - `24-03-SUMMARY.md` — authenticated follow availability, pending state, and acceptance-aware result.
    - `src/chat/message-presentation.ts` — Plan 24-01 visibility-first grouping; ignored projection must occur after target filtering.
    - `src/components/MessageGroup.svelte`, `HostWorkspace.svelte`, and `ChatRoute.svelte` — Task 1 shared action/focus/callback/composer structure.
    - `24-UI-SPEC.md` — disclosure copy, palette/rail, status copy, typography/spacing/color, 320px and reduced-motion contracts.
  </read_first>
  <files>src/chat/message-presentation.ts, src/components/MessageGroup.svelte, src/components/HostWorkspace.svelte, src/components/ChatRoute.svelte, tests/e2e/chat-user-interactions.spec.ts</files>
  <behavior>
    - Ignore persists immediately without confirmation; each post-filter consecutive ignored streak is one singular/plural disclosure keyed by sender+first visible message id and expands independently (IGNORE-01, D-06).
    - Expanded ignored content retains original order/timestamps/reactions/mention styling and cannot resurrect a non-target invite; expansion is ephemeral and never mutates messages/preferences beyond ignore itself.
    - Authenticated follow stays disabled/busy with `Following {name}…`, reports `Now following {name}.` only after relay acceptance, and retains safe retry on failure; anonymous guidance is exact and unavailable (D-08/D-10).
    - Highlight menu names Default plus five colors, selection persists immediately by participant pubkey, focuses Highlight again, announces set/clear, and applies a 2px rail to visible author/streaks in both panes without changing text contrast or mention label (HILITE-01, D-11).
    - Long names/titles truncate with full accessible text; bodies/disclosures wrap anywhere; 44px actions, 2px focus outline, viewport top layer/flip/internal scroll, 320px no overflow, short height, and reduced motion all pass in both panes.
    - Existing timestamps, reactions, join actions, sender alignment, host badge, composer emoji shortcuts, Send action, and Phase 19 geometry remain unchanged.
  </behavior>
  <action>
Add failing browser cases first for exact-room reload isolation, same-id/different-coordinator boundary, two independent ignored streaks, singular/plural/ordering, target filtering while expanded, palette set/reload/clear, anonymous/authenticated follow pending/accept/failure, long text, keyboard focus, 320px/short-height geometry, reduced motion, and Phase 19 regressions. Extend the Plan 24-01 projector after invite filtering to emit visible group entries with stable instance keys; each pane owns an ephemeral expanded-key Set and substitutes one centered full-width disclosure for an ignored entry until expanded. Wire Ignore to the Plan 24-02 exact-room API and close/restore focus immediately. Wire Highlight to the global typed palette through the shared menu's labelled sub-menu, with named swatches, exact announcements, decorative rail CSS variable/map, and Default clear. Wire Follow to Plan 24-03 availability and await its acceptance-aware API; keep anonymous control visibly disabled with exact guidance, and use only locked generic error status. Apply UI-SPEC typography, palette, hairlines, spacing, action order, 44px targets, overflow-wrap, focus-visible, viewportOverlay containment/flip, and reduced-motion behavior. Add no positional motion that obscures participant/composer and no destructive styling for Ignore.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --grep "ignore|follow|highlight|320px|short height|reduced motion|parity" --workers=1 &amp;&amp; pnpm exec vitest run tests/unit/message-presentation.test.ts tests/unit/chat-participant-preferences.test.ts tests/unit/nostr-invites.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Ignore adjacency/empty/ordering, highlight boundary/empty/encoding/precision, and follow pending/accept/failure are automated in both panes.
    - Every UI Considerations covered row and both flat backstops in frontmatter have browser evidence.
    - Compact/focus/reduced-motion and existing message/reaction/invite behavior remain green without sensitive output.
  </acceptance_criteria>
  <done>Ignore, follow, and highlight complete the shared participant menu with persistent/private/acceptance-aware behavior and full responsive/accessibility parity.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID | Feature / constraint | Primary plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Address, personalize, moderate locally, invite, and follow from encrypted chat without leaking non-target invite presentation | 24-01..04 | COVERED | Tracer proves signed presentation; expansions deliver private/Nostr/shared UI paths. |
| REQ | MENTION-01 | Editable text plus signed canonical kind-9 p recipient | 24-01 | COVERED | Protocol/session/auth/MLS tracer and composer integration. |
| REQ | MENTION-02 | Exact-viewer accessible emphasis in both panes | 24-01 | COVERED | Shared MessageGroup tracer and browser parity. |
| REQ | INVMSG-01 | Public invite visible; target-only invite completely absent elsewhere | 24-01 | COVERED | Pre-layout viewer projection and no-node/spacing evidence. |
| REQ | IGNORE-01 | Exact-room persisted ignore with independent streak disclosures | 24-02 | COVERED | Private store primary; 24-04 integrates disclosure UI. |
| REQ | HILITE-01 | Persistent clearable private highlight in both panes | 24-02 | COVERED | Typed global palette primary; 24-04 integrates presentation. |
| REQ | FOLLOW-01 | Earliest valid newest live active-author kind-3 state | 24-03 | COVERED | Application lifecycle, validation, deterministic reducer/subscription. |
| REQ | FOLLOW-02 | Serialized lossless strictly-newer accepted follow publication | 24-03 | COVERED | Queue/refresh/merge/sign/publish/commit plus safety backstop. |
| REQ | USER-01 | One non-self keyboard-accessible participant action menu | 24-04 | COVERED | Shared MessageGroup menu and both-pane browser evidence. |
| REQ | INVUSER-01 | Choose another active room and send its canonical invite to one participant | 24-04 | COVERED | Composite room chooser and sole-recipient send. |
| RESEARCH | — | Optional canonical recipient array normalized and included in compact proof/event conversion | 24-01 | COVERED | No dependency; reactions isolated. |
| RESEARCH | — | Deduplicate/filter valid targeted invites before grouping | 24-01 | COVERED | Pure visibility-first projection shared by both logs. |
| RESEARCH | — | Composer targets remain structured state, never name parsing | 24-01/04 | COVERED | Session option plus parent-local recipient recovery. |
| RESEARCH | — | Private versioned preference store with exact/global boundaries | 24-02 | COVERED | Strict parsing, sparse state, invariant test. |
| RESEARCH | — | Single managed kind-3 owner with generation/subscription/query reducer | 24-03 | COVERED | App lifecycle and injected tests. |
| RESEARCH | — | Serialized refresh/merge/acceptance and no optimistic commit | 24-03 | COVERED | Full follow transaction plus manual safety backstop. |
| RESEARCH | — | Reuse viewportOverlay, createInviteUrl, parseInviteMessage, verifyEvent, and existing SimplePool | 24-01/03/04 | COVERED | No hand-rolled crypto, relay client, overlay, or invite format. |
| RESEARCH | — | Wave 0 tests, full project gates, and upstream Cordn interop | 24-01..04 | COVERED | Both missing files created; final plan runs complete gates. |
| CONTEXT | D-01 | Menu belongs to non-self visible author identity only | 24-04 | COVERED | Exact trigger/self absence/order/focus. |
| CONTEXT | D-02 | Editable mention plus signed structured target | 24-01/04 | COVERED | Wire tracer and composer callback. |
| CONTEXT | D-03 | Exact-viewer mention prominence only | 24-01 | COVERED | Shared semantic label/rail. |
| CONTEXT | D-04 | Untagged public invite; tagged exact-viewer presentation; zero non-target subtree | 24-01 | COVERED | Filter before keyed grouping. |
| CONTEXT | D-05 | Invite mention/capability text never rendered | 24-01/04 | COVERED | Join action only and output scans. |
| CONTEXT | D-06 | Ignore private/local/exact-room with independent streaks | 24-02/04 | COVERED | Store plus disclosure projection. |
| CONTEXT | D-07 | Other-active-room chooser sends canonical sole-target invite | 24-04 | COVERED | Composite qualification and current URL. |
| CONTEXT | D-08 | Authenticated kind-3 follow; anonymous disabled guidance | 24-03/04 | COVERED | Service boundary and UI state. |
| CONTEXT | D-09 | Valid active-author newest deterministic live kind-3 | 24-03 | COVERED | One reducer and lifecycle. |
| CONTEXT | D-10 | Serialized lossless accepted follow publication | 24-03 | COVERED | Transaction/tests/backstop. |
| CONTEXT | D-11 | Private persistent accessible global highlight palette | 24-02/04 | COVERED | Typed palette/store/shared UI. |

Deferred/non-goal exclusions: cryptographic hiding of targeted room messages; blocking/reporting/moderation authority; server-side profiles; cross-device preference sync; historical plaintext mention rewriting. None is implemented by these plans.

<threat_model>
## ASVS posture

OWASP ASVS L1; critical/high findings block execution. Medium threats on participant-action/UI boundaries are mitigated.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author identity/message data → participant actions | Only exact non-self authors may expose actions; names are display-only. |
| room ledger → invite chooser/session send | Stored rooms include retired/current records and capability-bearing invite data. |
| local preferences/social state → shared renderer | Private/validated state changes only local UI; sensitive values must not escape. |
| anchored menu/chooser → viewport/focus | Overlay geometry, keyboard state, and busy async callbacks can strand focus or activate stale targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-17 | Elevation of Privilege | self participant actions | high | mitigate | Derive self from exact viewer/sender pubkeys, emit no trigger/menu for self, and assert zero hidden/actionable nodes in both panes. |
| T-24-18 | Information Disclosure | invite chooser/message/error | high | mitigate | Keep capabilities only in click-time local variable/session send, render join metadata only, and scan DOM/attributes/errors/log artifacts for absence. |
| T-24-19 | Tampering | stale author/room callback | high | mitigate | Capture exact participant and composite room choice, disable all pending controls, revalidate active/non-current room at dispatch, and restore exact origin focus. |
| T-24-20 | Spoofing | mention/invite recipient | high | mitigate | Pass exact author pubkey through typed callbacks into signed send options; names remain editable presentation only. |
| T-24-21 | Tampering | private ignore/highlight | medium | mitigate | Consume Plan 24-02 validated APIs only and assert StoredRoom/messages/wire data remain unchanged through toggle/expand/reload. |
| T-24-22 | Repudiation | follow UI success | high | mitigate | Await Plan 24-03 acceptance-aware follow promise and display success only after it resolves; generic retry on rejection. |
| T-24-23 | Denial of Service | menu/chooser/follow/invite reentry | medium | mitigate | Disable busy actions/rows, retain one open surface, serialize service calls, and test repeated keyboard/click activation. |
</threat_model>

<verification>
- `pnpm exec playwright test tests/e2e/chat-user-interactions.spec.ts --workers=1`
- `pnpm exec vitest run tests/unit/chat-protocol.test.ts tests/unit/message-presentation.test.ts tests/unit/chat-participant-preferences.test.ts tests/unit/nostr-invites.test.ts`
- `pnpm check:upstream`
- `pnpm test:upstream-interop`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `pnpm test`
- `pnpm test:e2e`
- `pnpm build`
- `git diff --check`
</verification>

<success_criteria>
- USER-01 and INVUSER-01 are directly proven, and every cross-cutting recipient/ignore/follow/highlight contract from Plans 24-01..03 is integrated in both panes.
- Every covered/backstop UI consideration, probe category, locked copy, focus rule, viewport rule, and safety prohibition has evidence.
- The full project gate, live upstream schema check, and pinned CAHMLS↔Cordn bidirectional invite/encrypted-message interop pass.
- All nine requirements have exactly one primary plan in frontmatter and no deferred idea enters scope.
</success_criteria>

<output>
Create `.planning/phases/24-chat-user-interactions/24-04-SUMMARY.md` when done.
</output>
