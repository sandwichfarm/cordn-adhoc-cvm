---
phase: 19-grouped-conversations-reactions
plan: "01"
status: complete
requirements: [CHAT-01, CHAT-02, REACT-01, REACT-02]
---

# Plan 19-01: Shared grouped conversation renderer

1. Add pure streak grouping and adaptive relative-time helpers with boundary-focused unit tests.
2. Add shared `MessageTimestamp` and `MessageGroup` components implementing the UI contract while retaining per-message reactions.
3. Replace repeated host and invitee message loops with the shared grouped renderer without changing protocol/state behavior.
4. Add Playwright evidence for ownership alignment, one avatar per streak, host badge/style, per-message metadata, compact containment, and existing reaction behavior.
5. Run TypeScript, lint, unit tests, relevant Playwright tests, build, and `git diff --check`.

## Goal-backward check

The plan directly proves every explicit layout and timing outcome. It does not rely on protocol changes, and both user-facing chat paths consume the same implementation, preventing host/invitee drift. **Verdict: PASS.**
