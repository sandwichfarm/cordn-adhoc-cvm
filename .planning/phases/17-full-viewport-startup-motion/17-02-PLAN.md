---
phase: 17-full-viewport-startup-motion
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/startup-signal-presentation.ts
  - src/components/StartupSignalField.svelte
  - src/components/HostWorkspace.svelte
  - tests/unit/startup-signal-presentation.test.ts
  - tests/e2e/workspace-lifecycle.spec.ts
autonomous: true
requirements:
  - MOTION-02
  - MOTION-03
must_haves:
  truths:
    - "The pane-filling field contains exactly one deterministic base ASCII bed plus exactly three concentric ASCII reveal layers, and each reveal uses both standard and WebKit radial-gradient masks with no border, outline, SVG, stroke, or standalone-circle substitute (D-05, D-06)."
    - "Normal motion is GSAP-driven from the current projected startup phase: transport and completed-room progress land perceptibly later, retry retains completed work, and exhaustion settles without implying completion (D-07, D-09, D-10, D-11)."
    - "The content-pane stage and full ASCII bed remain edge-to-edge through the exact pane right edge while all animation stays behind readable text and outside the global header and room rail (D-01, D-02, D-05, D-08)."
    - "Reduced motion renders the same pane-filling bed and three masks as a static composition while heading, status, progressbar, counts, retry, and delete actions remain semantically identical (D-13, D-14)."
    - "Scoped GSAP/media owners clean up on destruction and reconfiguration; repeated start/stop/retry cycles retain one field and never delay Phase 16 readiness, focus restoration, or connected chat availability (D-12, D-15, D-16)."
  artifacts:
    - path: "src/components/startup-signal-presentation.ts"
      provides: "Pure monotonic transport/restoration/retry/exhausted/stopping visual targets"
      exports: ["StartupSignalPresentation", "projectStartupSignal"]
    - path: "src/components/StartupSignalField.svelte"
      provides: "One base texture, exactly three true ASCII masks, scoped state-responsive GSAP, and static reduced-motion composition"
      contains: "gsap.matchMedia"
    - path: "src/components/HostWorkspace.svelte"
      provides: "Authoritative semantic panel/actions above the pane-scoped field"
      contains: "startup-progress-panel"
    - path: "tests/unit/startup-signal-presentation.test.ts"
      provides: "Monotonic sequence, retry hold, exhausted settle, empty, and stopping projection proof"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Mask count/style, progress responsiveness, retry/exhaustion, reduced-motion, cleanup, shell, and handoff proof"
  key_links:
    - from: "src/components/startup-signal-presentation.ts"
      to: "src/components/StartupSignalField.svelte"
      via: "typed forward target and truthful state select GSAP/CSS targets without owning recovery"
      pattern: "forwardPercent|recoveryState"
    - from: "src/components/StartupSignalField.svelte"
      to: "gsap"
      via: "one field-scoped context and media owner create/revert ambient and short state transitions"
      pattern: "gsap.context|gsap.matchMedia|revert"
    - from: "src/components/HostWorkspace.svelte"
      to: "CoordinatorStore.startupProgress"
      via: "existing status/progressbar/live-region/retry/delete bindings remain semantic/action truth"
      pattern: "startupProgress.roomRecovery|retryRoomRecovery|requestSidebarRoomRemoval"
    - from: "tests/e2e/workspace-lifecycle.spec.ts"
      to: "src/components/StartupSignalField.svelte"
      via: "computed masks/transforms plus reduced-motion emulation inspect rendered behavior inside the content pane"
      pattern: "emulateMedia|maskImage|webkitMaskImage"
---

<objective>
Complete the content-pane startup signal with exactly three genuine ASCII masks, truthful progress-responsive GSAP motion, scoped cleanup, and a static but fully readable reduced-motion path (MOTION-02, MOTION-03; D-05 through D-16).

Purpose: Enrich the corrected pane-scoped tracer without duplicating Phase 16 recovery state, weakening shell usability, or changing the immediate ready-state handoff.
Output: Exact mask verification, state-responsive scoped animation, retry/exhaustion behavior, reduced-motion parity, and repeated-cycle browser proof.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-full-viewport-startup-motion/17-CONTEXT.md
@.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md
@.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md
@.planning/phases/17-full-viewport-startup-motion/17-PATTERNS.md
@.planning/phases/17-full-viewport-startup-motion/17-VALIDATION.md
@.planning/phases/17-full-viewport-startup-motion/17-01-SUMMARY.md
@.planning/phases/16-resilient-rooms-recovery/16-06-SUMMARY.md
@src/components/startup-signal-presentation.ts
@src/components/StartupSignalField.svelte
@src/components/HostWorkspace.svelte
@tests/unit/startup-signal-presentation.test.ts
@tests/e2e/workspace-lifecycle.spec.ts

<decision_trace>
D-05 deterministic ASCII fills the entire content pane; D-06 exactly three true ASCII masks with no independent circle substitutes; D-07 restrained GSAP drift/scale/rotation/opacity/mask/phase motion; D-08 calm contrast behind readable content; D-09 real phase/percent/recovery input; D-10 forward progress plus distinct retry/exhaustion; D-11 semantic panel authority; D-12 smooth transitions with immediate readiness; D-13 static reduced-motion composition; D-14 independent progressbar/live semantics; D-15 scoped leak-free cleanup; D-16 transforms/opacity/mask properties without per-frame Svelte writes or DOM churn. Plan 17-01 establishes D-01 through D-04 and keeps the header/rail usable; those are invariants for every task here.
</decision_trace>

<interrupted_execution>
Continue from the adapted dirty-tree work after Plan 17-01. Retain the useful projector, typed field prop/data hooks, existing deterministic texture allocation, one-bed/three-copy markup, and any valid GSAP/mask work. Do not restore the stale viewport overlay, viewport dimensions, shell hiding, shell inert/aria isolation, or the incorrect viewport-owned test. Do not reset unrelated changes. Do not introduce phase/SDLC vocabulary into source filenames, source identifiers, test names, or non-planning documentation.
</interrupted_execution>

<interfaces>
Consume the exact committed Plan 17-01 `StartupSignalPresentation` interface and `projectStartupSignal(progress, status)` export. Keep it immutable and presentation-only; do not widen it with recovery ownership, timers, callbacks, mutable store references, or shell geometry.

The signal field remains decorative (`aria-hidden`, pointer-inert), while HostWorkspace retains every semantic progress value and existing Phase 16 action call outside animation ownership.
</interfaces>
</context>

## Artifacts this plan produces

- The existing `startup-signal-presentation.ts` export refined only for monotonic presentation behavior.
- One deterministic ASCII bed and exactly three true masked copies in `StartupSignalField.svelte`.
- Scoped GSAP normal-motion and static reduced-motion behavior.
- Expanded pure unit and workspace lifecycle browser coverage; no source/test naming uses phase/SDLC vocabulary.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive exactly three true ASCII masks from monotonic recovery truth</name>
  <files>src/components/startup-signal-presentation.ts, src/components/StartupSignalField.svelte, src/components/HostWorkspace.svelte, tests/unit/startup-signal-presentation.test.ts, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-01-SUMMARY.md` — consume the committed pane-scoped geometry, projector/field contract, shell-usability evidence, and dirty-tree adaptation notes.
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — exact layer count, mask rules, state response, timing, colors, copy, pane scope, and semantic rules are normative.
    - `.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md` — update field-local targets rather than creating a parallel recovery state or custom animation scheduler.
    - `src/components/startup-signal-presentation.ts` — preserve pure read-only derivation and exact Phase 16 value fields.
    - `src/components/StartupSignalField.svelte` — retain deterministic allocation, one bed, and three existing ASCII copies within the full pane-sized field root.
    - `src/components/HostWorkspace.svelte` — preserve pane-scoped ownership, direct semantic/action bindings, and visible usable shell.
    - `tests/unit/startup-signal-presentation.test.ts` — extend table-driven interrupted tests.
    - `tests/e2e/workspace-lifecycle.spec.ts` — extend the corrected pane helper and existing retry/exhaustion observer.
  </read_first>
  <behavior>
    - Test 1: increasing transport percentages and completed-room ratios produce nondecreasing forward targets; zero rooms reaches the visual endpoint without changing `0 / 0` truth.
    - Test 2: retry retains completed work/forward target, exhaustion freezes that target, and neither state becomes semantically or visually complete unless the real ratio is complete.
    - Test 3: rendered markup contains exactly one `.ascii-bed` and exactly three `.ascii-ring` elements, and all four contain deterministic ASCII texture that reaches the content pane's edges through the field root.
    - Test 4: each ring has non-`none` standard and WebKit radial-gradient mask images, zero visible border width, no outline, and no SVG/stroke/standalone-circle fallback.
    - Test 5: real progress changes advance exposed field targets; retry holds/slows with only the progress value amber, exhaustion settles, and the exact panel copy/actions remain unchanged.
  </behavior>
  <action>
    Add or adapt the unit sequence and browser cases `startup uses exactly three masked ASCII reveals` and `startup signal follows retry and exhaustion truth` before motion changes. Refine the projector only as needed to keep verified transport and room-ratio sequences clamped and nondecreasing; retry and exhaustion preserve completed work and state identity instead of synthesizing attempts, readiness, or completion. Keep roomName as escaped display text only and retain exact completed/total fields (D-09, D-10, D-11).

    In `StartupSignalField`, retain exactly one base bed and exactly three ASCII-bearing ring copies. Apply both `mask-image` and `-webkit-mask-image` radial-gradient bands to each `.ascii-ring`; visible ring shape may not come from border, outline, SVG, stroke, or any separate circle element. The field root and base bed continue to fill the entire corrected content-pane stage, including its exact right edge; ring planes may be concentric inside that full bed but may not size or crop the bed (D-05, D-06).

    Build one field-rooted `gsap.context()` plus `gsap.matchMedia()` lifecycle. Under no-preference, keep slow ambient 5–15 second drift and issue 200–400ms ease-out/sine target changes for transform, opacity, rotation, mask position/custom properties, and restrained phase color. Use the projector's forward target so later real progress lands perceptibly later without rebuilding/resetting the ambient owner. Retry holds completed work and reduces field energy; color only the existing `.startup-progress-value` with `#e4e78d` while retrying. Exhaustion stops forward progression and settles to a lower-energy composition. Keep the dark focal contrast zone and the corrected z-index split without covering or disabling the global header/rail (D-07, D-08, D-10).

    Update only DOM transforms, opacity, and mask-related CSS/custom properties through GSAP. Keep ASCII strings allocated once; do not use a custom animation-frame loop, ticker-driven Svelte writes, texture regeneration, keyed DOM replacement, or an animation-owned readiness signal. Preserve the exact Phase 16 automatic retry visibility, exhausted retry/delete calls, generic diagnostics, and immediate connected-session handoff (D-11, D-12, D-15, D-16).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/startup-signal-presentation.test.ts &amp;&amp; pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup uses exactly three masked ASCII reveals|startup signal follows retry and exhaustion truth"</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests prove nondecreasing transport/restoration sequences plus retained retry/exhausted work without creating a second recovery state machine.
    - Browser tests count exactly one base bed and exactly three ASCII-bearing ring layers with both true mask-image properties.
    - No ring obtains its shape from border, outline, SVG, stroke, or standalone circle DOM.
    - The full field/base bed still equals the actual content pane and its exact right edge; masks do not reintroduce the original uncovered-right defect.
    - GSAP properties respond to real progress, retry, and exhaustion without resetting progress, changing semantics, covering the shell, or delaying readiness.
    - Automatic retry exposes no manual retry action; exhaustion exposes the original retry and contextual delete paths.
  </acceptance_criteria>
  <done>The content-pane field has one deterministic base texture and exactly three genuine GSAP-animated ASCII mask reveals whose forward target and energy truthfully follow normal, retry, and exhausted recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep reduced motion static and clean up every repeated startup cycle</name>
  <files>src/components/StartupSignalField.svelte, src/components/HostWorkspace.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — reduced-motion output, pointer/accessibility isolation, stopping/handoff, shell usability, and quiet-motion limits are locked.
    - `.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md` — context/media owners must revert on destruction and configuration changes.
    - `src/components/StartupSignalField.svelte` — inspect the Task 1 owner before adding preference/state reconfiguration.
    - `src/components/HostWorkspace.svelte` — semantic content/actions and surrounding shell stay outside animation ownership.
    - `tests/e2e/workspace-lifecycle.spec.ts` — reuse pane-bound, shell-action, recovery, start/stop, and media-emulation fixtures.
  </read_first>
  <behavior>
    - Test 1: with reduced motion set before load, the pane-sized stage, one bed, three masks, heading, live status, progressbar, counts, retry, and delete remain visible and semantically identical while header and rail controls remain usable.
    - Test 2: under reduced motion, sampled transforms remain stable and truthful state changes update stable exposed CSS/data targets immediately rather than tweening.
    - Test 3: under normal motion, at least one compositor-friendly field property changes over a bounded interval while status text, aria values, shell geometry, and shell usability remain unchanged by animation frames.
    - Test 4: repeated start/stop/start and retry/exhaustion/retry cycles retain one field root, one bed, three rings, no page errors, and no accumulated animation owner after unmount/reconfiguration.
    - Test 5: stopping settles the field and completion removes the pane stage immediately; the global shell never disappears, and recovered exact-session chat becomes available without waiting for an exit flourish.
  </behavior>
  <action>
    Add browser cases `startup reduced motion stays static and readable` and `startup motion cleans up across repeated recovery cycles` before changing lifecycle code. Emulate reduced motion before navigation; compare computed transforms over a bounded interval and assert identical status/progress/action semantics plus pane/stage/field bounds and shell control usability. In normal mode, observe compositor properties rather than internal GSAP objects. Exercise repeated real start/stop and retained manual retry paths; assert stable DOM counts, no page errors, and the immediate ready/chat handoff.

    Implement the reduced branch inside the same component-scoped media owner: create no nonessential timeline, set stable CSS/custom-property targets immediately for each truthful presentation state, and render the identical content-pane-sized bed/three-mask DOM. Keep the decorative root `aria-hidden` and pointer-inert, with all live semantics/actions and the global header/rail above or outside animation ownership (D-13, D-14). Normal motion stays quiet: no flashing, shake, high-frequency pulse, or sudden field-wide brightness change.

    Ensure every timeline, tween, and media registration belongs to the field's `gsap.context()`/`gsap.matchMedia()` lifecycle and is reverted on destruction and before owner replacement. Repeated projection changes may update targets but may not stack infinite timelines or reset later progress. Treat stopping/handoff as a resting target and let the existing `localRoomReady` truth remove the stage immediately; never wait for a timeline, transition event, or timeout before focus or chat availability (D-12, D-15, D-16).

    Preserve Plan 17-01 pane geometry and shell visibility/usability in both media modes. Do not reintroduce fixed/browser-viewport geometry or shell hiding/isolation, and keep shipped filenames, identifiers, and test titles free of phase/SDLC vocabulary.
  </action>
  <verify>
    <automated>pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup reduced motion stays static and readable|startup motion cleans up across repeated recovery cycles" &amp;&amp; pnpm lint &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Reduced-motion mode creates a static content-pane composition with the same one bed/three masks and complete readable semantics/actions.
    - Normal mode changes only compositor-friendly field properties while animation never writes status text, aria values, recovery state, or shell state per frame.
    - Repeated start/stop/retry/exhaustion cycles leave one mounted field owner, clean up on removal/reconfiguration, and do not grow DOM or report page errors.
    - Header and room rail remain visible, focusable, and usable in normal and reduced modes; stage/field retain exact pane/right-edge equality.
    - Stage removal, focus continuity, and recovered chat availability follow coordinator truth immediately without waiting for visual completion.
    - A bounded 15-second 1280×720 normal-start observation plus one retry transition contains only slow continuous drift and a restrained short state response, with no field-wide flash, shake, rapid pulse, or abrupt luminance jump.
  </acceptance_criteria>
  <done>Reduced motion is static and readable, normal motion is scoped and responsive, repeated cycles are leak-free, and neither mode changes content-pane scope, shell usability, or immediate readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Presentation projection → GSAP/CSS | Truthful numeric/state input selects decorative targets and must not become animation-owned recovery state. |
| Component lifecycle/media query → browser animation runtime | Repeated mount, retry, stop, and preference changes create/dispose timelines that can otherwise consume resources. |
| Decorative pane DOM → semantic/action/shell DOM | Motion shares a content pane with recovery controls while header and rail remain outside its pointer, focus, and accessibility boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering | `StartupSignalField.svelte` state targets | high | mitigate | Accept immutable projected values, preserve semantic/action bindings to CoordinatorStore, and prove monotonic/retry/exhausted correspondence in unit plus browser tests. |
| T-17-06 | Denial of Service | GSAP context/media lifecycle | medium | mitigate | Own all timelines/tweens in one field-scoped context/media registration, revert on destruction/reconfiguration, and exercise repeated cycles with stable DOM and no page errors. |
| T-17-07 | Denial of Service | Reduced-motion and pointer behavior | medium | mitigate | Create no nonessential reduced-motion timeline, keep field pointer-inert/aria-hidden, and verify readable status/actions plus usable shell and stable transforms. |
| T-17-08 | Tampering | Mask implementation | medium | mitigate | Browser tests require exactly three ASCII-bearing elements with both real mask properties and reject visible border/outline/SVG substitutes by computed DOM/style evidence. |
</threat_model>

<verification>
- `pnpm test -- tests/unit/startup-signal-presentation.test.ts`
- `pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup"`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `pnpm test`
- `pnpm build`
- `git diff --check`
- Bounded judgment: observe one 15-second 1280×720 normal start plus one retry transition; reject field-wide flashing, shaking, rapid pulsing, or abrupt luminance changes.
</verification>

<success_criteria>
- Exactly one pane-filling base ASCII bed and exactly three true masked ASCII reveal layers render; no independent circle substitute supplies their shape.
- Real transport/room recovery progress advances the GSAP field monotonically; retry retains work, exhaustion settles, and Phase 16 semantics/actions remain authoritative.
- Reduced motion has no nonessential running field timeline and keeps all status/progress/recovery semantics plus header/rail usability intact.
- Scoped cleanup survives repeated start/stop/retry/reconfiguration without stacked timelines, duplicate field DOM, browser-viewport takeover, shell isolation, or delayed readiness.
- Full project lint, type-check, unit, focused browser, build, and diff checks pass.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Smooth accessible recovery motion completely fills only the workspace content pane while retaining the surrounding shell | 17-01, 17-02 | COVERED | Pane tracer followed by masks, motion, reduced motion, and cleanup expansion. |
| REQ | MOTION-01 | ASCII field covers the workspace content container through its exact right edge at supported desktop sizes | 17-01 | COVERED | Pairwise pane/stage/field x, y, width, height, and right-edge equality at 1024×640, 1280×720, and 1440×900. |
| REQ | MOTION-02 | Rings are GSAP-animated true ASCII masks rather than static circle borders | 17-02 | COVERED | Exact layer count, texture children, computed standard/WebKit masks, and substitute rejection. |
| REQ | MOTION-03 | Smooth progress-responsive motion honors reduced motion without hiding truth | 17-01, 17-02 | COVERED | Pure projector, semantic separation, state-responsive GSAP, reduced-motion parity, and cleanup. |
| RESEARCH | — | Phase 16 remains the only recovery/retry/exhaustion/readiness owner | 17-01, 17-02 | COVERED | Pure read-only projection; original panel/actions/localRoomReady gate retained. |
| RESEARCH | — | Positioned content pane owns an absolute inset-0 100%-sized stage | 17-01 | COVERED | Fixed/browser-viewport ownership removed; exact bounds proved against `host-chat`. |
| RESEARCH | — | Header and room rail remain visible, focusable, and usable during desktop startup | 17-01, 17-02 | COVERED | Shell isolation removed; focus and activation verified at all required sizes and media modes. |
| RESEARCH | — | One deterministic base bed and three radial-gradient ASCII mask copies | 17-02 | COVERED | Allocation/DOM retained and mechanically counted. |
| RESEARCH | — | Field-local GSAP context/matchMedia uses transforms, opacity, and mask properties | 17-02 | COVERED | One scoped owner; no custom frame loop or per-frame Svelte writes. |
| RESEARCH | — | Reduced motion renders static masks while semantics remain independent | 17-02 | COVERED | Media emulation, transform stability, semantic parity, and shell usability evidence. |
| RESEARCH | — | Vitest proves projection; Playwright proves rendered browser behavior | 17-01, 17-02 | COVERED | Existing/new focused unit file and workspace lifecycle suite. |
| RESEARCH | — | Use installed Svelte/GSAP/Vitest/Playwright only | 17-01, 17-02 | COVERED | No package or lockfile changes. |
| CONTEXT | D-01 | Startup owns the workspace content pane, not the browser viewport | 17-01 | COVERED | Absolute pane child with no fixed/browser geometry. |
| CONTEXT | D-02 | Header and room rail stay visible/usable; stage reaches the pane's exact right edge | 17-01, 17-02 | COVERED | Shell focus/activation plus rectangle equality. |
| CONTEXT | D-03 | Status/settings/retry/delete remain reachable | 17-01, 17-02 | COVERED | Existing actions and focus-managed dialogs retained above field. |
| CONTEXT | D-04 | Pane-sized inset-0 100% geometry avoids document scroll and clipped actions | 17-01 | COVERED | Computed position and three-size bounds/overflow checks. |
| CONTEXT | D-05 | Deterministic ASCII reaches every pane edge | 17-01, 17-02 | COVERED | Full field/base bed plus exact right-edge proof. |
| CONTEXT | D-06 | Rings are true masks with no border substitute | 17-02 | COVERED | Computed masks and substitute rejection. |
| CONTEXT | D-07 | GSAP supplies layered restrained motion | 17-02 | COVERED | Slow ambient and short state target updates. |
| CONTEXT | D-08 | Contrast/calm center preserves content legibility | 17-01, 17-02 | COVERED | Pane-relative focal widths, z-index split, and panel contrast. |
| CONTEXT | D-09 | Real phase/percent/recovery state feeds the field | 17-01, 17-02 | COVERED | Typed projector and real browser fixtures. |
| CONTEXT | D-10 | Progress is forward and retry/exhaustion are distinct | 17-02 | COVERED | Unit sequences plus real recovery observer. |
| CONTEXT | D-11 | Textual progress remains authoritative | 17-01, 17-02 | COVERED | Direct CoordinatorStore semantic/action bindings remain outside animation. |
| CONTEXT | D-12 | Smooth transitions do not delay readiness | 17-01, 17-02 | COVERED | Immediate exact-session stage removal/chat handoff checks. |
| CONTEXT | D-13 | Reduced motion is static and readable | 17-02 | COVERED | Emulated-media transform and semantic checks. |
| CONTEXT | D-14 | Progressbar/live semantics are independent of decoration | 17-01, 17-02 | COVERED | Field aria-hidden/pointer-inert; panel remains authoritative. |
| CONTEXT | D-15 | Animation cleanup is scoped and leak-free | 17-02 | COVERED | Context/media revert plus repeated-cycle evidence. |
| CONTEXT | D-16 | Animation avoids per-frame Svelte writes and DOM churn | 17-02 | COVERED | GSAP compositor targets and stable DOM counts. |
| UI | bounds/right edge | Pane/stage/field exact equality at three desktop sizes | 17-01 | COVERED | x, y, width, height, and right edge within 1px. |
| UI | shell controls | Header and rail controls remain visible/focusable/usable | 17-01, 17-02 | COVERED | No hiding/inert/aria isolation; representative keyboard and action proof. |
| UI | loading/error/empty/partial | Phase 16 status, counts, retry, exhaustion, and deletion remain exact | 17-01, 17-02 | COVERED | Existing fixtures and action paths retained. |
| UI | overflow/long text | Short pane owns internal focal scroll and escaped labels wrap | 17-01 | COVERED | Computed overflow, reachability, and no document scroll. |
| UI | masked decoration | One bed plus three pointer-inert ASCII mask layers | 17-02 | COVERED | DOM/style count and accessibility isolation. |
| UI | reduced motion | Static identical composition with semantic/action parity | 17-02 | COVERED | Pre-load media emulation and stable transform sampling. |
| CONTEXT | Deferred | Presence, notifications, invitations, and shell-control consolidation | — | EXCLUDED | Explicitly deferred to Phase 18. |
| CONTEXT | Deferred | Grouped messages and reaction polish | — | EXCLUDED | Explicitly deferred to Phase 19. |

All GOAL, REQ, RESEARCH, and CONTEXT items are covered; no deferred idea is implemented.

<output>
Create `.planning/phases/17-full-viewport-startup-motion/17-02-SUMMARY.md` when done.
</output>
