---
phase: 17-full-viewport-startup-motion
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/startup-signal-presentation.ts
  - src/components/StartupSignalField.svelte
  - src/components/HostWorkspace.svelte
  - tests/unit/startup-signal-presentation.test.ts
  - tests/e2e/workspace-lifecycle.spec.ts
autonomous: true
requirements:
  - MOTION-02
  - MOTION-03
must_haves:
  truths:
    - "The startup field contains exactly one full-edge base ASCII bed plus three concentric ASCII reveal layers, and each reveal uses both standard and WebKit radial-gradient mask-image bands (D-05, D-06)."
    - "Normal motion is GSAP-driven from the current projected startup phase: transport and completed-room progress land perceptibly later, retry retains completed work with a calm amber value treatment, and exhaustion settles without implying completion (D-07, D-09, D-10, D-11)."
    - "Exhaustion settles the field while retaining the documented room-named error copy, `Retry recovery`, and contextual delete confirmation; automatic retry retains `Trying again…` and exposes no manual retry action."
    - "Reduced-motion startup renders the same full-edge ASCII bed and three masks as a static composition while heading, current status, recovery count, progressbar, retry, and delete actions remain semantically identical (D-13, D-14)."
    - "Scoped GSAP/media owners are reverted on destruction and reconfiguration, and repeated start/stop/retry cycles keep one live field without accumulating timelines or DOM nodes (D-15, D-16)."
    - statement: "During startup, the one base ASCII bed and all three masked reveal layers are present at the viewport edges while the field remains pointer-inert and absent from the accessibility tree."
      verification: backstop
  prohibitions:
    - statement: "The startup rings must not silently become standalone border, outline, SVG-stroke, or untextured circle decorations."
      status: resolved
      verification: test
      reason: "Task 1 acceptance criteria are enforced by the named Playwright case `startup uses exactly three masked ASCII reveals`; its focused command counts one bed and three ASCII-bearing rings, requires non-`none` standard/WebKit computed masks, rejects visible border/outline styles, and rejects SVG, stroke, or standalone circle DOM substitutes."
    - statement: "Startup motion must not use flashing, shaking, high-frequency pulses, or sudden full-screen brightness changes that turn truthful recovery into an accessibility hazard."
      status: resolved
      verification: judgment
      reason: "Automated bounds come from `startup reduced motion stays static and readable` and `startup motion cleans up across repeated recovery cycles`: reduced mode has stable sampled transforms, normal mode changes only compositor properties, and semantics stay unchanged. Residual visual judgment is bounded to one 15-second 1280x720 normal-start observation plus one retry transition; fail on any field-wide flash, shake, rapid pulse, or abrupt full-screen luminance jump, and accept only slow continuous drift with one restrained 200-400ms state response."
    - statement: "Startup motion must not delay coordinator readiness, focus restoration, recovery status changes, or chat availability for a visual flourish."
      status: resolved
      verification: test
      reason: "Task 2 acceptance criteria are enforced by the named Playwright case `startup motion cleans up across repeated recovery cycles`; its focused command asserts stage removal, normal-chrome focusability, and recovered-chat availability immediately after coordinator truth permits handoff, without waiting for or advancing an exit animation."
  artifacts:
    - path: "src/components/startup-signal-presentation.ts"
      provides: "Pure transport/restoration/retry/exhausted/stopping visual targets derived from Phase 16 truth"
      exports: ["StartupSignalPresentation", "projectStartupSignal"]
    - path: "src/components/StartupSignalField.svelte"
      provides: "One base texture, exactly three true masked ASCII reveals, scoped state-responsive GSAP, and static reduced-motion composition"
      contains: "gsap.matchMedia"
    - path: "src/components/HostWorkspace.svelte"
      provides: "Authoritative text/progress/actions above the field and state-specific visual attributes without a second state machine"
      contains: "startup-progress-panel"
    - path: "tests/unit/startup-signal-presentation.test.ts"
      provides: "Monotonic sequence, retry hold, exhausted settle, empty, and stopping projection regression proof"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Exact mask count/styles, real recovery responsiveness, retry/exhaustion truth, reduced-motion, and repeated-cycle cleanup proof"
  key_links:
    - from: "src/components/startup-signal-presentation.ts"
      to: "src/components/StartupSignalField.svelte"
      via: "typed forward target and truthful mode select GSAP/CSS targets without mutating recovery data"
      pattern: "forwardPercent|recoveryState"
    - from: "src/components/StartupSignalField.svelte"
      to: "gsap"
      via: "one field-rooted context and matchMedia owner create ambient timelines and short target tweens, then revert on cleanup/reconfiguration"
      pattern: "gsap.context|gsap.matchMedia|revert"
    - from: "src/components/HostWorkspace.svelte"
      to: "CoordinatorStore.startupProgress"
      via: "existing status/progressbar/live-region/retry/delete bindings remain the sole semantic and action truth"
      pattern: "startupProgress.roomRecovery|retryRoomRecovery|requestSidebarRoomRemoval"
    - from: "tests/e2e/workspace-lifecycle.spec.ts"
      to: "src/components/StartupSignalField.svelte"
      via: "computed mask/transform/custom-property checks and reduced-motion emulation inspect rendered behavior"
      pattern: "emulateMedia|maskImage|webkitMaskImage"
---

<objective>
Complete the startup signal experience with exactly three genuine ASCII masks, truthful progress-responsive GSAP motion, scoped cleanup, and a static but fully readable reduced-motion path (MOTION-02, MOTION-03; D-06 through D-16).

Purpose: Enrich the proven fixed viewport tracer without duplicating Phase 16 recovery state or weakening its accessible/actionable truth.
Output: State-responsive scoped animation, exact mask verification, retry/exhaustion/reduced-motion behavior, and repeated-cycle browser proof.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-full-viewport-startup-motion/17-CONTEXT.md
@.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md
@.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md
@.planning/phases/17-full-viewport-startup-motion/17-PATTERNS.md
@.planning/phases/17-full-viewport-startup-motion/17-VALIDATION.md
@.planning/phases/17-full-viewport-startup-motion/17-01-SUMMARY.md
@.planning/phases/16-resilient-rooms-recovery/16-06-SUMMARY.md
@src/components/startup-signal-presentation.ts
@src/components/StartupSignalField.svelte
@src/components/HostWorkspace.svelte
@tests/unit/startup-signal-presentation.test.ts
@tests/e2e/workspace-lifecycle.spec.ts

<decision_trace>
D-06 true ASCII masks only; D-07 restrained GSAP drift/scale/rotation/opacity/phase motion; D-09 real phase/percent/recovery input; D-10 forward progress plus distinct retry/exhaustion; D-11 semantic panel authority; D-12 smooth transitions with immediate readiness; D-13 static reduced-motion composition; D-14 independent progressbar/live semantics; D-15 scoped leak-free cleanup; D-16 transforms/opacity/mask properties without per-frame Svelte writes or DOM churn. D-01 through D-05 and D-08 were established by Plan 17-01 and remain invariants.
</decision_trace>

<interfaces>
Plan 17-01 exports:

```typescript
export interface StartupSignalPresentation {
  // Executor uses the exact committed interface from Plan 17-01.
}

export function projectStartupSignal(
  progress: CoordinatorStartupProgress,
  status: CoordinatorStatus,
): StartupSignalPresentation;
```

Consume the committed interface from `17-01-SUMMARY.md` and source rather than widening it with recovery ownership, timers, callbacks, or mutable store references.
</interfaces>
</context>

## Artifacts this phase produces

- New file `src/components/startup-signal-presentation.ts` with exported `StartupSignalPresentation` and `projectStartupSignal(progress, status)`.
- New typed `signal` prop contract on `StartupSignalField.svelte`.
- New file `tests/unit/startup-signal-presentation.test.ts`.
- New behavior coverage inside the existing `tests/e2e/workspace-lifecycle.spec.ts`; no SDLC/planning vocabulary is introduced into source or test identifiers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive exactly three true ASCII masks from monotonic recovery presentation</name>
  <files>src/components/startup-signal-presentation.ts, src/components/StartupSignalField.svelte, src/components/HostWorkspace.svelte, tests/unit/startup-signal-presentation.test.ts, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-01-SUMMARY.md` — consume the committed projector/field contract and tracer evidence.
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — exact layer count, masks, state table, timing, colors, copy, and semantic rules are normative.
    - `.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md` — use one component-local GSAP lifecycle and update targets rather than recreating timelines.
    - `src/components/startup-signal-presentation.ts` — preserve pure read-only derivation and the Phase 16 value fields.
    - `src/components/StartupSignalField.svelte` — retain deterministic texture allocation, one bed, and three existing texture copies.
    - `src/components/HostWorkspace.svelte:1502` — keep exact automatic retry/exhaustion copy and action visibility.
    - `tests/unit/startup-signal-presentation.test.ts` — extend table-driven pure-function tests.
    - `tests/e2e/workspace-lifecycle.spec.ts:389` — extend the existing multi-room retry/exhaustion observer and fixtures.
  </read_first>
  <behavior>
    - Test 1: increasing transport percentages and completed-room ratios produce nondecreasing forward targets; zero rooms reaches the visual endpoint without changing `0 / 0` recovery truth.
    - Test 2: retry retains the same completed work/forward target, exhausted freezes at that target, and neither state becomes visually complete unless the real ratio is complete.
    - Test 3: rendered markup has exactly one `.ascii-bed` and three `.ascii-ring` elements; each ring owns an ASCII texture and non-`none` standard plus WebKit radial-gradient mask images.
    - Test 4: each ring has zero visible border width, `outline-style: none`, and no SVG/stroke/circle fallback exists in the field.
    - Test 5: real recovery mutations advance the field's exposed forward target monotonically; retry slows/holds with only the progress value amber, exhaustion settles, and the panel copy/actions remain exact.
  </behavior>
  <action>
    Add the unit sequence and computed-style Playwright cases titled `startup uses exactly three masked ASCII reveals` and `startup signal follows retry and exhaustion truth` first. Refine the pure projector only as needed to keep transport and room-ratio targets clamped and forward under the verified Phase 16 inputs; retry and exhausted must preserve the completed ratio and state identity, not synthesize attempts or completion. Keep roomName as escaped display text only and retain exact completed/total values (D-09, D-10, D-11).

    In `StartupSignalField`, keep exactly the existing one base bed and three ring copies. Apply both `mask-image` and `-webkit-mask-image` radial-gradient bands to every `.ascii-ring`; do not add any separate ring element or substitute visual. Build one field-rooted `gsap.context()` plus `gsap.matchMedia()` lifecycle. Under no-preference, keep slow ambient 5–15 second drift and add 200–400ms ease-out/sine target updates for transform, opacity, rotation, mask position/custom properties, and restrained phase color. Use the projector's forward target so a later real state lands perceptibly later without rebuilding/resetting the ambient timeline. Retry holds completed work and slows field energy; in `HostWorkspace`, use the existing recovery-state hook to color only `.startup-progress-value` `#e4e78d` while retrying. Exhaustion stops forward progression and settles energy. Use `#7cf59d` only for the approved accent roles and keep the dark focal contrast zone (D-06, D-07, D-08, D-10).

    Update only DOM transforms, opacity, and mask-related CSS/custom properties through GSAP. Keep deterministic ASCII strings allocated once; do not use a custom animation frame loop, ticker-driven Svelte writes, texture regeneration, or keyed DOM replacement (D-15, D-16). Preserve the exact Phase 16 panel, automatic retry visibility, exhausted retry/delete calls, and readiness handoff (D-11, D-12).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/startup-signal-presentation.test.ts &amp;&amp; pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup uses exactly three masked ASCII reveals|startup signal follows retry and exhaustion truth"</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests prove nondecreasing normal/restoration sequences plus retained retry/exhausted work without a second recovery state machine.
    - Browser tests count exactly one base bed and exactly three ring layers, each with its own ASCII texture and both true mask-image properties.
    - No ring obtains its visible shape from border, outline, SVG, stroke, or a standalone circle.
    - GSAP transform/opacity/rotation/mask targets respond to real progress, retry, and exhaustion without resetting later progress or changing semantic values.
    - Automatic retry exposes no manual retry action; exhaustion exposes the exact retry and contextual delete paths already verified in Phase 16.
  </acceptance_criteria>
  <done>The full-edge field has one base texture and exactly three genuine, GSAP-animated ASCII mask reveals whose energy and forward target truthfully follow normal, retry, and exhausted recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make reduced motion static, semantics identical, and lifecycle cleanup leak-free</name>
  <files>src/components/StartupSignalField.svelte, src/components/HostWorkspace.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — reduced-motion output, pointer/accessibility isolation, stopping/handoff, and quiet-motion limits are locked.
    - `.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md` — revert context/media ownership on destruction and configuration changes.
    - `src/components/StartupSignalField.svelte` — inspect the Task 1 lifecycle owner before adding preference/state reconfiguration.
    - `src/components/HostWorkspace.svelte:1502` — semantic content and actions stay outside animation control.
    - `tests/e2e/workspace-lifecycle.spec.ts` — reuse recovery/start/stop fixtures and emulate media before page load.
  </read_first>
  <behavior>
    - Test 1: with `prefers-reduced-motion: reduce` set before load, the stage, one bed, three masks, heading, live status, progressbar, counts, retry, and delete remain visible and semantically identical.
    - Test 2: under reduced motion, sampled ring/texture transforms remain stable over time and truthful state changes update stable exposed CSS/data targets immediately rather than tweening.
    - Test 3: under normal motion, at least one field transform changes over an observation interval while progress text and aria values remain unchanged by animation frames.
    - Test 4: repeated start/stop/start and retry/exhaustion/retry cycles retain exactly one field root and four texture nodes, produce no page errors, and never accumulate active motion owners after unmount/reconfiguration.
    - Test 5: stopping settles the current field and completion removes the stage immediately, restores normal chrome/focusability, and makes recovered chat available without waiting for an exit flourish.
  </behavior>
  <action>
    Add Playwright cases titled `startup reduced motion stays static and readable` and `startup motion cleans up across repeated recovery cycles` before changing the component. Emulate reduced motion before navigation, compare computed transforms over a bounded observation interval, and assert semantic text/ARIA/action parity. In normal mode, observe compositor properties rather than internal GSAP objects. Exercise repeated real start/stop and retained manual retry paths; assert one field root, one base plus three rings, stable DOM node counts, restored normal chrome, no page errors, and immediate ready/chat handoff.

    Implement the reduce branch inside the same component-scoped media owner: create no nonessential GSAP timeline, set stable CSS/custom-property targets immediately for each truthful presentation state, and render the same ASCII/mask DOM. Keep `aria-hidden` and `pointer-events: none` on the decorative root, with all live semantics and controls in HostWorkspace above and outside animation ownership (D-13, D-14). For normal motion, retain only quiet ambient cycles and short state transitions; omit flashing, shake, high-frequency pulse, and full-screen brightness jumps.

    Ensure every timeline/tween/media registration is owned by the field's `gsap.context()`/`gsap.matchMedia()` and reverted both when the component is destroyed and when preference/state configuration replaces an owner. Do not let repeated projection changes stack infinite timelines; update or replace scoped targets while preserving current transforms. Treat stopping/handoff as a resting target and remove the stage as soon as coordinator truth permits (D-12, D-15, D-16). Keep contextual retry/removal controls and text-only room-name interpolation unchanged.
  </action>
  <verify>
    <automated>pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup reduced motion stays static and readable|startup motion cleans up across repeated recovery cycles" &amp;&amp; pnpm lint &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Reduced-motion mode creates a static full-viewport composition with the same one base/three masks and complete readable semantics/actions.
    - Normal mode visibly changes compositor-friendly field properties while animation never writes progress text or ARIA values per frame.
    - Repeated start/stop/retry/exhaustion cycles leave one mounted field owner, clean up on removal/reconfiguration, and do not grow field DOM or report page errors.
    - Decorative DOM remains pointer-inert and absent from the accessibility tree in every mode.
    - In `startup motion cleans up across repeated recovery cycles`, stopping and completion settle/remove presentation in the same coordinator-ready handoff without advancing a timer or waiting for `transitionend`/animation completion; normal chrome becomes focusable and recovered chat becomes available immediately.
    - End-of-phase judgment observes one 15-second 1280x720 normal start plus one retry transition and rejects any field-wide flash, shake, rapid pulse, or abrupt full-screen luminance jump; only slow continuous drift and a restrained 200-400ms state response satisfy the motion-safety prohibition.
  </acceptance_criteria>
  <done>Motion preference and repeated lifecycle cycles are fully automated: reduced motion is static/readable, normal motion is scoped/responsive, and cleanup preserves one leak-free presentation owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Presentation projection → GSAP/CSS | Truthful numeric/state input selects decorative targets and must not become animation-owned recovery state. |
| Component lifecycle/media query → browser animation runtime | Repeated mount, retry, stop, and preference changes create and dispose timelines that can otherwise consume resources. |
| Decorative full-screen DOM → semantic/action DOM | Motion and masks share the viewport with recovery status and destructive controls but cannot intercept or replace them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering | `StartupSignalField.svelte` state targets | high | mitigate | Accept immutable projected values, preserve panel/action bindings to `CoordinatorStore`, and prove monotonic/retry/exhausted correspondence in unit plus browser tests. |
| T-17-06 | Denial of Service | GSAP context/media lifecycle | medium | mitigate | Own all timelines/tweens in one field-scoped context/media registration, revert on destruction/reconfiguration, and exercise repeated start/stop/retry cycles with stable DOM and no page errors. |
| T-17-07 | Denial of Service | Reduced-motion and pointer behavior | medium | mitigate | Create no nonessential reduce-mode timeline, keep the field pointer-inert/aria-hidden, and verify readable status/actions plus stable transforms under emulated reduced motion. |
| T-17-08 | Tampering | Mask implementation | medium | mitigate | Browser tests require exactly three ASCII-bearing elements with both real mask properties and reject visible border/outline/SVG substitutes by computed DOM/style evidence. |
</threat_model>

<verification>
- `pnpm test -- tests/unit/startup-signal-presentation.test.ts`
- `pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup"`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `pnpm test`
- `pnpm build`
- `git diff --check`
- Bounded judgment: observe one 15-second 1280x720 normal start plus one retry transition; fail on field-wide flashing, shaking, rapid pulsing, or abrupt full-screen luminance jumps, and record the result against the resolved motion-safety prohibition.
</verification>

<success_criteria>
- Exactly one base ASCII bed and exactly three true masked ASCII reveal layers render; no independent circle substitute supplies their shape.
- Real transport/room recovery progress advances the GSAP field monotonically; retry retains work, exhaustion settles, and the Phase 16 panel/actions remain authoritative.
- Reduced motion has no nonessential running field timeline and keeps all status/progress/recovery semantics readable.
- Scoped cleanup survives repeated start/stop/retry/reconfiguration without stacked timelines, duplicate field DOM, or delayed readiness.
- Full project lint, type-check, unit, focused Playwright, build, and diff checks pass.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Smooth accessible recovery motion fills the desktop viewport | 17-01, 17-02 | COVERED | Fixed tracer followed by masks/motion/accessibility expansion. |
| REQ | MOTION-01 | ASCII field covers every supported desktop viewport without a side gutter | 17-01 | COVERED | Exact bounds, adjacency, scroll, chrome isolation, and three-size Playwright matrix. |
| REQ | MOTION-02 | Rings are GSAP-animated true ASCII masks rather than static circle borders | 17-02 | COVERED | Exact layer count, texture children, computed masks, and substitute rejection. |
| REQ | MOTION-03 | Smooth progress-responsive motion honors reduced motion without hiding truth | 17-01, 17-02 | COVERED | Pure projector, state-responsive GSAP, semantics parity, and cleanup. |
| RESEARCH | — | Phase 16 remains the only recovery/retry/exhaustion/readiness state owner | 17-01, 17-02 | COVERED | Pure read-only projector; existing semantic/action bindings retained. |
| RESEARCH | — | Fixed overlay owns viewport and focal column owns any short-screen scroll | 17-01 | COVERED | Tracer plus matrix/overflow expansion. |
| RESEARCH | — | One deterministic base bed and three radial-gradient ASCII mask copies | 17-02 | COVERED | Existing allocation/DOM retained and mechanically counted. |
| RESEARCH | — | Field-local GSAP context/matchMedia with transforms, opacity, and mask properties | 17-02 | COVERED | One scoped owner; no custom frame loop or per-frame Svelte writes. |
| RESEARCH | — | Reduced motion renders static masks while semantics remain independent | 17-02 | COVERED | Media emulation and transform/semantic parity evidence. |
| RESEARCH | — | Vitest proves the projector and Playwright proves rendered browser behavior | 17-01, 17-02 | COVERED | New behavior-named unit file and existing workspace lifecycle suite. |
| RESEARCH | — | Use installed Svelte/GSAP/Vitest/Playwright only | 17-01, 17-02 | COVERED | No package or lockfile changes and no external service/API. |
| CONTEXT | D-01 | Startup/stopping owns the entire viewport | 17-01 | COVERED | Fixed 100vw × 100dvh stage. |
| CONTEXT | D-02 | Normal chrome leaves no gutter and is noninteractive | 17-01 | COVERED | Explicit visual/inert/aria isolation. |
| CONTEXT | D-03 | Status/settings/retry/delete remain reachable | 17-01, 17-02 | COVERED | Existing panel/actions retained above field; dialog tested. |
| CONTEXT | D-04 | Desktop and short viewports avoid document scroll/clipped actions | 17-01 | COVERED | Three-size bounds plus internal overflow checks. |
| CONTEXT | D-05 | Deterministic ASCII reaches every viewport edge | 17-01 | COVERED | Existing seeded textures and full-edge bed. |
| CONTEXT | D-06 | Rings are true masks with no border substitute | 17-02 | COVERED | Computed mask/substitute assertions. |
| CONTEXT | D-07 | GSAP supplies layered restrained motion | 17-02 | COVERED | Ambient and short state targets. |
| CONTEXT | D-08 | Contrast/calm center preserves content legibility | 17-01, 17-02 | COVERED | Z-index/focal widths/panel contrast retained. |
| CONTEXT | D-09 | Real phase/percent/recovery state feeds the field | 17-01, 17-02 | COVERED | Typed pure projector and real browser flow. |
| CONTEXT | D-10 | Progress is forward and retry/exhaustion are distinct | 17-02 | COVERED | Unit sequences plus recovery observer. |
| CONTEXT | D-11 | Textual progress remains authoritative | 17-01, 17-02 | COVERED | Direct store bindings remain outside animation. |
| CONTEXT | D-12 | Smooth transitions do not delay readiness | 17-01, 17-02 | COVERED | Immediate stage removal/chat handoff checks. |
| CONTEXT | D-13 | Reduced motion is static and readable | 17-02 | COVERED | Emulated-media transform and semantic checks. |
| CONTEXT | D-14 | Progressbar/live semantics are independent of decoration | 17-01, 17-02 | COVERED | Field aria-hidden; panel stays authoritative. |
| CONTEXT | D-15 | Animation cleanup is scoped and leak-free | 17-02 | COVERED | Context/media revert plus repeated-cycle evidence. |
| CONTEXT | D-16 | Animation avoids per-frame Svelte writes and DOM churn | 17-02 | COVERED | GSAP compositor targets and stable DOM counts. |
| UI | loading/status | Full-stage loading with semantic Phase 16 truth | 17-01 | COVERED | Plain must-have truth. |
| UI | error/actions | Exhaustion settles and retains exact recovery actions | 17-02 | COVERED | Plain must-have truth. |
| UI | empty | Zero-room documented copy and successful continuation | 17-01 | COVERED | Plain must-have truth. |
| UI | partial | Exact count/current-room and shared monotonic ratio | 17-01 | COVERED | Plain must-have truth. |
| UI | overflow | Desktop/short viewport held-out layout proof | 17-01 | COVERED | Structured `verification: backstop` truth. |
| UI | long-text | Wrapped label with independent numeric space | 17-01 | COVERED | Plain must-have truth. |
| UI | decorative loading | Full-edge one-bed/three-mask pointer/accessibility isolation | 17-02 | COVERED | Structured `verification: backstop` truth. |
| EDGE | adjacency | Field/stage edges meet exactly without gap or scroll-causing overlap | 17-01 | COVERED | Rectangle plus document dimension assertions. |
| EDGE | empty | Startup remains meaningful with zero recovery targets | 17-01 | COVERED | Exact copy and successful continuation. |
| EDGE | ordering | Field/content/dialog stacking and DOM order are deterministic | 17-01 | COVERED | DOM/computed z-index checks. |
| EDGE | mask classification | Exactly three true mask-image layers and no substitutes | 17-02 | COVERED | Computed style and DOM checks. |
| EDGE | state motion | Forward progress, retry/exhaustion, and reduced-motion semantics | 17-02 | COVERED | Unit and browser behavior evidence. |
| CONTEXT | Deferred | Presence/notifications/invitations/shell-control consolidation | — | EXCLUDED | Explicitly deferred to Phase 18. |
| CONTEXT | Deferred | Grouped messages and reaction polish | — | EXCLUDED | Explicitly deferred to Phase 19. |

UI Considerations report 0 unresolved items; no planner assumption is required. The recalled prohibitions are resolved above with explicit test or bounded-judgment descriptors tied to named Task 1/2 evidence.

<output>
Create `.planning/phases/17-full-viewport-startup-motion/17-02-SUMMARY.md` when done.
</output>
