---
phase: 17-full-viewport-startup-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/startup-signal-presentation.ts
  - src/components/StartupSignalField.svelte
  - src/components/HostWorkspace.svelte
  - tests/unit/startup-signal-presentation.test.ts
  - tests/e2e/workspace-lifecycle.spec.ts
autonomous: true
requirements:
  - MOTION-01
  - MOTION-03
must_haves:
  truths:
    - "During start, stop, room recovery, exhaustion, and recovered-room handoff, the startup stage replaces only the workspace content pane and matches that pane's x, y, width, height, and right edge within 1px (D-01, D-02, D-04)."
    - "The global header/topbar and desktop room rail remain visible, enabled, focusable, and usable while startup is shown; the decorative stage never owns or isolates the browser viewport or surrounding shell (D-02, D-03)."
    - "The startup stage is absolute inside a positioned content pane with inset 0 and 100% width/height; startup geometry never uses a fixed overlay, viewport-sized units, or browser-window measurements (D-04)."
    - "Phase 16 remains the sole source of recovery phase, current room, aggregate progress, retry/exhaustion, deletion, and readiness truth; chat appears only after completed recovery plus an attached connected exact session (D-09, D-11, D-12, D-14)."
    - "A pure presentation projection carries coordinator truth into a pointer-inert ASCII field without mutating the coordinator or delaying the immediate ready-state handoff (D-09, D-10, D-12)."
  artifacts:
    - path: "src/components/startup-signal-presentation.ts"
      provides: "Pure read-only mapping from CoordinatorStartupProgress to visual targets"
      exports: ["StartupSignalPresentation", "projectStartupSignal"]
    - path: "src/components/StartupSignalField.svelte"
      provides: "Pane-filling decorative ASCII field consuming the typed projection"
      contains: "data-testid=\"startup-ascii-field\""
    - path: "src/components/HostWorkspace.svelte"
      provides: "Positioned content-pane ownership with the surrounding header and rail preserved"
      contains: "startup-stage"
    - path: "tests/unit/startup-signal-presentation.test.ts"
      provides: "Transport, room-ratio, empty, retry, exhausted, and stopping projection proof"
    - path: "tests/e2e/workspace-lifecycle.spec.ts"
      provides: "Three-size pane-bound equality, shell usability, recovery truth, and immediate handoff proof"
  key_links:
    - from: "src/components/HostWorkspace.svelte"
      to: "src/components/startup-signal-presentation.ts"
      via: "a Svelte derived value projects existing CoordinatorStore startup truth"
      pattern: "projectStartupSignal.*startupProgress"
    - from: "src/components/HostWorkspace.svelte"
      to: "src/components/StartupSignalField.svelte"
      via: "the typed projection feeds only the decorative field while semantic UI reads CoordinatorStore directly"
      pattern: "StartupSignalField"
    - from: "tests/e2e/workspace-lifecycle.spec.ts"
      to: "src/components/HostWorkspace.svelte"
      via: "Playwright compares pane, stage, and field rectangles and activates representative shell controls"
      pattern: "host-chat|startup-ascii-field|invite-panel"
---

<objective>
Correct the startup tracer so real Phase 16 recovery truth drives a content-pane-scoped ASCII surface while the global header and room rail remain visible and usable (MOTION-01, MOTION-03; D-01 through D-05, D-08 through D-12, D-14).

Purpose: Establish the production ownership boundary before motion enrichment: the startup presentation fills only the actual workspace content pane, especially its exact right edge, and never takes over the browser viewport or disables the surrounding shell.
Output: A retained pure projector and typed signal input, corrected pane-scoped geometry, preserved Phase 16 semantics/actions/handoff, and focused unit plus Playwright proof at the required desktop sizes.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-full-viewport-startup-motion/17-CONTEXT.md
@.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md
@.planning/phases/17-full-viewport-startup-motion/17-RESEARCH.md
@.planning/phases/17-full-viewport-startup-motion/17-PATTERNS.md
@.planning/phases/17-full-viewport-startup-motion/17-VALIDATION.md
@.planning/phases/16-resilient-rooms-recovery/16-06-SUMMARY.md
@src/coordinator/types.ts
@src/coordinator/coordinator.svelte.ts
@src/components/HostWorkspace.svelte
@src/components/StartupSignalField.svelte
@src/components/startup-signal-presentation.ts
@tests/unit/startup-signal-presentation.test.ts
@tests/e2e/workspace-lifecycle.spec.ts

<decision_trace>
The source CONTEXT groups locked bullets without numeric labels; these citations assign them in source order. D-01 startup owns the workspace content pane only; D-02 the header and room rail remain visible/usable while stage and field reach the pane's exact right edge; D-03 status/settings/retry/delete remain reachable; D-04 the positioned pane contains an absolute inset-0 100%-sized stage without fixed/viewport geometry or document scrolling; D-05 deterministic ASCII reaches every pane edge; D-06 rings are true ASCII masks; D-07 GSAP supplies restrained layered motion; D-08 a calm contrast zone preserves legibility; D-09 real phase/percent/recovery truth drives presentation; D-10 progress moves forward and retry/exhaustion remain distinct; D-11 textual progress is authoritative; D-12 transitions never delay readiness; D-13 reduced motion is static and readable; D-14 progressbar/live semantics remain independent; D-15 cleanup is component-scoped; D-16 animation avoids per-frame Svelte writes and DOM churn.
</decision_trace>

<interrupted_execution>
The working tree contains an interrupted implementation. Preserve and adapt the useful `projectStartupSignal` helper, its unit tests, the typed `signal` prop/data hooks, and any valid signal-field work. Do not reset or discard unrelated dirty-tree work. Remove the stale `startupViewportOwned`, `startup-viewport-owned`, and `viewport-owned` ownership branch; remove its fixed overlay/viewport dimensions and its header, rail, and management visibility, pointer, inert, and aria isolation. Rewrite the incorrect viewport-owned browser assertion into pane-bound and shell-usability proof. Do not introduce phase/SDLC vocabulary into source filenames, source identifiers, test names, or non-planning documentation.
</interrupted_execution>

<interfaces>
From src/coordinator/types.ts, consume the existing `CoordinatorStatus`, `CoordinatorStartupProgress`, and hosted-room recovery fields without widening their ownership.

Existing Phase 16 action contracts in HostWorkspace.svelte remain the only action paths:
- `coordinator.retryRoomRecovery()` for exhausted recovery.
- `requestSidebarRoomRemoval(exhaustedRecoveryRoom, trigger)` for contextual failed-room deletion.
- `localRoomReady` requires completed recovery plus an attached connected exact session before chat renders.
- The semantic `role="progressbar"` and polite `role="status"` continue to read `coordinator.startupProgress` directly.
</interfaces>
</context>

## Artifacts this plan produces

- `src/components/startup-signal-presentation.ts` exporting `StartupSignalPresentation` and `projectStartupSignal(progress, status)`.
- A typed `signal` prop on `StartupSignalField.svelte`.
- `tests/unit/startup-signal-presentation.test.ts`.
- Pane-bound and shell-usability coverage inside the existing workspace lifecycle browser suite.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Adapt the interrupted startup slice to the workspace content pane</name>
  <files>src/components/startup-signal-presentation.ts, src/components/StartupSignalField.svelte, src/components/HostWorkspace.svelte, tests/unit/startup-signal-presentation.test.ts, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — content-pane ownership, exact right-edge equality, focal widths, shell usability, semantic ownership, and immediate handoff are locked.
    - `.planning/phases/17-full-viewport-startup-motion/17-PATTERNS.md` — use `host-chat` as the positioned containing block and the embedded-chat bounds helper as the layout analog.
    - `.planning/phases/16-resilient-rooms-recovery/16-06-SUMMARY.md` — preserve the generation-owned transaction, three-attempt policy, generic diagnostics, exhausted actions, and exact-session reveal gate.
    - `src/components/HostWorkspace.svelte` — inspect the uncommitted fixed-overlay and shell-isolation branch before adapting it in place.
    - `src/components/StartupSignalField.svelte` — retain the typed input and deterministic field skeleton already introduced.
    - `src/components/startup-signal-presentation.ts` — retain the pure browser-independent projection where it matches the approved mapping.
    - `tests/unit/startup-signal-presentation.test.ts` — extend the existing interrupted coverage rather than replacing it.
    - `tests/e2e/workspace-lifecycle.spec.ts` — adapt the uncommitted viewport-owned test and reuse the existing pane-alignment pattern.
  </read_first>
  <behavior>
    - Test 1: transport percent and room completion project to forward visual targets while exact phase, recovery state, completed, total, and room name remain copied without fixture mutation.
    - Test 2: at 1280×720 the `host-chat` pane, startup stage, and ASCII field have equal x, y, width, height, and right edge within 1px, while the stage is smaller and offset from the browser viewport by the visible shell.
    - Test 3: computed geometry reports a positioned content pane, an absolute stage with inset 0 and 100% resolved width/height, and an absolute field filling that stage.
    - Test 4: the global header and desktop rail are visible, not inert, not aria-hidden, keyboard-focusable, and their representative existing controls can be activated without the stage intercepting interaction.
    - Test 5: the field exposes non-sensitive projected state while the visible status, progressbar values, retry/delete visibility, and local-room reveal gate remain bound to Phase 16 truth.
  </behavior>
  <action>
    Start by adapting the interrupted browser case into `startup fills the workspace content pane and keeps shell controls usable`, and observe it fail against the current fixed overlay/shell isolation. Retain the existing pure projector and typed field input, extending the unit cases for clamping, zero-room completion, retry/exhausted identity, and stopping/resting only where needed. The helper stays browser-free and side-effect-free: it may copy and clamp presentation inputs, but it must not own timers, infer recovery outcomes, mutate CoordinatorStore, import Svelte/GSAP, or reinterpret diagnostics (D-09, D-10, D-11, D-12).

    In `HostWorkspace`, remove the uncommitted viewport-ownership predicate/classes and all shell hiding/isolation derived from it. Keep the global header, desktop room rail, and their normal controls rendered and interactive during startup. Make `host-chat` the positioned containing block and make `.startup-stage` pane-scoped with `position: absolute`, `inset: 0`, `width: 100%`, `height: 100%`, and `overflow: hidden`; the field root is likewise absolute and inset to all four stage edges. Do not use a fixed stage, `100vw`, `100dvh`, browser-window dimensions, or any startup state that hides, inerts, aria-hides, blurs, or captures interaction from the global header or rail (D-01, D-02, D-04).

    Preserve the existing conditional ordering: local chat wins only for `localRoomReady`; otherwise the startup branch remains through starting, stopping, recovery, exhaustion, and the short running-plus-attached-session handoff. Keep the existing semantic panel, retry call, exact contextual deletion path, and immediate stage removal when `localRoomReady` becomes true. Retain the starting-branch `Review settings` action only if it uses the existing focus-managed dialog and does not replace or duplicate a still-usable global shell control (D-03, D-11, D-12, D-14).

    Size the focal column relative to the pane as `min(512px, calc(100% - 32px))`, size the panel as `min(448px, 100%)`, and confine short-pane vertical overflow to the focal column. Keep the decorative field at z-index 0, `aria-hidden`, and pointer-inert; keep readable content/actions above it at z-index 1. Preserve escaped text interpolation and never expose room names in selectors, styles, or data hooks. Adapt the interrupted source and tests in place; do not reset other working-tree changes and do not introduce phase/SDLC naming into shipped code or tests.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/startup-signal-presentation.test.ts &amp;&amp; pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup fills the workspace content pane and keeps shell controls usable"</automated>
  </verify>
  <acceptance_criteria>
    - The corrected browser case is observed failing against the interrupted fixed-overlay implementation and passing after adaptation.
    - At 1280×720, pane/stage/field x, y, width, height, and right edge match within 1px; the stage is `absolute` inside `host-chat`, not a browser overlay.
    - Header and desktop rail remain visible and usable: representative controls receive keyboard focus and complete their existing action without pointer interception.
    - No startup predicate/class hides or isolates the header, rail, or management shell; normal compact/mobile rail behavior outside this desktop contract remains intact.
    - The useful projector, typed signal prop, data hooks, and valid field work from the interrupted implementation are retained and adapted rather than discarded.
    - CoordinatorStore, retry timing/attempts, generic diagnostics, exact deletion, and `localRoomReady` semantics are unchanged.
  </acceptance_criteria>
  <done>One real coordinator start now projects verified recovery truth into a production pane-scoped signal field while the global header and room rail remain visible, focusable, and usable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every desktop pane edge and preserve recovery actions through handoff</name>
  <files>src/components/HostWorkspace.svelte, src/components/StartupSignalField.svelte, tests/e2e/workspace-lifecycle.spec.ts</files>
  <read_first>
    - `.planning/phases/17-full-viewport-startup-motion/17-UI-SPEC.md` — apply all three desktop sizes, exact pane/stage/field equality, right-edge proof, 16px inline clearance, internal overflow, and shell-action requirements.
    - `src/components/HostWorkspace.svelte` — preserve the corrected Task 1 content-pane architecture and Phase 16 recovery branch ordering.
    - `src/components/StartupSignalField.svelte` — retain one field root filling the stage and its non-sensitive presentation hooks.
    - `tests/e2e/workspace-lifecycle.spec.ts` — extend the Task 1 pane helper plus existing zero-room, multi-room retry/exhaustion, settings, and contextual deletion fixtures.
  </read_first>
  <behavior>
    - Test 1: at 1024×640, 1280×720, and 1440×900, pane/stage/field x, y, width, height, and right edge are pairwise equal within 1px, with no document-level horizontal or vertical overflow.
    - Test 2: at each required size, a representative header control and a representative rail control remain visible, enabled, non-inert, outside aria-hidden ancestry, keyboard-focusable, and usable.
    - Test 3: zero targets show `No rooms to restore` and `0 of 0 rooms restored`; partial recovery keeps exact current room/completed/total/progressbar truth and a nondecreasing presentation target.
    - Test 4: automatic retry shows `Trying again…` without manual retry; exhaustion shows exact room-named copy, `Retry recovery`, and the existing contextual failed-room confirmation.
    - Test 5: a long escaped room name wraps without numeric collision or document overflow, visible actions remain reachable in a short pane, and successful exact-session recovery removes the stage and reveals chat immediately.
  </behavior>
  <action>
    Extend the Task 1 helper into a table-driven rectangle comparison for 1024×640, 1280×720, and 1440×900. Compare the actual `host-chat`, `.startup-stage`, and `startup-ascii-field` rectangles pairwise: x/left, y/top, width, height, and computed right edge must each differ by no more than 1px. Also prove the stage is pane-scoped rather than browser-scoped by retaining the visible topbar/rail offsets and by activating one existing header control plus one existing room-rail/context control at every size. Assert those shell regions are visible, enabled, focusable, not inert, not aria-hidden, and not covered by the decorative stage (D-01, D-02, D-03, D-04).

    Add or adapt focused cases named `startup covers every supported content pane`, `startup recovery states stay truthful`, and `startup handoff keeps actions reachable`. Reuse the existing recovery fixtures and assertions rather than building a parallel state machine. Cover zero targets, partial progress, automatic retry, exhaustion, contextual deletion, a markup-like long room name rendered as text, focal-column scrolling on the short pane, and the successful exact-session handoff. Keep the progressbar/live region authoritative and keep stage removal synchronous with the existing readiness truth rather than an animation callback (D-09, D-10, D-11, D-12, D-14).

    Preserve non-startup layout, compact/mobile rail behavior, settings/removal dialog focus management, and all dirty-tree work outside these files. Use behavior and computed-layout assertions rather than screenshots, fixed sleeps, or browser-viewport equality. Keep shipped filenames, identifiers, and test titles free of phase/SDLC vocabulary.
  </action>
  <verify>
    <automated>pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup covers every supported content pane|startup recovery states stay truthful|startup handoff keeps actions reachable" &amp;&amp; pnpm lint &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All three required desktop sizes have pairwise pane/stage/field x, y, width, height, and right-edge equality within 1px, including exact coverage of the pane's full right portion.
    - The stage remains absolute and pane-contained at every size; it creates no document-level scroll and never uses browser-viewport ownership.
    - Header and room rail are visible and usable at every required desktop size, and representative focus/activation assertions prove the stage cannot intercept them.
    - Zero, partial, retry, exhausted, deletion, long-text, and immediate handoff behavior remains exactly consistent with Phase 16 truth.
    - Existing non-startup and compact/mobile workspace behavior is unchanged.
  </acceptance_criteria>
  <done>Automated browser evidence proves exact content-pane coverage through the right edge at every supported desktop size while shell controls, recovery truth, actions, and immediate chat handoff remain usable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoordinatorStore → presentation projector | Verified recovery truth crosses into decorative visual targets but must remain coordinator-owned. |
| Content pane → startup decoration | The stage fills one positioned grid cell while the surrounding header and room rail remain outside its pointer/focus boundary. |
| Stored room display text → startup DOM | A persisted room name crosses into status/error copy and must remain escaped text rather than markup, selectors, or styles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `projectStartupSignal` / semantic panel | high | mitigate | Keep projection pure/read-only, keep text/actions bound directly to CoordinatorStore, and prove exact values plus unchanged retry/removal paths in unit and browser tests. |
| T-17-02 | Denial of Service | Pane stage versus global shell controls | high | mitigate | Constrain stage/field to the positioned content pane, keep them pointer-inert, and prove visible/focusable/activatable header and rail controls at each desktop size. |
| T-17-03 | Tampering | Room-name rendering | high | mitigate | Preserve ordinary Svelte text interpolation; exclude names from data hooks, CSS values, selectors, and HTML injection; exercise markup-like long text in Playwright. |
| T-17-04 | Information Disclosure | Recovery diagnostics | high | mitigate | Preserve Phase 16 generic room-named copy and tests that reject relay, signer, invite, storage, key, and decrypted-message details. |
</threat_model>

<verification>
- `pnpm test -- tests/unit/startup-signal-presentation.test.ts`
- `pnpm test:e2e -- tests/e2e/workspace-lifecycle.spec.ts -g "startup"`
- `pnpm lint`
- `pnpm exec tsc --noEmit`
- `git diff --check`
</verification>

<success_criteria>
- Startup stage and ASCII field exactly equal the workspace content pane, including its right edge, at 1024×640, 1280×720, and 1440×900.
- No startup code takes browser-viewport ownership or hides, inerts, aria-hides, or intercepts the global header or desktop room rail.
- Real Phase 16 progress produces a typed read-only presentation without changing retry, exhaustion, deletion, diagnostics, or readiness behavior.
- Shell focus/activation, zero/partial/retry/exhaustion/long-text behavior, and immediate exact-session handoff have automated browser evidence.
</success_criteria>

<output>
Create `.planning/phases/17-full-viewport-startup-motion/17-01-SUMMARY.md` when done.
</output>
